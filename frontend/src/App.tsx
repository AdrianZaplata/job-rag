// Wave 3a stub — Plan 04-05 replaces with the full routes tree.
// This stub renders nothing meaningful so npm run build + dev succeed pre-Plan-05.
export default function App() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>job-rag (Wave 3a placeholder)</h1>
      <p>Routes land in Plan 04-05.</p>
    </main>
  )
}
