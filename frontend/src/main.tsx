// Task 1 scaffold-only stub — Task 2 replaces with full AUTH-07 race-fix bootstrap.
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './app.css'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <main>Scaffold</main>
  </StrictMode>,
)
