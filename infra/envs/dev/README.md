# Dev environment (scaffold-only)

> **Not applied in v1.** Per CONTEXT.md D-04 the dev environment is scaffold-only — `terraform plan` works as a sanity check; `terraform apply` is documented but deferred. Cost stays at strict €0.

---

## Why scaffold-only?

_Plan 06 fills the rationale: V1 is single-user (Adrian) demo; dev provisioning would burn the €0 budget without delivering value. Apply path stays documented for the day staging actually becomes useful._

## Apply path (deferred)

_Plan 06 fills the documented-but-not-executed apply path._
