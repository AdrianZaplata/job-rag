---
phase: 4
slug: frontend-shell-auth
status: draft
shadcn_initialized: false
preset: new-york (planned — initialized during plan execution per D-20)
created: 2026-05-19
revised: 2026-05-19 (rev 1 — typography + spacing blockers, tightened CTAs, account-menu aria-label)
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Frontend Shell + Auth. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Note on `shadcn_initialized: false`:** Phase 4 D-01 creates `frontend/` from scratch. The plan executor runs `npm create vite@latest frontend -- --template react-ts` and then `npx shadcn@latest init -t vite` with the new-york preset locked in D-20. The frontmatter flips to `true` after the planner's shadcn-init plan-task lands; the design contract below is what the executor must produce.

---

## 0. Source-of-Truth Map

Every value below traces to a locked upstream decision. The checker should cross-reference this column when validating.

| Contract Section | Source |
|------------------|--------|
| Design System (tool, preset, libs, font) | CONTEXT.md D-20 + STACK.md §1 + `<design_system_locked>` orchestrator block |
| Theme mode (light+dark, default dark, toggle, persistence) | CONTEXT.md D-20 + orchestrator `<design_system_locked>` |
| Spacing scale | Tailwind v4 default + Linear-dense reduction (CONTEXT.md PROJECT.md "Linear-dense aesthetic") |
| Typography (Geist Sans / Geist Mono via `@fontsource/*`) | CONTEXT.md D-20 + orchestrator `<design_system_locked>` |
| Color (zinc accent on zinc grays, 60/30/10) | CONTEXT.md D-20 + PROJECT.md "grayscale + one accent" |
| Component inventory (first-wave shadcn set) | CONTEXT.md "Claude's Discretion" `Pre-scaffolded shadcn components` + orchestrator `<design_system_locked>` |
| Route table + AppShell shape | CONTEXT.md D-17, D-18 + `<phase_scope_for_ui>` items 1-10 |
| AccessDenied page (oid copy-block + clipboard) | CONTEXT.md D-09 + `<phase_scope_for_ui>` item 3 |
| EmptyState pattern + phase placeholders | CONTEXT.md D-19c/d + `<phase_scope_for_ui>` item 4 |
| ErrorBoundary fallback | CONTEXT.md D-19a + `<phase_scope_for_ui>` item 6 |
| Route Suspense fallback (`RouteSkeleton`) | CONTEXT.md D-19b + `<phase_scope_for_ui>` item 7 |
| Hidden `/debug/agent-stream` page | CONTEXT.md D-16 + `<specifics>` "dev-only gate" + `<phase_scope_for_ui>` item 10 |
| AUTH-07 first-paint policy | CONTEXT.md D-05 + `<phase_scope_for_ui>` item 11 |
| Copywriting (CTAs, OID runbook, empty/error states) | researcher inferred from REQUIREMENTS SHEL-01..06 + AUTH-01..07 + CONTEXT.md D-09 OID-bootstrap UX |
| Registry safety | shadcn official only (no third-party blocks in Phase 4) |

---

## 1. Design System

| Property | Value | Notes |
|----------|-------|-------|
| Tool | shadcn/ui | initialized as part of plan execution, not pre-existing |
| Preset | new-york | locked by CONTEXT.md D-20 + STACK.md §1 |
| Style mode | both light + dark, default dark | top-nav `<ThemeToggle>` |
| Theme persistence | `localStorage['theme']` with fallback to `window.matchMedia('(prefers-color-scheme: dark)')` | applied via Tailwind `class="dark"` on `<html>` |
| Component library | Radix UI primitives (transitive via shadcn) | no direct dep management |
| Variant tool | `class-variance-authority` (`cva`) | shadcn default |
| `cn()` utility | `clsx` + `tailwind-merge` | shadcn-generated `lib/utils.ts` |
| Icon library | `lucide-react` | shadcn default; tree-shakeable SVGs |
| CSS framework | Tailwind CSS v4.2.x via `@tailwindcss/vite` | `@theme inline` block in `app.css` per STACK.md §1 |
| UI font | Geist Sans | self-hosted via `@fontsource/geist-sans` |
| Code/mono font | Geist Mono | self-hosted via `@fontsource/geist-mono` (load-bearing for OID code-block and `/debug/agent-stream` log) |
| Toast | `sonner` (shadcn-shipped) | hooks into success/failure of OID-copy and dev-page actions |
| Theme system tokens | shadcn `--background`, `--foreground`, `--primary`, `--accent`, `--border`, `--muted`, `--destructive`, `--ring` (zinc derivatives) | see §4 Color |

**Anti-choices reaffirmed** (from STACK.md §1, do not deviate during execution):

- No MUI / Chakra / AntD (heavy runtimes, fight Tailwind, not Linear-aligned).
- No CDN font (use `@fontsource/*` per D-20 — avoids CDN latency, plays with Vite asset pipeline).
- No `shadcn@canary` — stable `@latest` is sufficient.
- No custom theme variables outside the shadcn token set in v1 (extend only if a phase actually needs it).

---

## 2. Spacing Scale

Linear-dense aesthetic = tight spacing, low chrome. The scale below is the **prescriptive set** for Phase 4 surfaces; only these values may appear in production class names without justification. All values are multiples of 4 from the standard 8-point set {4, 8, 16, 24, 32, 48, 64}.

| Token | Tailwind v4 utility | px | Usage in Phase 4 |
|-------|---------------------|----|------------------|
| 2xs | `p-1` / `gap-1` | 4 | Icon-to-label gap inside buttons; chip internal padding |
| xs | `p-2` / `gap-2` | 8 | Compact spacing inside nav-item, inside form-field; gap between sibling buttons; card internal padding for top-nav items; EmptyState body-to-heading offset |
| md | `p-4` / `gap-4` | 16 | Default container padding; gap between AppShell sections; card internal sections |
| lg | `p-6` / `gap-6` | 24 | Page-content padding; vertical rhythm between distinct UI blocks |
| xl | `p-8` / `gap-8` | 32 | Section break inside ErrorBoundary fallback and AccessDenied page |
| 2xl | `p-12` | 48 | AccessDenied page top margin / hero spacing |
| 3xl | `p-16` | 64 | (Not used in Phase 4; reserved for Phase 5/6/7 dashboard hero) |

**Exceptions** (documented and justified — not part of the prescriptive token set):

- **Top-nav row height:** fixed `h-12` (48px) per Linear-dense convention — canonical top-nav height for Linear/shadcn dense headers; height/width architecture, not a spacing token.
- **Touch targets:** sign-out, theme toggle, and copy-to-clipboard buttons are minimum `h-9 w-9` (36×36px) for icon-only variants, or `h-9` with horizontal padding `px-3` for labeled variants. WCAG 2.5.5 enhanced (44px) is **NOT** met for icon-only controls — accepted because v1 is single-user desktop-first; revisit when mobile becomes a real surface. Height/width architecture, not a spacing token.
- **OID copy-block:** `p-4` interior plus `font-mono` text content; the `<pre>` itself has no extra margin beyond what its parent card supplies.

All other values not in the table above (e.g., `p-3`, `p-5`, `p-7`, `gap-3`, `gap-10`) are **disallowed** without explicit override note in the PR.

---

## 3. Typography

Exactly 4 sizes, exactly 2 weights (400 regular + 600 semibold). Linear-dense profile.

| Role | Size (px) | Tailwind utility | Weight | Line height | Usage |
|------|-----------|------------------|--------|-------------|-------|
| Body | 14 | `text-sm` | 400 (normal) | 1.5 (`leading-normal`) | Default body text, nav labels, dropdown items, EmptyState body, error message body |
| Label | 12 | `text-xs` | 400 (normal) | 1.4 (`leading-snug`) | Form field labels, badge text, "Phase 5 coming soon" tag, dev-only banner on `/debug/agent-stream` |
| Heading | 18 | `text-lg` | 600 (semibold) | 1.3 (`leading-snug`) | Page headings (`<h1>` on each route), AccessDenied "Access denied" heading, ErrorBoundary "Something went wrong" heading |
| Display | 24 | `text-2xl` | 600 (semibold) | 1.2 (`leading-tight`) | Reserved for empty-state hero headlines and the OID code-block label "Your account ID:" (only place Display fires in Phase 4) |

**Mono usage** (Geist Mono):

- OID value in the AccessDenied copy-block: `text-sm font-mono` (14px, normal).
- `/debug/agent-stream` event log: `text-xs font-mono` (12px, normal, line-height `leading-snug`).
- Technical-details disclosure inside the ErrorBoundary fallback: `text-xs font-mono` in a collapsible `<details>` block.

**Anti-choices:**

- No `text-base` (16px) — would break the 4-size cap; body is `text-sm` in Linear-dense.
- No `font-medium` (500) and no font-weight 700/800 — would break the 2-weight cap. Label uses `font-normal` (400), matching Linear's own labels at 12px (the visual delta between 400 and 500 Geist Sans at 12px is marginal).
- No italic except inside the technical-details JSON (which is mono and may show stringified errors as-is).

---

## 4. Color

60/30/10 split applied to the zinc palette in **both** light and dark modes. Values are shadcn new-york defaults with `base-color: zinc`; the executor does NOT override these.

### Dark mode (default first paint)

| Role | Token (CSS var) | OKLCH / hex equivalent | Usage |
|------|-----------------|------------------------|-------|
| Dominant (60%) | `--background` | `oklch(0.141 0.005 285.823)` ≈ `#0a0a0c` (zinc-950) | Page background, top-nav background |
| Secondary (30%) | `--card` / `--popover` / `--muted` | `oklch(0.21 0.006 285.885)` ≈ `#18181b` (zinc-900) | Card surfaces, dropdown popover, EmptyState card, OID code-block container, ErrorBoundary card |
| Accent (10%) | `--primary` / `--ring` | `oklch(0.985 0 0)` ≈ `#fafafa` (zinc-50 — inverted "primary" in dark mode per shadcn new-york) | Primary CTA buttons (Copy OID, Sign in), focus rings, active nav-tab underline |
| Foreground | `--foreground` | `oklch(0.985 0 0)` ≈ `#fafafa` (zinc-50) | Default text on dominant background |
| Muted foreground | `--muted-foreground` | `oklch(0.705 0.015 286.067)` ≈ `#a1a1aa` (zinc-400) | Secondary text, EmptyState body, "Phase N coming soon" copy |
| Border | `--border` | `oklch(1 0 0 / 10%)` ≈ `#ffffff1a` | Card borders, top-nav bottom border, input borders |
| Destructive | `--destructive` | `oklch(0.704 0.191 22.216)` ≈ `#f87171` (red-400) | Sign-out hover-confirm; reserved for Phase 7 resume-delete and Phase 6 abort-stream — NOT actively rendered in Phase 4 except in dropdown-menu destructive variant for Sign out |

### Light mode

| Role | Token | OKLCH / hex equivalent | Usage |
|------|-------|------------------------|-------|
| Dominant (60%) | `--background` | `oklch(1 0 0)` ≈ `#ffffff` | Page background, top-nav background |
| Secondary (30%) | `--card` / `--popover` / `--muted` | `oklch(0.967 0.001 286.375)` ≈ `#f4f4f5` (zinc-100) | Card surfaces, dropdown popover, EmptyState card |
| Accent (10%) | `--primary` | `oklch(0.21 0.006 285.885)` ≈ `#18181b` (zinc-900) | Primary CTA buttons, focus rings, active nav-tab underline |
| Foreground | `--foreground` | `oklch(0.141 0.005 285.823)` ≈ `#0a0a0c` (zinc-950) | Default text |
| Muted foreground | `--muted-foreground` | `oklch(0.552 0.016 285.938)` ≈ `#71717a` (zinc-500) | Secondary text |
| Border | `--border` | `oklch(0.92 0.004 286.32)` ≈ `#e4e4e7` (zinc-200) | Borders |
| Destructive | `--destructive` | `oklch(0.577 0.245 27.325)` ≈ `#dc2626` (red-600) | Same usage as dark — sign-out destructive variant |

### Accent reserved-for list (the 10% rule)

The accent (`--primary`) is **reserved for the following four element categories only** in Phase 4. Any other use is a contract violation.

1. **Primary CTA buttons** — exactly one per surface: "Copy ID" on `/access-denied`; "Sign in" if rendered (the AuthGate auto-redirects so this is rarely visible); "Back to dashboard" on the ErrorBoundary fallback (Reload page is the secondary outline variant); "Open in new tab" on the dev-page warning banner.
2. **Active nav-tab indicator** — a 2px-tall underline below the currently-active tab in the top-nav (Dashboard / Chat / Profile). Only ONE tab is accented at a time.
3. **Focus rings** (`--ring`) — every interactive element gets a `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` treatment. This is a11y-required, not decorative.
4. **Theme toggle "active state" dot** — a 4px accent dot inside the theme-toggle icon button when hovered (sub-pixel detail; optional in v1).

**Explicitly NOT accented in Phase 4:** links, badges (unless a specific badge needs to signal "active" status), icons, table-row hover state, dropdown-item hover state (uses `--muted` for hover background instead).

### Destructive reserved-for list

Destructive (`--destructive`) is reserved for:

1. **Sign-out menu item** — in the dropdown menu, rendered with the shadcn `dropdown-menu` destructive variant (red text, no background until hover, then `bg-destructive/10`).
2. **(Pre-allocated, not rendered in Phase 4)** ErrorBoundary "Reload page" button if/when retry strategies fail repeatedly — kept neutral in v1.

---

## 5. Component Inventory (first wave)

The executor installs these shadcn components during Phase 4. **No other shadcn components are introduced in this phase** — later phases will add their own (auto-form, data-table, etc.).

| Component | shadcn block | Used in Phase 4 by | Notes |
|-----------|--------------|--------------------|-------|
| Button | `button` | Primary CTAs, theme toggle, copy-to-clipboard, sign-out trigger | Variants used: `default` (accent), `ghost` (icon-only top-nav buttons), `destructive` (sign-out menu item) |
| Card | `card` | EmptyState surface, AccessDenied container, ErrorBoundary fallback container | New-york style ships with subtle border + shadow-sm |
| Skeleton | `skeleton` | `<RouteSkeleton>` per-route Suspense fallback; per-feature loading states (Phase 5/6/7 consume) | One skeleton rectangle per content slot; pulse animation default |
| Dropdown Menu | `dropdown-menu` | Top-nav account menu (Sign out + theme toggle group) | Radix-backed; auto-handles keyboard nav |
| Dialog | `dialog` | NOT rendered in Phase 4; installed for Phase 5+ readiness | Listed for inventory completeness |
| Sonner (toast) | `sonner` | "Copied to clipboard" toast on AccessDenied, "Error reported" toast on ErrorBoundary | Single `<Toaster position="bottom-right" richColors />` mounted in AppShell |
| Input | `input` | NOT rendered in Phase 4 except as `/debug/agent-stream` prompt field; installed for Phase 5+ readiness | Single-line text input |
| Badge | `badge` | "Phase 5 coming soon" tag inside placeholder pages; "DEV" badge on `/debug/agent-stream` banner | Use `secondary` variant (muted background) for "coming soon"; `outline` for "DEV" |

### Custom components (Phase 4 ships)

These are app-level components, not shadcn primitives. They live in `frontend/src/components/`.

| Component | File | Purpose | Composed from |
|-----------|------|---------|---------------|
| `AppShell` | `components/AppShell.tsx` | Top-nav layout wrapper around `<Outlet/>` | div + Button + DropdownMenu + ThemeToggle + sonner Toaster |
| `AuthGate` | `components/AuthGate.tsx` | MSAL boundary — if unauthenticated, calls `loginRedirect`; else renders children | uses `useIsAuthenticated()` hook |
| `ThemeToggle` | `components/ThemeToggle.tsx` | Top-nav button that toggles light/dark + persists to localStorage | Button + lucide `Sun`/`Moon` icons |
| `ErrorBoundary` | `components/ErrorBoundary.tsx` | Root render-error catcher | wraps `react-error-boundary` lib + Card + Button + collapsible `<details>` |
| `RouteSkeleton` | `components/RouteSkeleton.tsx` | Suspense fallback for React.lazy code-split chunks | Skeleton rectangles in a card-shaped layout |
| `EmptyState` | `components/EmptyState.tsx` | Typed empty-state primitive (props: `icon`, `heading`, `body`, `cta?`) | Card + lucide icon + typography + optional Button |
| `PhasePlaceholder` | `components/PhasePlaceholder.tsx` | "Coming in Phase N" stub for `/dashboard`, `/chat`, `/profile` | EmptyState composition with specific copy per route |
| `AccessDeniedPage` | `routes/AccessDenied.tsx` | OID bootstrap surface (outside AuthGate) | Card + `<pre><code>` mono block + Copy Button + Sonner toast |
| `NotFoundPage` | `routes/NotFound.tsx` | `*` route fallback | EmptyState with "Page not found" heading and Dashboard link |
| `DebugAgentStreamPage` | `routes/DebugAgentStream.tsx` | Dev-only SSE probe (dev-flag-gated) | Input + Button + fixed-width `<pre>` log + readSSEStream consumer |

---

## 6. Route Table

Locked by CONTEXT.md D-17 "Route table (preliminary)". Reproduced here as the executable contract.

| Path | Inside AuthGate? | Component | Layered States Required |
|------|------------------|-----------|-------------------------|
| `/` | yes | `<Navigate to="/dashboard" replace />` | none |
| `/dashboard` | yes | `<PhasePlaceholder phase={5} feature="Dashboard" />` | EmptyState-style placeholder (lazy-load → RouteSkeleton) |
| `/chat` | yes | `<PhasePlaceholder phase={6} feature="Chat" />` | EmptyState-style placeholder (lazy-load → RouteSkeleton) |
| `/profile` | yes | `<PhasePlaceholder phase={7} feature="Profile" />` | EmptyState-style placeholder (lazy-load → RouteSkeleton) |
| `/access-denied` | **no** (outside AuthGate to avoid redirect loop per D-18) | `<AccessDeniedPage />` | renders immediately, no Suspense; no skeleton |
| `/debug/agent-stream` | yes | `<DebugAgentStreamPage />` (lazy-loaded, dev-only gate via `import.meta.env.DEV \|\| import.meta.env.VITE_DEBUG_PAGES`) | RouteSkeleton during lazy-load; live event-log fills as `readSSEStream` yields |
| `*` | no | `<NotFoundPage />` | renders immediately |

---

## 7. AppShell Anatomy

The top-nav is the only persistent chrome in Phase 4. Locked shape:

```
┌──────────────────────────────────────────────────────────────────┐
│  job-rag    Dashboard  Chat  Profile        [☀/🌙]  [account ▾] │  ← h-12, px-6, border-b
└──────────────────────────────────────────────────────────────────┘
│                                                                  │
│                       <Outlet/>                                  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- **Left cluster (px-6 from left edge):** logo wordmark "job-rag" (`text-sm font-semibold`, foreground color) + horizontal nav tabs (Dashboard / Chat / Profile) with 24px (`gap-6`) between items.
- **Right cluster (px-6 from right edge):** theme toggle icon button + account dropdown trigger (icon button with `User` lucide icon, `aria-label="Open account menu"`).
- **Active tab indicator:** 2px tall accent underline (`border-b-2 border-primary`) on the active tab; inactive tabs have no underline; hover shows `border-b-2 border-muted-foreground/50`.
- **Account dropdown content:** single menu item "Sign out" (destructive variant). v1 has no "Profile" or "Settings" item — those land in Phase 7.
- **Logo behavior:** clicking the wordmark navigates to `/dashboard`. Not an `<a>` to an external URL — uses React Router `<Link>`.
- **Sign-out semantics:** triggers `msalInstance.logoutRedirect({postLogoutRedirectUri: SWA_origin})` per D-12. Renders as a `<button>` inside the DropdownMenu, not an `<a>`.
- **Keyboard reachability:** Tab order is logo → Dashboard → Chat → Profile → theme toggle → account dropdown. All controls have `focus-visible` rings (accent color).
- **`<Toaster />` mount:** rendered inside AppShell after `<Outlet/>` so toasts persist across route transitions.

---

## 8. AccessDenied Page Anatomy (`/access-denied`)

The OID bootstrap surface — the most copy-heavy surface in Phase 4. Locked by D-09 and `<phase_scope_for_ui>` item 3.

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                         Access denied                            │  ← Display, 24px semibold
│                                                                  │
│   Your account is not on the allowlist. Send the ID below        │  ← Body, 14px regular, muted
│   to the administrator to request access.                        │
│                                                                  │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │ Your account ID                                          │   │  ← Label, 12px normal
│   │ ┌────────────────────────────────────────────────────┐   │   │
│   │ │  abc12345-6789-0abc-def0-123456789abc              │   │   │  ← Mono, 14px, in <pre>
│   │ └────────────────────────────────────────────────────┘   │   │
│   │                                          [Copy ID]       │   │  ← Primary CTA, accent
│   └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│   Administrator runbook                                          │  ← Heading, 18px semibold
│                                                                  │
│   1. az keyvault secret set \                                    │  ← Mono code block,
│        --vault-name jobrag-prod-kv \                             │     12px, in card with
│        --name seeded-user-entra-oid \                            │     muted background
│        --value <paste here>                                      │
│                                                                  │
│   2. az containerapp revision restart \                          │
│        --name jobrag-api-prod \                                  │
│        --resource-group jobrag-prod-rg                           │
│                                                                  │
│   3. Reload this page and sign in again.                         │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- **Container:** Card at `max-w-2xl mx-auto mt-12 p-8` — vertically positioned so it sits at first-fold on a 1080p display.
- **Empty-OID edge case:** if `msalInstance.getActiveAccount()?.idTokenClaims?.oid` returns undefined (user landed here without logging in), show a fallback EmptyState card with copy "Sign in first to see your account ID" and a "Sign in" Button. Do NOT show an empty `<pre>` block.
- **Copy button behavior:** label is `Copy ID`; clicking copies the OID to clipboard via `navigator.clipboard.writeText(oid)`. On success: Sonner toast `Copied to clipboard` (bottom-right, success variant, 2s duration). On failure (e.g., insecure context): toast `Couldn't copy — please select and copy manually` (error variant).
- **Runbook code block:** rendered as `<pre class="font-mono text-xs">` inside a Card with muted background (`bg-muted`). NOT a copy button per command — Adrian's a power user; one-click-each is friction without value.
- **No "Try again" button** — the page is reached via redirect from a 403 response; the user must complete the runbook out-of-band before returning.

---

## 9. ErrorBoundary Fallback Anatomy

D-19a. Layered as the outermost render-error catcher.

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                      Something went wrong                        │  ← Heading, 18px semibold
│                                                                  │
│   The app hit an unexpected error. You can try going back to     │  ← Body, 14px regular, muted
│   the dashboard, or reload the page.                             │
│                                                                  │
│   [ Back to dashboard ]  [ Reload page ]                         │  ← Two buttons: accent + outline
│                                                                  │
│   ▶ Technical details                                            │  ← <details> toggle
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

- **Container:** Card at `max-w-2xl mx-auto mt-12 p-8`.
- **CTAs:**
  - "Back to dashboard" — primary action; uses `<Button variant="default">` (accent); calls `navigate('/dashboard')` and resets the error boundary.
  - "Reload page" — secondary action; uses `<Button variant="outline">` (border, no fill); calls `window.location.reload()`.
- **Technical details:** Inside a native `<details>` element. When expanded, shows `error.message` and `error.stack` in a `<pre class="font-mono text-xs">` block on a muted background. Truncated to first 1000 chars to avoid overflowing the card.
- **No retry button** in v1 — retry semantics are domain-specific and live in per-feature error states; ErrorBoundary is the absolute floor.

---

## 10. EmptyState + PhasePlaceholder Pattern (SHEL-06 layered pattern)

This is the **reusable primitive** Phase 5/6/7 will consume. Locked by D-19c/d.

### `<EmptyState>` props (typed contract)

```ts
type EmptyStateProps = {
  icon: LucideIcon            // required — visual anchor
  heading: string             // required — Display typography
  body: string                // required — Body typography, muted
  cta?: { label: string; onClick: () => void }  // optional — primary CTA
}
```

### Phase 4 instantiations

| Route | Icon | Heading | Body | CTA |
|-------|------|---------|------|-----|
| `/dashboard` | `BarChart3` | Dashboard coming soon | The dashboard widgets land in Phase 5. Check the roadmap for progress. | none |
| `/chat` | `MessageSquare` | Chat coming soon | The streaming chat surface lands in Phase 6. Check the roadmap for progress. | none |
| `/profile` | `User` | Profile coming soon | Resume upload and profile editing land in Phase 7. Check the roadmap for progress. | none |
| `*` (404) | `FileQuestion` | Page not found | The page you're looking for doesn't exist. | `{ label: "Go to dashboard", onClick: () => navigate('/dashboard') }` |

### Visual structure

```
┌──────────────────────────────────────┐
│                                      │
│            [Icon, 48px]              │  ← lucide icon, muted-foreground color, h-12 w-12
│                                      │
│         Heading text here            │  ← Display, 24px semibold, foreground
│                                      │
│       Body copy here, can wrap       │  ← Body, 14px regular, muted-foreground
│       across multiple lines.         │
│                                      │
│          [Optional CTA]              │  ← Button, accent variant if present
│                                      │
└──────────────────────────────────────┘
```

- **Container:** Card at `max-w-md mx-auto mt-24 p-8 text-center`.
- **Icon:** rendered at `h-12 w-12 text-muted-foreground mx-auto mb-4`.
- **Heading-to-body gap:** `mb-2` (8px).
- **Body-to-CTA gap:** `mt-4` (16px).
- **No illustrations** in Phase 4 — a single lucide icon is enough; SVG illustrations are a portfolio-polish v2.

---

## 11. RouteSkeleton Pattern (SHEL-06 layer b)

The Suspense fallback for React.lazy-loaded routes. One pattern, used for every lazy-loaded route in Phase 4 (and reused by Phase 5/6/7).

```
┌──────────────────────────────────────┐
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓ (skeleton heading)   │  ← Skeleton h-6 w-1/3
│                                      │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ (body) │  ← Skeleton h-4 w-full
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ (body cont.)   │  ← Skeleton h-4 w-2/3
│                                      │
│  ▓▓▓▓▓▓▓▓▓▓▓ (button-shaped)        │  ← Skeleton h-9 w-32
└──────────────────────────────────────┘
```

- **Container:** matches EmptyState container shape (`max-w-md mx-auto mt-24 p-8`) so the layout doesn't shift when content resolves.
- **Pulse animation:** shadcn's default `Skeleton` component pulse (CSS animation, `animate-pulse`).
- **Display duration:** typically <200ms for code-split chunks on a warm browser; the skeleton must look intentional even if it flashes for one frame.

---

## 12. Dev-Only `/debug/agent-stream` Page

D-16 + dev-only gate per `<specifics>`. Minimal-chrome interactive surface for proving end-to-end SSE works.

```
┌──────────────────────────────────────────────────────────────────┐
│ [DEV] Agent stream probe                                         │  ← Badge "DEV" + Heading
│                                                                  │
│ ┌────────────────────────────────────────────┐  ┌─────────────┐  │
│ │ Ask the agent something…                   │  │ Send query  │  │  ← Input + Button
│ └────────────────────────────────────────────┘  └─────────────┘  │
│                                                                  │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │ event: token  data: {"text":"Hello"}                         │ │  ← Fixed-width log
│ │ event: token  data: {"text":" world"}                        │ │     12px mono on muted bg
│ │ event: tool_start  data: {"name":"search_jobs",...}          │ │     auto-scrolls to bottom
│ │ event: tool_end  data: {"name":"search_jobs",...}            │ │     each line is one
│ │ event: heartbeat                                             │ │     `event:` SSE frame
│ │ event: final  data: {"reason":"complete"}                    │ │
│ └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

- **Gate:** route only mounts if `import.meta.env.DEV === true \|\| import.meta.env.VITE_DEBUG_PAGES === 'true'`. In prod build without the flag, navigating to `/debug/agent-stream` falls through to `*` (NotFound).
- **DEV badge:** `<Badge variant="outline">DEV</Badge>` placed inline with the heading. Tone: "this is intentional, not leaked".
- **Input:** standard shadcn `<Input>`, `placeholder="Ask the agent something…"`.
- **Send button:** label is `Send query`; standard shadcn `<Button>`, default (accent) variant. Disabled while a stream is active.
- **Log container:** scrollable `<pre>` with `max-h-96 overflow-y-auto bg-muted p-4 font-mono text-xs leading-snug`. Each event is one line; long `data` payloads wrap with CSS `word-break: break-all`.
- **Clear-on-resubmit:** clicking Send query clears the log first, then re-streams.
- **Stream lifecycle copy:** while connecting (waiting for first byte), show `… connecting` in muted-foreground at the top of the log. While streaming, no extra label. On `final`, append a separator line `--- end of stream ---`. On error, append a destructive-colored line `--- error: {reason} ---`.

---

## 13. Copywriting Contract

| Element | Copy |
|---------|------|
| **Top-nav logo wordmark** | `job-rag` (lowercase, matches repo + Docker image naming) |
| **Top-nav tabs** | `Dashboard`, `Chat`, `Profile` (literal SHEL-04 + D-17 wording, title case) |
| **Account dropdown trigger** | (icon only, `User` icon — no visible label; `aria-label="Open account menu"`) |
| **Sign-out menu item** | `Sign out` (Microsoft canonical phrasing; renders red via destructive variant) |
| **Theme toggle aria-label** | `Toggle theme (currently {light/dark})` — dynamic |
| **AccessDenied heading** | `Access denied` |
| **AccessDenied body** | `Your account is not on the allowlist. Send the ID below to the administrator to request access.` |
| **AccessDenied OID label** | `Your account ID` |
| **AccessDenied Copy button** | `Copy ID` |
| **AccessDenied copy-success toast** | `Copied to clipboard` |
| **AccessDenied copy-failure toast** | `Couldn't copy — please select and copy manually` |
| **AccessDenied runbook heading** | `Administrator runbook` |
| **AccessDenied empty-OID fallback heading** | `Sign in first` |
| **AccessDenied empty-OID fallback body** | `Sign in to see the account ID you need to share.` |
| **AccessDenied empty-OID fallback CTA** | `Sign in` |
| **PhasePlaceholder heading template** | `{Feature} coming soon` (e.g. `Dashboard coming soon`) |
| **PhasePlaceholder body template** | `The {feature} surface lands in Phase {N}. Check the roadmap for progress.` |
| **Dashboard placeholder body (exact)** | `The dashboard widgets land in Phase 5. Check the roadmap for progress.` |
| **Chat placeholder body (exact)** | `The streaming chat surface lands in Phase 6. Check the roadmap for progress.` |
| **Profile placeholder body (exact)** | `Resume upload and profile editing land in Phase 7. Check the roadmap for progress.` |
| **NotFound heading** | `Page not found` |
| **NotFound body** | `The page you're looking for doesn't exist.` |
| **NotFound CTA** | `Go to dashboard` |
| **ErrorBoundary heading** | `Something went wrong` |
| **ErrorBoundary body** | `The app hit an unexpected error. You can try going back to the dashboard, or reload the page.` |
| **ErrorBoundary primary CTA** | `Back to dashboard` |
| **ErrorBoundary secondary CTA** | `Reload page` |
| **ErrorBoundary details disclosure** | `Technical details` |
| **DebugAgentStream heading** | `Agent stream probe` |
| **DebugAgentStream input placeholder** | `Ask the agent something…` |
| **DebugAgentStream send button** | `Send query` |
| **DebugAgentStream connecting label** | `… connecting` |
| **DebugAgentStream end-of-stream separator** | `--- end of stream ---` |
| **DebugAgentStream error separator template** | `--- error: {reason} ---` |

### Copywriting tone rules

- **Voice:** Plain, direct, second-person. No marketing fluff. No emoji.
- **Specificity over politeness:** "Your account is not on the allowlist" beats "Sorry, we couldn't verify your account." The user needs to know WHAT to do, not how the system feels about it.
- **Sentence case** for headings (not Title Case): "Access denied" not "Access Denied". Matches Linear convention.
- **No oxford commas** — Linear-style brevity.
- **Phase reference is canonical:** every "coming soon" copy names the phase number. This is portfolio-friendly (visitors understand the roadmap) and accurate (no hand-wave "soon").
- **Capitalize "Sign out" as imperative**, but "Sign in" inside running text stays lowercase. Title-case in buttons; sentence-case in body copy.
- **CTAs are verb + noun**, never bare verbs: "Copy ID" not "Copy"; "Reload page" not "Reload"; "Send query" not "Send". The noun makes the affordance unambiguous even when the surrounding context is lost (e.g., screen reader linear scan).

### Destructive action confirmations

Phase 4 has **one** destructive action: Sign out.

| Action | Confirmation approach |
|--------|----------------------|
| Sign out | No confirmation dialog. Single click in dropdown menu triggers `logoutRedirect`. Rationale: sign-out is idempotent and reversible (user just signs back in); a confirm dialog would be theatre. The destructive-variant styling (red text) is the visual confirmation that this is a state-changing action. |

Phase 7 (resume delete) and Phase 6 (abort active stream) will introduce confirmation patterns when they need them.

---

## 14. Motion + Interaction

Linear-dense aesthetic = subtle, fast, no theatrics.

| Element | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| Theme toggle icon swap | Cross-fade between Sun/Moon | 150ms | `ease-in-out` (Tailwind `transition-all duration-150`) |
| Dropdown menu open/close | Radix default (scale + fade) | 150ms | Radix default |
| Toast in/out | Sonner default (slide + fade) | 200ms | Sonner default |
| Skeleton pulse | shadcn default (CSS `animate-pulse`) | 1.5s loop | `ease-in-out` |
| Focus ring | Instant (no animation) | 0ms | n/a |
| Button hover | Background color shift | 100ms | `ease-out` |
| Active-tab underline | Instant on route change (no slide) | 0ms | n/a |
| Route transition | None (instant content swap inside Outlet) | n/a | n/a |
| ErrorBoundary details expand | Native `<details>` (no custom animation) | n/a | n/a |

**Anti-motion rules:**

- No page-transition animations. Route changes are instant.
- No parallax or scroll-triggered effects.
- No "shimmer" loading effect — use Skeleton pulse.
- `prefers-reduced-motion`: all animations respect the media query. Sonner respects it natively; shadcn skeleton respects it via Tailwind's `motion-reduce:animate-none` (apply this utility on the Skeleton primitive).

---

## 15. Accessibility Floor

WCAG 2.1 AA baseline. Specific Phase 4 obligations:

| Requirement | Implementation |
|-------------|----------------|
| `<html lang="en">` | Set in `index.html` |
| Color contrast (text) | Zinc-50 on zinc-950 (dark) and zinc-950 on white (light) both exceed 12:1 — WCAG AAA |
| Color contrast (muted text) | Zinc-400 on zinc-950 = 7.5:1 (AAA); zinc-500 on white = 4.6:1 (AA) — both pass for body sizes |
| Focus visible | Every interactive element has `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` |
| Keyboard reachability | All controls (tabs, theme toggle, account menu, sign out, copy button) reachable in Tab order. No keyboard traps. |
| Sign-out is a `<button>` | Rendered as DropdownMenuItem (which is a button under the hood), never an `<a href>` |
| Theme toggle aria | `aria-label="Toggle theme (currently {light/dark})"` — dynamic with current state |
| Account dropdown trigger aria | `aria-label="Open account menu"` — icon-only button (`User` lucide icon) needs an accessible name |
| AccessDenied OID `<pre>` | Wrapped in a labeled region: `<div role="region" aria-label="Your account ID">` |
| Copy button | Has visible label "Copy ID" — not icon-only. After copy, Sonner toast announces (Sonner uses `role="status"` by default). |
| Errors announced | ErrorBoundary heading rendered inside `role="alert"` region so screen readers announce on appear |
| Skeleton fallbacks | Wrapped in `role="status" aria-label="Loading"` |
| Top-nav active tab | `aria-current="page"` on the active `<Link>` |
| Tab landmark | Top-nav uses `<nav aria-label="Primary">` |
| Main landmark | Outlet wrapper uses `<main>` |
| Skip-to-main link | NOT required in v1 (only 3-tab nav, low TabIndex cost). Revisit if nav grows. |

---

## 16. Loading-State Policy (AUTH-07 / D-05)

The `<phase_scope_for_ui>` item 11 spelled this out — locking it here:

- **During the `await msalInstance.initialize()` + `await msalInstance.handleRedirectPromise()` window in `main.tsx` (~50-150ms cold load):** show **nothing** — the browser's natural blank page. Do NOT render an HTML loading splash. Rationale: at this point React hasn't mounted; injecting a manual splash via `index.html` would be a separate UI surface to maintain, and the blank window for 50-150ms is imperceptible on modern hardware.
- **If MSAL is still initializing on first paint** (defensive — should never happen if D-05 is implemented correctly): the AuthGate component checks `useMsal().inProgress !== 'none'`; if true, renders `<RouteSkeleton />` instead of redirecting. **Critically: NEVER renders a login form** as the first visible UI. The MSAL `loginRedirect` is the only path to the auth UI, and it's a full-page navigation, not an in-app form.
- **During lazy-route load:** RouteSkeleton inside Suspense.
- **During TanStack Query fetches** (Phase 5/6/7 will add these): per-feature Skeleton; the pattern Phase 4 ships is what they compose.

---

## 17. Registry Safety

Phase 4 uses shadcn official only. No third-party shadcn registries are declared. The registry vetting gate is not triggered.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (`https://ui.shadcn.com/r/styles/new-york`) | `button`, `card`, `skeleton`, `dropdown-menu`, `dialog`, `sonner`, `input`, `badge` | not required (official registry) |
| third-party | none declared | n/a |

If a future Phase 5/6/7 plan introduces a third-party block (e.g., for a data table from `tablecn` or a chart library from `chartcn`), it must run the vetting gate in its own UI-SPEC and cannot inherit Phase 4's safety stance.

---

## 18. Things This Phase Does Not Decide

Out-of-scope for Phase 4 UI-SPEC (later phases will lock these):

- Dashboard widget visuals, filter bar layout, country-map representation → Phase 5 UI-SPEC.
- Chat transcript layout, tool-call chip design (collapsed/expanded variants), message-input affordance, streaming "cursor" indicator → Phase 6 UI-SPEC.
- Resume upload affordance (drop zone vs file-picker), extracted-skill diff visualization, skill chip tick/untick interaction → Phase 7 UI-SPEC.
- Cold-start "warming" state distinct from "thinking" state (Pitfall 4) → Phase 6 UI-SPEC will land this on top of the readSSEStream helper Phase 4 ships.

Each future phase's UI-SPEC MUST reference this Phase 4 contract as its inherited baseline (especially Spacing §2, Typography §3, Color §4, EmptyState §10, RouteSkeleton §11, Copywriting §13 tone rules, Motion §14, Accessibility §15).

---

## 19. Checker Sign-Off

- [ ] Dimension 1 Copywriting: pending checker review (see §13 + §8 + §9 + §10 + §12)
- [ ] Dimension 2 Visuals: pending checker review (see §5 + §7 + §8 + §9 + §10 + §12)
- [ ] Dimension 3 Color: pending checker review (see §4)
- [ ] Dimension 4 Typography: pending checker review (see §3)
- [ ] Dimension 5 Spacing: pending checker review (see §2)
- [ ] Dimension 6 Registry Safety: pending checker review (see §17)

**Approval:** pending

---

*Generated by gsd-ui-researcher in --auto mode. Pre-populated from 20 locked CONTEXT.md decisions + PROJECT.md "Linear-dense aesthetic" + STACK.md §1. No user input requested per --auto flag.*

*Revision 1 (2026-05-19): typography collapsed to 2 weights (Label 500 → 400); spacing 12px row removed (top-nav items → `p-2`, EmptyState body offset already `mb-2`); CTAs tightened to verb+noun ("Copy" → "Copy ID", "Reload" → "Reload page", "Send" → "Send query"); account dropdown trigger gained `aria-label="Open account menu"`. No other decisions changed.*
