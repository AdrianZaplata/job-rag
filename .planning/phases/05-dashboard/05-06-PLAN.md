---
phase: 5
plan: 06
type: execute
wave: 3
depends_on: [05-05]
files_modified:
  - .planning/phases/05-dashboard/05-UAT.md
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, SHEL-03, SHEL-06]
threats_addressed: []

must_haves:
  truths:
    - "Adrian has signed in to the live SWA at https://witty-flower-065dac003.7.azurestaticapps.net/dashboard and seen 3 widgets render with real data"
    - "Flipping country PL -> DE -> EU -> WW produces visibly different numbers across all 3 widgets (E12 / Phase 5 success criterion #5 canary)"
    - "Refreshing /dashboard?country=DE&seniority=senior&remote=remote preserves filter state (DASH-06)"
    - "Show more Dialog opens with a 50-row scrollable table that scrolls inside the dialog (not the page)"
    - "Theme toggle still works on /dashboard - widget chart colors swap on theme change"
    - "Cold-start ACA caveat documented: first dashboard load after idle takes ~225s; subsequent <1s"
    - ".planning/phases/05-dashboard/05-UAT.md captures the 6 M-marker results with timestamps"
  artifacts:
    - path: .planning/phases/05-dashboard/05-UAT.md
      provides: "Manual UAT evidence with 6 M-markers: M1-screenshots, M2-country flip, M3-refresh, M4-dialog, M5-theme, M6-cold-start note"
      min_lines: 80
      contains: "M1"
  key_links:
    - from: 05-UAT.md
      to: Live ACA + SWA stack (deployed via Phase 3 + Phase 4)
      via: "manual verification against witty-flower-065dac003.7.azurestaticapps.net"
      pattern: "https://"
---

<objective>
Phase 5 close-out: run a manual UAT pass against the deployed dev/prod ACA + SWA stack to confirm the 3-widget dashboard works end-to-end with real Entra-authenticated traffic and a real corpus. Capture evidence in a UAT.md so the verifier and future readers can audit the live behavior.

Purpose:
- Phase 5 success criterion #5 (the "different numbers per country" canary) CANNOT be exercised via unit tests against synthetic fixtures — only real corpus + real backend SQL proves filter changes flow through to the data layer
- DASH-06 deep-link refresh-safety is a Phase 4 carry-forward; UAT verifies the integration with the dashboard surface
- Phase 4 theme toggle integration verified against the Recharts CSS-var theme binding
- Cold-start ACA caveat documented for future readers (Phase 8 portfolio polish may revisit)

Output:
- `.planning/phases/05-dashboard/05-UAT.md` with 6 M-marker evidence sections; each marker has either a screenshot path, a timestamp, or a verbatim observation
- All 6 markers must be marked "PASS" or "FAIL with note"; partial passes are explicit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-VALIDATION.md
@.planning/phases/05-dashboard/05-05-PLAN.md
@.planning/STATE.md

<interfaces>
<!-- Live system endpoints. -->

Live SWA frontend (per STATE.md "Current Focus" section):
- https://witty-flower-065dac003.7.azurestaticapps.net
- Sign-in via Entra External ID (CIAM tenant); Adrian's seeded user has oid 18d774c1-62ac-4416-8945-b5eca715e9ed
- /dashboard route loads inside AuthGate (Phase 4)

Live ACA backend:
- Behind the SWA's API proxy
- Cold-start cost per memory aca-cold-start-profile.md: ~225s for first-byte; ~0.2s warm

Phase 4 theme toggle:
- Top-nav button (AppShell)
- Persists to localStorage.theme
- Drives `:root.dark` class on documentElement; Recharts var(--chart-1) resolves via CSS vars

Dashboard filter URL examples (from UI-SPEC section 10):
- /dashboard -> default state (WW / undefined / any)
- /dashboard?country=DE -> Germany
- /dashboard?country=DE&seniority=senior&remote=remote -> filtered
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live UAT against deployed SWA — execute 6 M-markers and record evidence in 05-UAT.md</name>
  <what-built>
    Phase 5 ships the full Dashboard surface (Plans 05-01 through 05-05). The 3 widgets (TopSkillsCard, SalaryBandsCard, CvVsMarketCard) live behind AuthGate at /dashboard. Backend endpoints (3 GET /dashboard/*) are wired through fastapi-azure-auth (AUTH-06 oid allowlist).

    This task validates the deployed surface against the live ACA + Postgres corpus. Adrian executes the 6 M-markers from a browser; Claude records the evidence in 05-UAT.md.

    Note on cold-start: the FIRST request after idle takes ~225s (memory aca-cold-start-profile.md). Plan for this — Adrian should expect a multi-minute initial wait; subsequent fetches are sub-second.
  </what-built>
  <how-to-verify>
**Before starting:** ensure the latest Phase 5 frontend bundle is deployed to SWA. If `git log --oneline -1` shows Plan 05-05 SUMMARY, the deploy-spa.yml workflow should have fired on the merge. If unsure, check the Actions tab.

**M1 — Initial dashboard render with real data**

1. Open https://witty-flower-065dac003.7.azurestaticapps.net in a fresh incognito window
2. Sign in with the customer account (`adrian@jobrag.onmicrosoft.com`)
3. Navigate to `/dashboard`
4. **Wait** — first load may take 3-4 minutes if the container scaled to zero
5. Observe the 3 widgets render: TopSkillsCard (skill list), SalaryBandsCard (3 bars), CvVsMarketCard (hero number + chips)
6. Take a screenshot of the rendered dashboard; save to `.planning/phases/05-dashboard/uat-screenshots/m1-dashboard-default.png`

**Expected:** 3 cards rendered; each shows non-zero data; no Alert errors visible; no Skeletons stuck visible.

**Record in 05-UAT.md:**
- timestamp of initial visit
- time to first-byte (rough estimate; from address bar)
- screenshot path
- whether all 3 widgets rendered data
- PASS/FAIL

**M2 — Country flip exercises SQL (Phase 5 success criterion #5 canary)**

1. With dashboard loaded, change country filter to `Poland`. Observe URL becomes `/dashboard?country=PL`. Wait for 3 widget refetches.
2. Record top-3 skills + footer count + p50 salary + CV mean_score (eyeball values)
3. Change country filter to `Germany`. Repeat observation.
4. Change country filter to `EU`. Repeat observation.
5. Change country filter to `Worldwide`. Repeat observation.
6. Take a screenshot of each state; save as `m2-pl.png`, `m2-de.png`, `m2-eu.png`, `m2-ww.png`

**Expected:** At least one widget's numeric output differs across the 4 country values. The sample-size footer ("N postings") MUST visibly change for at least some pairs. If PL/DE/EU/WW all return identical numbers, this is a FAIL — the filter isn't reaching SQL.

**Record:** the recorded values per country (table); the screenshot paths; PASS/FAIL.

**M3 — Refresh preserves filter state (DASH-06)**

1. Navigate to `/dashboard?country=DE&seniority=senior&remote=remote`
2. Wait for widgets to load
3. Hard-refresh the page (Ctrl/Cmd-R)
4. Observe filters retained: country dropdown shows `Germany`; seniority shows `Senior`; remote toggle is on `Remote`
5. URL still reads `?country=DE&seniority=senior&remote=remote`
6. Take screenshot `m3-refresh-state-preserved.png`

**Expected:** No flash to login page (AUTH-07 carry-forward); no filter reset; URL identical post-refresh.

**Record:** PASS/FAIL + screenshot.

**M4 — Show more dialog (DASH-05)**

1. Ensure country filter is Worldwide (so the corpus has plenty of skills)
2. Click "Show more" button on TopSkillsCard
3. Dialog opens; verify:
   - Title: "All top skills"
   - Description: "Full ranked list of hard skills across the filtered postings."
   - Table with columns: #, Skill, Must, Nice, Total
   - Scrolling the table body scrolls WITHIN the dialog, not the underlying page
   - Up to 50 rows visible (or however many the corpus has)
4. Press Escape to close the dialog
5. Take screenshot `m4-show-more-dialog.png`

**Expected:** Dialog opens, scrolls inside, closes cleanly.

**Record:** PASS/FAIL + screenshot + observed row count.

**M5 — Theme toggle on /dashboard (Phase 4 carry-forward)**

1. With dashboard loaded, click the theme toggle in AppShell top-nav
2. Observe:
   - Background swaps between dark / light
   - Recharts BarChart color swaps (via `var(--chart-1)`)
   - Card backgrounds adapt
   - Text contrast maintained
3. Toggle back to verify reversibility
4. Take screenshot of each state: `m5-dark.png` and `m5-light.png`

**Expected:** Visual transition is smooth; no broken styles; chart visible in both modes.

**Record:** PASS/FAIL + 2 screenshots.

**M6 — Cold-start ACA caveat documented (Phase 8 candidate; not a fix)**

This marker does NOT require an action — it's a documentation marker. Adrian confirms the observed cold-start time during M1 and Claude records it in 05-UAT.md as a known limitation.

**Record:** observed cold-start time during M1; the note "Phase 8 may revisit via min_replicas=1 (~€8/mo); out of v1 budget."
  </how-to-verify>
  <resume-signal>
Type "approved" once all 6 markers are PASS or FAIL-with-note, with screenshots saved to .planning/phases/05-dashboard/uat-screenshots/ and 05-UAT.md captured.

If a marker FAILS, do NOT resume until either (a) the failure is documented and triaged as acceptable, or (b) a remediation plan is opened.

For typical issues:
- M1 widgets don't render -> open a debug task to inspect browser console + ACA logs
- M2 numbers don't change between countries -> backend filter not reaching SQL; debug Plan 05-02 _apply_filters
- M3 filters reset on refresh -> useDashboardFilters bug; debug Plan 05-04
- M4 dialog scrolls the page instead of inside -> CSS override; check the max-h-[70vh] overflow rule
- M5 chart colors don't swap -> var(--chart-1) not picked up; check app.css theme block
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 05-UAT.md capturing the 6 M-marker results</name>
  <files>.planning/phases/05-dashboard/05-UAT.md</files>
  <read_first>
    - .planning/phases/03-infrastructure-ci-cd/03-SMOKE.md (analog UAT file from Phase 3; copy the M-marker structure)
    - .planning/phases/05-dashboard/05-VALIDATION.md section "Manual-Only Verifications" (the 6 markers seed list)
  </read_first>
  <action>
Create `.planning/phases/05-dashboard/05-UAT.md` with the following structure (Claude fills the Findings columns from Task 1's verification):

```markdown
---
phase: 5
slug: dashboard
status: complete
date: <YYYY-MM-DD>
uat_environment: dev SWA + dev ACA
swa_origin: https://witty-flower-065dac003.7.azurestaticapps.net
---

# Phase 5 — Manual UAT Evidence

> Per VALIDATION.md section "Manual-Only Verifications". 6 M-markers; each must
> PASS or be documented as FAIL-with-note. Cross-references the success
> criteria in ROADMAP.md Phase 5.

## Environment

- SWA origin: <fill from frontmatter>
- Backend ACA: <fill — pull from envs/prod/main.tf outputs>
- Adrian's signed-in oid: 18d774c1-62ac-4416-8945-b5eca715e9ed (seeded user)
- Browser: <fill, e.g. Chrome 128 / Safari 18>
- Date / time: <fill>

## M1 — Initial dashboard render

| Property | Value |
|----------|-------|
| Cold-start observed (first-byte) | <fill, e.g. ~225s / warm ~0.5s> |
| Widgets rendered | TopSkillsCard / SalaryBandsCard / CvVsMarketCard |
| Errors visible | <fill or "none"> |
| Screenshot | uat-screenshots/m1-dashboard-default.png |
| Verdict | PASS / FAIL |
| Notes | <fill> |

## M2 — Country flip exercises SQL (success criterion #5)

| Country | Top-1 skill (eyeball) | Footer "N postings" | p50 salary | CV mean_score |
|---------|----------------------|---------------------|------------|---------------|
| PL | <fill> | <fill> | <fill> | <fill> |
| DE | <fill> | <fill> | <fill> | <fill> |
| EU | <fill> | <fill> | <fill> | <fill> |
| WW | <fill> | <fill> | <fill> | <fill> |

**At least one column differs across the 4 rows:** <YES / NO>
**Verdict:** PASS / FAIL
**Screenshots:** m2-pl.png, m2-de.png, m2-eu.png, m2-ww.png

## M3 — Refresh preserves state (DASH-06 + AUTH-07 carry)

| Property | Value |
|----------|-------|
| URL before refresh | /dashboard?country=DE&seniority=senior&remote=remote |
| URL after refresh | <fill> |
| Filter UI state after refresh | country=Germany / seniority=Senior / remote=Remote (expected) |
| Login flash observed | <NO expected / YES actual> |
| Screenshot | uat-screenshots/m3-refresh-state-preserved.png |
| Verdict | PASS / FAIL |
| Notes | <fill> |

## M4 — Show more Dialog (DASH-05)

| Property | Value |
|----------|-------|
| Dialog title | "All top skills" |
| Dialog description | "Full ranked list of hard skills across the filtered postings." |
| Table columns | #, Skill, Must, Nice, Total |
| Row count observed | <fill, e.g. 50 or less> |
| Scroll behavior | inside dialog (not page) |
| Escape closes | YES / NO |
| Screenshot | uat-screenshots/m4-show-more-dialog.png |
| Verdict | PASS / FAIL |
| Notes | <fill> |

## M5 — Theme toggle on /dashboard

| Property | Value |
|----------|-------|
| Dark -> Light works | YES / NO |
| Light -> Dark works | YES / NO |
| Recharts chart color swaps (var(--chart-1)) | YES / NO |
| Card backgrounds adapt | YES / NO |
| Text contrast maintained | YES / NO |
| Screenshots | uat-screenshots/m5-dark.png, uat-screenshots/m5-light.png |
| Verdict | PASS / FAIL |
| Notes | <fill> |

## M6 — Cold-start documentation (Phase 8 candidate)

> The first dashboard load after the container has scaled to zero takes ~225s
> (ACA cold-start; see memory file aca-cold-start-profile.md). Subsequent loads
> are sub-second. This is intentional — `min_replicas=0` keeps the project at
> €0/mo runtime. Phase 8 portfolio polish may revisit by flipping
> `min_replicas=1` for ~€8/mo continuous warmth.

| Property | Value |
|----------|-------|
| Observed cold-start time (M1) | <fill> |
| Subsequent warm latency | <fill, e.g. <1s> |
| Recommendation | accept; revisit in Phase 8 if portfolio demo experience warrants |
| Verdict | DOCUMENTED |
| Notes | <fill> |

## Roadmap Success-Criteria Map

| Criterion | Marker | Verdict |
|-----------|--------|---------|
| #1 deep-link pre-populates filters; changing filter updates URL | M3 + dashboard interaction | <fill> |
| #2 top-skills widget shows top 8-10 hard skills + Show more drill-down | M1 + M4 | <fill> |
| #3 salary-bands p25/p50/p75 with N of M footnote | M1 + M2 | <fill> |
| #4 CV-vs-market aggregate score + top 3 missing skills | M1 + M2 | <fill> |
| #5 Country flip produces different numbers (SQL flow proof) | M2 | <fill> |

## Overall Phase 5 Verdict

<PASS / PASS WITH MINOR DEVIATIONS / FAIL>

## Deviations from spec (if any)

<List any observed UI deviations vs UI-SPEC, or any feature that didn't work
exactly as expected. Cross-reference the affected D-XX decision so future
readers can trace the divergence.>
```

Notes for executor:
- Replace every `<fill>` placeholder with the actual observation from Task 1
- Use 24-hour ISO timestamps where time is recorded
- If a marker FAILS, the deviation MUST be triaged in "Deviations from spec" with one of: (a) accepted-as-is + reason, (b) opened-as-Phase-5-gap-closure plan, (c) deferred to Phase 8
- The `uat-screenshots/` subfolder is gitignored by convention (per Phase 3 03-SMOKE.md pattern) — don't commit the binaries; only reference the paths in the markdown
  </action>
  <verify>
    <automated>test -f .planning/phases/05-dashboard/05-UAT.md && grep -c "^## M" .planning/phases/05-dashboard/05-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-dashboard/05-UAT.md` returns 0
    - `grep -c "^## M" .planning/phases/05-dashboard/05-UAT.md` returns at least 6 (M1, M2, M3, M4, M5, M6)
    - `grep -q "phase: 5" .planning/phases/05-dashboard/05-UAT.md` returns 0 (frontmatter)
    - `grep -q "Roadmap Success-Criteria Map" .planning/phases/05-dashboard/05-UAT.md` returns 0
    - `grep -q "Overall Phase 5 Verdict" .planning/phases/05-dashboard/05-UAT.md` returns 0
    - `! grep -q "<fill>" .planning/phases/05-dashboard/05-UAT.md` returns 0 (no remaining placeholders)
    - `grep -q "PASS\|FAIL" .planning/phases/05-dashboard/05-UAT.md` returns 0 (each marker has a verdict)
    - `grep -q "M2.*country\|M2 — Country" .planning/phases/05-dashboard/05-UAT.md` returns 0 (success criterion #5 canary covered)
  </acceptance_criteria>
  <done>
    .planning/phases/05-dashboard/05-UAT.md exists with 6 M-marker sections + Roadmap Success-Criteria Map + Overall Verdict; no <fill> placeholders remain; each marker has either PASS or FAIL-with-note; Phase 5 success criterion #5 (M2 country-flip canary) explicitly recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adrian's browser to SWA | TLS via SWA's managed cert; standard Phase 3 + Phase 4 carry-forward |
| SWA to ACA | TLS via ACA's managed cert; standard Phase 3 + Phase 4 carry-forward |
| ACA to Postgres | Private VNet; standard Phase 3 carry-forward |

## STRIDE Threat Register

Plan 05-06 is purely manual verification + documentation. No new code, no new endpoints. Threat surface inherited verbatim from Phase 3 + 4 + the Plan 05-01 through 05-05 stack.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-06-01 | Information Disclosure | Screenshot content | mitigate | Screenshots in `uat-screenshots/` show only Adrian's own seeded-user data (his profile, the public corpus). Subfolder gitignored to avoid binary commits. No third-party PII visible since corpus is curated and uses public job-posting URLs only. |
| T-5-06-02 | Spoofing | Sign-in during UAT | accept | Adrian uses his Entra customer account `adrian@jobrag.onmicrosoft.com`. AUTH-06 single-user oid allowlist (Phase 4 D-08) rejects any other user. UAT requires this oid; no spoofing surface. |
</threat_model>

<verification>
**05-UAT.md integrity:**
```
test -f .planning/phases/05-dashboard/05-UAT.md
grep -c "^## M" .planning/phases/05-dashboard/05-UAT.md   # >= 6
! grep -q "<fill>" .planning/phases/05-dashboard/05-UAT.md
```

**Phase 5 success criterion #5 canary** (M2):
```
grep -A 10 "## M2" .planning/phases/05-dashboard/05-UAT.md | grep -E "(PASS|FAIL|YES|NO)"
```
Expected: shows the actual verdict + the "at least one column differs" row marked YES.

**Cold-start documented** (M6):
```
grep -A 8 "## M6" .planning/phases/05-dashboard/05-UAT.md
```
Expected: contains "min_replicas=0" or "€0/mo" + the Phase 8 revisit note.
</verification>

<success_criteria>
Plan 05-06 ships clean when:
1. .planning/phases/05-dashboard/05-UAT.md exists with all 6 M-markers filled in
2. Every M-marker has a Verdict (PASS / FAIL / DOCUMENTED) - no unfilled placeholders
3. M2 (country-flip canary) records actual numeric observations across PL / DE / EU / WW with at least one column differing
4. The Roadmap Success-Criteria Map shows verdict for each of the 5 Phase 5 success criteria
5. Overall Phase 5 Verdict is recorded (PASS / PASS WITH MINOR DEVIATIONS / FAIL)
6. If any marker FAILS, the deviation is triaged in the "Deviations from spec" section
7. M6 documents the cold-start as accepted limitation pending Phase 8
</success_criteria>

<output>
Create .planning/phases/05-dashboard/05-06-SUMMARY.md per template. Include:
- Pointer to 05-UAT.md
- Overall verdict (PASS / PASS WITH MINOR DEVIATIONS / FAIL)
- List of M-marker results in a single table
- If any FAIL: link to remediation plan (gap-closure or deferred-to-Phase-8)
- Final assertion: "Phase 5 ships when all 6 DASH-* requirements are observable in the live UAT M-marker evidence"
</output>
</content>
