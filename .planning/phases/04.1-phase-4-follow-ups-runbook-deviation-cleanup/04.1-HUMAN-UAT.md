---
status: diagnosed
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
source: [04.1-VERIFICATION.md]
started: 2026-05-22T00:25:00.000Z
updated: 2026-05-23T00:10:00.000Z
---

## Current Test

[paused — issue found on Test 2; awaiting fix-or-continue decision]

## Tests

### 1. Live Deploy API smoke must succeed on a RunningAtMaxScale revision

expected: Next `master` push triggers `Deploy API` workflow → smoke step observes `runningState=RunningAtMaxScale` (or `Running`) within the 150s budget → workflow exits green. The success log line interpolates `$STATE` (e.g. `Smoke passed after Ns — revision XXX is RunningAtMaxScale and serving /health 200`). Verified via the GitHub Actions run logs on the next master push.
result: [pending]

### 2. Live `terraform apply` in `infra/external/` must converge inline identifier_uris

expected: Next `cd infra/external && terraform apply` shows a plan with: (a) `azuread_application_identifier_uri.api` planned to destroy, (b) `azuread_application.api` planned to update-in-place with the new `identifier_uris = ["api://${client_id}"]` attribute. After approve, `az ad app show --id ${client_id} --query identifierUris -o tsv` returns the populated `api://...` URI (no longer the silent-empty failure mode documented in memory `terraform-azuread-identifier-uri-unreliable.md`). Verified via `terraform plan` + `terraform apply` + the `az` query post-apply.
result: issue
reported: |
  `terraform plan -var-file=terraform.tfvars.local` (run from Adrian's mac in infra/external/)
  showed the expected DESTROY of azuread_application_identifier_uri.api but then aborted:

      Error: Self-referential block
        on main.tf line 32, in resource "azuread_application" "api":
        32:   identifier_uris = ["api://${azuread_application.api.client_id}"]
        Configuration for azuread_application.api may not refer to itself.

  Plan output: `Plan: 0 to add, 0 to change, 1 to destroy.` followed by the
  self-reference error — apply was NOT attempted (plan never completed cleanly).
  The Phase 04.1 fix-5 source change in main.tf is invalid HCL: a resource
  block cannot reference its own computed attributes (client_id), even though
  the dedicated azuread_application_identifier_uri resource was removed in
  the same patch. The "inline identifier_uris" approach as implemented cannot
  plan, let alone apply.
severity: blocker

### 3. Live ACA revision restart applies SEEDED_USER_ENTRA_OID rotation without manual exec

expected: When `SEEDED_USER_ENTRA_OID` is rotated in Key Vault and the ACA backend revision restarts, `users.entra_oid` is updated to the new value automatically by `init_db()::_seed_entra_oid()` on container boot. No `az containerapp exec --command python3 ...` invocation needed. Verified by rotating the KV secret to a test value → `az containerapp revision restart ...` → connecting to prod DB → `SELECT entra_oid FROM users WHERE id = '00000000-0000-0000-0000-000000000001'` returns the new value. (Workflow forbids the actual rotation during this phase; verification deferred to next operator-initiated rotation.)
result: [pending]

### 4. Live MSAL-init rejection in browser surfaces BootErrorFallback

expected: Triggering a real top-level await rejection in `frontend/src/main.tsx` (e.g. by temporarily breaking the MSAL `authority` to an invalid URL or by hitting an AADSTS500011-class error from Entra during a real sign-in attempt) causes the SPA to render `BootErrorFallback` (white styled error surface with role="alert", error message + truncated stack, "Reload page" button) instead of leaving `<div id="root">` blank. Verified via live SPA dogfood — observe the styled fallback, not a blank page.
result: [pending]

## Summary

total: 4
passed: 0
issues: 1
pending: 3
skipped: 0
blocked: 0

## Gaps

- truth: "`cd infra/external && terraform apply -var-file=terraform.tfvars.local` produces a clean plan + apply where `azuread_application_identifier_uri.api` is destroyed and `azuread_application.api` is updated in-place with `identifier_uris = [\"api://${client_id}\"]`, and `az ad app show --id ${client_id} --query identifierUris -o tsv` returns the populated `api://...` URI afterward."
  status: failed
  reason: "User reported: terraform plan halts with `Error: Self-referential block` on `main.tf:32` — the inline `identifier_uris = [\"api://${azuread_application.api.client_id}\"]` references the resource it's being defined on, which Terraform forbids. Plan shows `Plan: 0 to add, 0 to change, 1 to destroy.` for the standalone identifier_uri resource, then aborts on the self-reference error. Apply never runs."
  severity: blocker
  test: 2
  root_cause: |
    Plan 04.1-05 mis-designed fix-5: setting `identifier_uris = ["api://${azuread_application.api.client_id}"]` *inline on the `azuread_application.api` block itself* is rejected by Terraform's resource-graph layer (`terraform validate` passes — the attribute is in the v3.x schema — but `terraform plan` aborts with `Self-referential block`). The deeper design problem is that there is NO inline workaround in the `hashicorp/azuread` ~> 3.x provider for the `api://<self.client_id>` case; cross-resource references work, self-references never do. Reverting to the original standalone `azuread_application_identifier_uri.api` is also unacceptable — it re-exposes the documented silent-fail mode (state says `Created`, live `identifierUris` stays `[]`, AADSTS500011 at sign-in) per memory `terraform-azuread-identifier-uri-unreliable.md`. The URI shape `api://<api_client_id>` is load-bearing (auth.py:72 strips `api://` prefix to extract the client_id for JWT validation), so switching to a static-string URI like `api://jobrag-prod` is NOT a valid alternative.
  artifacts:
    - path: "infra/external/main.tf"
      issue: "Lines 21-47: `azuread_application.api` block with self-referential `identifier_uris` on line 32. The nested `api { … }` block (lines 34-46) carrying `requested_access_token_version = 2` + `oauth2_permission_scope` is not supported on `azuread_application_registration` directly and must move to a sibling `azuread_application_api_access` resource if the registration-split pattern is adopted."
    - path: "infra/external/outputs.tf"
      issue: "Lines 17 and 22 reference `one(azuread_application.api.identifier_uris)` — broken transitively because the inline attribute can't be evaluated. Must rewire to read from the side-resource (`azuread_application_identifier_uri.api.identifier_uri`)."
    - path: "infra/external/README.md"
      issue: "Step 2 (`terraform apply`) and Step 3 (`Wire outputs`) apply paths describe the broken inline approach with no operator note about the resource decomposition or state reconciliation that the next apply will perform."
    - path: "scripts/refresh-external-outputs.sh"
      issue: "Line 11: reads `terraform output -raw api_audience_uri` but has no post-output assertion that `az ad app show --id ${api_client_id} --query identifierUris -o tsv` is non-empty. Silent-fail risk on `azuread_application_identifier_uri` is currently un-mitigated at the moment downstream files would be rewritten."
    - path: "infra/envs/prod/README.md"
      issue: "Lines 218 and 229: 'manual `az ad app update --identifier-uris`' unblock row and the Phase 04.1 fix-5 paragraph both describe the broken approach. The false-premise claim that 'the inline attribute is the reliable form per azuread provider ~> 3.x' (which motivated plan-05) must be retracted."
    - path: "~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md"
      issue: "The 2026-05-21 addendum (lines 17-25) endorses the broken inline approach as 'the structural fix'. Must be superseded by a new addendum naming the split (registration + identifier_uri + api_access) pattern as the actual fix and explicitly documenting the script-level `az ad app show` verification that backstops the silent-fail risk."
    - path: "src/job_rag/api/auth.py"
      issue: "Read-only constraint reference, line 72: `settings.backend_audience.removeprefix(\"api://\")` extracts the API client_id for JWT validation, so the `api://<api_client_id>` URI shape is load-bearing. Any fix must preserve this shape; switching to a static string URI would break sign-in."
  missing:
    - "Split `azuread_application.api` into THREE resources: `azuread_application_registration.api` (base — display_name, sign_in_audience), `azuread_application_identifier_uri.api` (referencing `azuread_application_registration.api.client_id` — now a cross-resource reference, not self-referential), and `azuread_application_api_access.api` (carrying `oauth2_permission_scope` + `requested_access_token_version = 2`)."
    - "Repoint `azuread_service_principal.api.client_id` and `azuread_application.spa`'s `required_resource_access.resource_app_id` to the new `azuread_application_registration.api.client_id` source."
    - "Rewire `infra/external/outputs.tf` `api_audience_uri` + `api_scope_name` outputs to read from the new side-resources."
    - "Add post-`terraform output` assertion to `scripts/refresh-external-outputs.sh` that calls `az ad app show --id ${api_client_id} --query identifierUris -o tsv` and aborts non-zero if empty — fail-loud at the moment downstream `.env.production`/`prod.tfvars.local`/GitHub secrets are about to be rewritten."
    - "Update `infra/external/README.md` Step 2 + Step 3 to describe the new resource decomposition and the destroy-old-resource-on-next-apply state reconciliation."
    - "Rewrite the Phase 04.1 fix-5 paragraph in `infra/envs/prod/README.md` (lines ~218, ~229) and remove the false-premise inline-is-reliable claim."
    - "Write a superseding addendum to memory `terraform-azuread-identifier-uri-unreliable.md` naming the split pattern as the actual fix and documenting the script-level `az ad app show` verification."
  debug_session: ".planning/debug/external-identifier-uris-self-ref.md"
