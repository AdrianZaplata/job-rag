---
status: partially_verified
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
source: [04.1-VERIFICATION.md, 04.1-06-PLAN.md]
started: 2026-05-22T00:25:00.000Z
updated: 2026-05-23T20:40:00.000Z
---

## Current Test

[Test 2 resolved by Phase 04.1-06. Tests 1, 3, 4 remain pending — orthogonal
live-cloud verifications; pick up naturally on the next master push (Test 1),
the next SEEDED_USER_ENTRA_OID rotation (Test 3), or the next MSAL-init
rejection in production (Test 4).]

## Tests

### 1. Live Deploy API smoke must succeed on a RunningAtMaxScale revision

expected: Next `master` push triggers `Deploy API` workflow → smoke step observes `runningState=RunningAtMaxScale` (or `Running`) within the 150s budget → workflow exits green. The success log line interpolates `$STATE` (e.g. `Smoke passed after Ns — revision XXX is RunningAtMaxScale and serving /health 200`). Verified via the GitHub Actions run logs on the next master push.
result: [pending]

### 2. Live `terraform apply` in `infra/external/` must converge inline identifier_uris

expected: Next `cd infra/external && terraform apply` shows a plan with: (a) `azuread_application_identifier_uri.api` planned to destroy, (b) `azuread_application.api` planned to update-in-place with the new `identifier_uris = ["api://${client_id}"]` attribute. After approve, `az ad app show --id ${client_id} --query identifierUris -o tsv` returns the populated `api://...` URI (no longer the silent-empty failure mode documented in memory `terraform-azuread-identifier-uri-unreliable.md`). Verified via `terraform plan` + `terraform apply` + the `az` query post-apply.
result: pass
verified: |
  Phase 04.1-06 landed the structural fix: `infra/external/main.tf` split
  `azuread_application.api` into `azuread_application_registration.api`
  (with `requested_access_token_version = 2` as a direct top-level attribute) +
  `azuread_application_identifier_uri.api` + `azuread_application_permission_scope.access_as_user`
  (replaces the failed inline-self-reference attempt; `azuread_application_api_access`
  intentionally NOT used — wrong abstraction). `scripts/refresh-external-outputs.sh`
  now carries an `az ad app show --query identifierUris` assertion that exits 2
  with an unblock instruction if the live list is empty.

  Operator-verified 2026-05-23T20:40Z:
  - `terraform plan -var-file=terraform.tfvars.local` — exit 2 (changes pending,
    no errors). Shape: `Plan: 5 to add, 1 to change, 4 to destroy.` (matches
    the predicted 3-add/1-change/2-destroy/2-replace prediction in
    infra/external/README.md State Reconciliation, with replaces rolled into
    add+destroy in Terraform v1.9+ display format). No `Self-referential block`
    error, no `Reference to undeclared resource` error, no
    `azuread_application_api_access` in plan.
  - `terraform apply` — `Apply complete! Resources: 5 added, 1 changed, 4 destroyed.`
    OLD api_client_id `a12dfd07-4a63-4edd-9dd0-593aa7ecca20` destroyed; NEW
    api_client_id `f4ced229-1b9d-4120-94aa-ade642e7fc43`. spa_client_id
    `40f1fa8b-6e44-4b75-bec5-a151d67c974a` unchanged. New
    api_audience_uri = `api://f4ced229-1b9d-4120-94aa-ade642e7fc43`.
  - `az ad app show --id f4ced229-... --query identifierUris -o tsv` and
    `az ad app show --id f4ced229-... --query api.requestedAccessTokenVersion -o tsv` —
    transitively verified via the end-to-end sign-in PASS in step 10 (operator
    did not paste the explicit `tsv` output; both assertions are load-bearing
    for sign-in — empty identifierUris would re-raise AADSTS500011 and
    requestedAccessTokenVersion ≠ 2 would have CIAM reject the SPA's token).
  - `bash scripts/refresh-external-outputs.sh` — exit 0; success-path
    assertion line `✓ Live identifierUris=api://f4ced229-...` printed before
    downstream file rewrites; `frontend/.env.production` and
    `infra/envs/prod/prod.tfvars.local` rewritten with the new GUID.
  - `gh secret set VITE_API_AUDIENCE --body "api://f4ced229-..."` — updated.
    `gh secret set VITE_SPA_CLIENT_ID --body "40f1fa8b-..."` — idempotent
    (no change).
  - `cd infra/envs/prod && terraform apply` —
    `Apply complete! Resources: 0 added, 3 changed, 0 destroyed.` ACA rolled
    to new revision with updated BACKEND_AUDIENCE env var.
  - Admin consent — `az ad app permission list-grants --id <SPA SP object_id>`
    returns `consentType=AllPrincipals` for scope `access_as_user` against the
    new API SP `074923e6-3802-4d61-a8dc-3d8b7ace4211`. Terraform's
    `azuread_service_principal_delegated_permission_grant` propagated the
    AllPrincipals admin consent automatically when the grant was re-created
    via TF — the plan's "must re-grant via portal" warning was overcautious;
    no portal step was needed.
  - End-to-end sign-in roundtrip — PASS. Fresh incognito on
    https://witty-flower-065dac003.7.azurestaticapps.net, signed in as
    `adrian@jobrag.onmicrosoft.com`, dashboard `/dashboard` loaded with real
    data (88 postings, 853 unique hard skills; top skills Python 69, English
    38, Azure 22, AWS 21, CI/CD 20; salary bands p25 €106,250 / p50 €684,864
    / p75 €869,796; CV-vs-market match score 0.28). No AADSTS500011, no
    AADSTS50059, no JWT 401.

  Unplanned but required sub-step (Rule 1 deviation, commit `c28bb13`):
  Sign-in initially failed with AADSTS500011 after the secrets rotation
  because Vite inlines env vars at BUILD time, and the SWA still served
  bundles built against the OLD api_client_id. Workflow `deploy-spa.yml` only
  triggered on `frontend/**` changes. Atomic commit `c28bb13` added a
  `workflow_dispatch:` trigger to `deploy-spa.yml` AND rolled
  `frontend/.env.production VITE_API_AUDIENCE` to the new GUID. Pushing
  master triggered Deploy SPA #8, which rebuilt the bundle with the new
  VITE_API_AUDIENCE from the GitHub secret. After that deploy completed,
  sign-in worked. A separate pre-existing CI gap (`deploy-infra.yml` fails
  on missing `var.home_ip`, failing since at least 2026-05-21) was
  surfaced by this push and captured to
  `.planning/todos/pending/deploy-infra-home-ip-gap.md` — NOT caused by
  04.1-06, out of scope for this plan.
severity: resolved

### 3. Live ACA revision restart applies SEEDED_USER_ENTRA_OID rotation without manual exec

expected: When `SEEDED_USER_ENTRA_OID` is rotated in Key Vault and the ACA backend revision restarts, `users.entra_oid` is updated to the new value automatically by `init_db()::_seed_entra_oid()` on container boot. No `az containerapp exec --command python3 ...` invocation needed. Verified by rotating the KV secret to a test value → `az containerapp revision restart ...` → connecting to prod DB → `SELECT entra_oid FROM users WHERE id = '00000000-0000-0000-0000-000000000001'` returns the new value. (Workflow forbids the actual rotation during this phase; verification deferred to next operator-initiated rotation.)
result: [pending]

### 4. Live MSAL-init rejection in browser surfaces BootErrorFallback

expected: Triggering a real top-level await rejection in `frontend/src/main.tsx` (e.g. by temporarily breaking the MSAL `authority` to an invalid URL or by hitting an AADSTS500011-class error from Entra during a real sign-in attempt) causes the SPA to render `BootErrorFallback` (white styled error surface with role="alert", error message + truncated stack, "Reload page" button) instead of leaving `<div id="root">` blank. Verified via live SPA dogfood — observe the styled fallback, not a blank page.
result: [pending]

## Summary

total: 4
passed: 1
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

(none — Phase 04.1-06 closed the Test 2 blocker. Tests 1, 3, 4 remain pending
per CONTEXT D-02 deferred-verification policy — they require live cloud/browser
operations the original Phase 04.1 forbade.)
