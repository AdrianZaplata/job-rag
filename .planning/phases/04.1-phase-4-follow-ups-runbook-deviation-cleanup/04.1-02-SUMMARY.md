---
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
plan: 02
subsystem: infra
tags: [github-actions, azure-container-apps, smoke-check, deploy, ci, bash]

requires:
  - phase: 03-infrastructure-ci-cd
    provides: ".github/workflows/deploy-api.yml — per-revision FQDN smoke + 150s poll budget + ActivationFailed/Failed terminal short-circuit (Gap 10.C original fix)"
  - phase: 04-frontend-shell-auth
    provides: "Deviation #4 in 04-06-SUMMARY.md documenting the RunningAtMaxScale false-red against revision 0000010"
provides:
  - "deploy-api.yml smoke check that accepts BOTH Running AND RunningAtMaxScale as healthy terminal states — RunningAtMaxScale is the steady state when a revision is currently scaled to its replica ceiling and is no longer a false-red trigger"
  - "Success-log line now interpolates `$STATE` so triage readers can see WHICH terminal state was hit (was hardcoded to literal `Running`)"
affects:
  - "Next push to master (whenever Adrian pushes) — the deploy-api workflow run will use the updated smoke check"
  - "Future ACA deploy workflows in this repo (e.g. if a worker container is added) — pattern reusable verbatim"

tech-stack:
  added: []
  patterns:
    - "Multi-state OR-conditional in bash CI smoke checks — `[ \"$STATE\" = \"A\" ] || [ \"$STATE\" = \"B\" ]` over `case` or regex, matches local convention used by the ActivationFailed/Failed terminal-failure branch on line 141"
    - "Inline `$STATE` interpolation in success-log lines (over hardcoded literal) — preserves which-terminal-state-was-hit forensics in CI run history with zero added log noise"

key-files:
  created: []
  modified:
    - ".github/workflows/deploy-api.yml — smoke success branch (lines 148-158): comment header expanded to name both healthy terminal states, conditional widened to OR, success log uses `$STATE`. Iteration-fall-through log line, terminal-failure short-circuit (ActivationFailed/Failed), 150s budget, per-revision FQDN, revision-list lookup + latestRevisionName fallback — all unchanged."

key-decisions:
  - "Comment header rewritten to multi-line block (4 explanatory lines + cross-reference to deviation #4) rather than one-line tweak — the existing block-comment convention in this workflow (e.g. lines 86-90 explaining Gap 10.C's per-revision-FQDN rationale) sets the precedent that smoke-check policy gets its 'why' documented inline, not in a separate file. Trade-off: 3 extra comment lines for durable triage context."
  - "Success-log change from `is Running and serving` (hardcoded) to `is $STATE and serving` (interpolated) — small enough to fit the must-have 'success log names the state observed' truth without splitting into a separate log line. Single `echo` is cleaner than branching log paths."
  - "Used `gsd-tools commit` per plan instruction (atomic commit produced hash 7fd0f61 listing only `.github/workflows/deploy-api.yml`). Pre-staged ROADMAP/STATE/SUMMARY-01 files from the in-flight rebase had already been absorbed by Plan 01's docs commit (afa27aa) by the time Task 04 ran, so the working tree was clean when staging `.github/workflows/deploy-api.yml`. No deviation needed."

patterns-established:
  - "Multi-state-healthy bash conditional pattern — when a CI gate polls an external resource for a terminal state and the resource has >1 valid healthy state, use OR over regex/case for grep-friendliness and consistency with the local ActivationFailed/Failed terminal-failure branch. Reusable for any future deploy-target where the resource has multiple steady states."
  - "Comment-header-as-deviation-cross-reference — when a structural fix lands as a workflow tweak, embed `Phase XX.Y fix N — see SUMMARY deviation #M` inline in the file's comment header so the next operator triaging the smoke-check finds the deviation context without grepping the planning tree. Trade-off: planning-system-aware comments in workflow files; acceptable in this repo because all CI workflows already reference SUMMARY/runbook anchors."

requirements-completed: []

duration: 3min 12s
completed: 2026-05-21
---

# Phase 04.1 Plan 02: deploy-api.yml smoke fix Summary

**`.github/workflows/deploy-api.yml` smoke check now accepts both `Running` and `RunningAtMaxScale` as healthy terminal states — eliminates the false-red on revisions that scale-out during the 150s poll budget (e.g. Deploy API #11 against revision 0000010).**

## Performance

- **Duration:** 3 min 12 s
- **Started:** 2026-05-21T21:12:11Z
- **Completed:** 2026-05-21T21:15:23Z
- **Tasks:** 4 (all executed in order)
- **Files modified:** 1 (one atomic commit)

## Accomplishments

- Smoke conditional on line 154 widened to `[ "$STATE" = "Running" ] || [ "$STATE" = "RunningAtMaxScale" ]`, eliminating the false-red Deploy API #11 case
- Success-log line on line 155 changed from hardcoded `is Running and serving` to `is $STATE and serving` — preserves which-terminal-state-was-hit forensics for future triage
- Comment header (lines 148-152) expanded with the lifecycle rationale + cross-reference to `04-06-SUMMARY.md` deviation #4
- Iteration-fall-through log line, terminal-failure short-circuit (ActivationFailed/Failed on line 141), 150s budget, per-revision FQDN smoke, revision-list lookup + latestRevisionName fallback — all unchanged
- YAML syntax validated via PyYAML (`yaml.safe_load` exit 0); actionlint + shellcheck not installed locally (skipped per plan D-13)
- Memory file `aca-deploy-verifier-trap.md` got the one-paragraph "Update (2026-05-21): Phase 04.1 plan 02 landed the structural fix" addendum

## Task Commits

Each task was committed atomically (single commit for the whole plan per CONTEXT.md D-03):

1. **Task 01: Edit deploy-api.yml (3 coordinated edits — comment, conditional, success log)** — `7fd0f61` (fix)
2. **Task 02: YAML syntax check via PyYAML + optional actionlint/shellcheck** — verification only, no commit
3. **Task 03: Memory addendum to `aca-deploy-verifier-trap.md`** — outside repo (`~/.claude/.../memory/`), no commit (memory store is not git-tracked from this repo)
4. **Task 04: Atomic commit** — `7fd0f61` (already created by gsd-tools when Task 01's edit was finalized via `--files` flag)

**Plan metadata:** _(this SUMMARY.md commit + STATE/ROADMAP update will be the final docs commit, separate from `7fd0f61`)_

## Files Created/Modified

- `.github/workflows/deploy-api.yml` — smoke check success branch: 3 coordinated edits widening healthy-terminal-state acceptance from `{Running}` to `{Running, RunningAtMaxScale}`. Committed in `7fd0f61`.
- `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/aca-deploy-verifier-trap.md` — one-paragraph dated addendum noting the structural fix. Memory store, not git-tracked.

## Decisions Made

- **Comment header rewritten to a 5-line block (not a one-line tweak):** matched the existing block-comment convention in this workflow (lines 86-90 explain Gap 10.C's per-revision-FQDN rationale in a similar block). Smoke-check policy lives inline; this stays consistent.
- **Success-log change kept inline (one `echo`, interpolated `$STATE`)** over splitting into separate-log-per-terminal-state branches. Single echo line is cleaner; `$STATE` interpolation is bash-idiomatic and zero-cost.
- **`gsd-tools commit` (not raw `git commit`)** used per plan instruction. The pre-staged rebase files (ROADMAP/STATE/04.1-01-SUMMARY) had been auto-absorbed by Plan 01's docs commit (`afa27aa`) before Task 04 ran, so the working tree was clean when staging only `.github/workflows/deploy-api.yml`. The resulting commit (`7fd0f61`) lists exactly 1 file.
- **No `terraform validate` / `pytest` / `ruff` invocation** — none of those apply to a workflow-YAML edit. The PyYAML syntax check is the canonical local gate per CONTEXT D-13.

## Deviations from Plan

None - plan executed exactly as written.

The only thing worth a footnote: at the start of execution the working tree showed an interactive-rebase-in-progress state from Plan 01 (STATE.md flagged `UU`, ROADMAP.md/04.1-01-SUMMARY.md staged). By the time Task 04 staged `.github/workflows/deploy-api.yml`, the rebase had auto-finished (Plan 01's docs commit `afa27aa` had landed) and `git status` was clean. The `gsd-tools commit` invocation produced exactly the required atomic commit. No Rule 1/2/3 fix needed; the situation resolved itself before it became a problem.

---

**Total deviations:** 0
**Impact on plan:** Plan executed exactly as written. All 4 tasks completed in order; all acceptance criteria met; all verification gates pass.

## Issues Encountered

- **Working-tree CRLF warning on `.github/workflows/deploy-api.yml`:** git emitted `warning: in the working copy ..., CRLF will be replaced by LF the next time Git touches it` on staging. Inspected `.gitattributes` — file is marked `text=set eol=lf`, so the commit blob is normalized to LF; the CRLF warning is benign (working-tree-only artifact, not in the committed content). `git diff` output shows clean LF. No action needed.

## User Setup Required

None - no external service configuration required. The fix is local-only; the live verification happens on the next push to `master` (whenever Adrian pushes — D-01 forbids `git push` from this phase).

## Next Phase Readiness

- Plan 02 of Phase 04.1 complete.
- **Next:** Plan 04.1-03 (main.tsx try/catch around MSAL bootstrap + BootErrorFallback component). Depends on 04.1-02 per the serial-execution chain (D-04).
- **Pending on Adrian:** A push to `master` will trigger Deploy API workflow with the new smoke logic. The fix will be live-verified by the next workflow run reaching a `RunningAtMaxScale` revision — no action item, just observation. If the next push happens to hit a revision that stays in `Running` (no scale-out), the conditional is still satisfied; the fix is forward-compatible with both paths.
- **No blockers** for 04.1-03.

## Verification Trail

| Gate | Command | Result |
|------|---------|--------|
| `RunningAtMaxScale` present | `grep -q 'RunningAtMaxScale' .github/workflows/deploy-api.yml` | PASS (3 occurrences) |
| OR-conditional present (count=1) | `grep -cE '\[ "\$STATE" = "Running" \] \|\| \[ "\$STATE" = "RunningAtMaxScale" \]' .github/workflows/deploy-api.yml` | PASS (1) |
| Success log uses `$STATE` | `grep -q 'is \$STATE and serving /health 200' .github/workflows/deploy-api.yml` | PASS |
| Comment header updated | `grep -q 'runningState in {Running, RunningAtMaxScale}' .github/workflows/deploy-api.yml` | PASS |
| ActivationFailed/Failed branch unchanged | `grep -nE 'ActivationFailed.*Failed|Failed.*ActivationFailed' .github/workflows/deploy-api.yml` | PASS (lines 88, 141) |
| OLD literal-only conditional GONE | `! grep -E 'if \[ "\$STATE" = "Running" \]; then' .github/workflows/deploy-api.yml` | PASS (absent) |
| YAML syntax | `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-api.yml'))"` | PASS |
| Commit subject matches `^fix\(04\.1-02\):` | `git log -1 --pretty=format:%s` | PASS (`fix(04.1-02): accept RunningAtMaxScale as healthy in deploy-api smoke`) |
| Commit lists exactly 1 file | `git log -1 --stat` | PASS (`.github/workflows/deploy-api.yml` only) |
| Memory addendum landed | `grep -q 'Phase 04.1 plan 02' ~/.claude/.../aca-deploy-verifier-trap.md` | PASS |

## Self-Check: PASSED

- `.planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-02-SUMMARY.md` — FOUND
- `.github/workflows/deploy-api.yml` — FOUND (modified)
- Commit `7fd0f61` — FOUND (`git log --oneline --all` matches)
- Memory addendum in `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/aca-deploy-verifier-trap.md` — FOUND (contains "Phase 04.1 plan 02" anchor)

---
*Phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup*
*Completed: 2026-05-21*
