---
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
plan: 06
subsystem: infra
tags: [terraform, azuread, entra-external, identifier-uri, refactor, runbook, gap-closure, schema-introspection]

requires:
  - phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
    provides: "04.1-05-SUMMARY.md (the broken inline-self-reference refactor this plan closes the gap on) + 04.1-HUMAN-UAT.md Test 2 (result: issue — the verbatim Terraform `Error: Self-referential block` blocker) + .planning/debug/external-identifier-uris-self-ref.md (full gsd-debugger diagnosis pointing at the split pattern as the canonical fix)."
  - phase: 04-frontend-shell-auth
    provides: "04-06-SUMMARY.md deviation #7 (silent-fail on azuread_application_identifier_uri) + the load-bearing api://<api_client_id> URI shape contract preserved in src/job_rag/api/auth.py:72 (`settings.backend_audience.removeprefix(\"api://\")`)."
provides:
  - "infra/external/main.tf — three-resource split: azuread_application_registration.api (lightweight base, carries requested_access_token_version = 2 as a direct top-level attribute) + azuread_application_identifier_uri.api (URI side-resource, cross-references registration.client_id — plans cleanly) + azuread_application_permission_scope.access_as_user (carries the OAuth2 scope from the old nested api { oauth2_permission_scope { … } } block). All cross-refs from azuread_service_principal.api + azuread_application.spa.required_resource_access repointed from azuread_application.api.client_id to azuread_application_registration.api.client_id. Delegated-permission-grant claim_values hardened from literal [\"access_as_user\"] to explicit cross-ref [azuread_application_permission_scope.access_as_user.value]."
  - "infra/external/outputs.tf — api_client_id rewired to azuread_application_registration.api.client_id; api_audience_uri + api_scope_name rewired to azuread_application_identifier_uri.api.identifier_uri (Set-unwrap via one() no longer needed — side-resource exposes a plain string). Same literal string values as before (api://<api_client_id>)."
  - "scripts/refresh-external-outputs.sh — Phase 04.1-06 fail-loud assertion inserted between the terraform-output reads (line 12) and the existing exit-1 missing-output check (line 14). Runs `az ad app show --id $API_CLIENT_ID --query identifierUris -o tsv` BEFORE any downstream file rewrite; exits 2 with FATAL message naming memory file + unblock command (`az ad app update --identifier-uris ...`) if live identifierUris is empty. Mitigates the documented silent-fail risk on azuread_application_identifier_uri at the consumer boundary, NOT in Terraform."
  - "infra/external/README.md — new 'State reconciliation (Phase 04.1-06 refactor)' subsection appended after 'When to re-apply'; full destroy/create/replace cascade documented (2 destroy + 3 add + 2 replace + 1 in-place change → Terraform v1.9+ renders as 5 add / 1 change / 4 destroy); admin-consent re-grant via portal documented as EXPECTED (not fallback) because the API SP is replaced; api_client_id-changes consequence (VITE_API_AUDIENCE + prod.tfvars.local + GitHub secret roll to new GUID, literal api:// shape preserved) named. Step 2 + Step 3 updated with the new resource-decomposition story + script assertion mention."
  - "infra/envs/prod/README.md — line ~218 unblock table row 2 updated (Phase 04.1-06 refactor named, script assertion mentioned); line ~229 paragraph entirely rewritten — false-premise 'inline attribute is the reliable form per azuread provider ~> 3.x' claim from 04.1-05 RETRACTED; new prose names the split pattern + script-level mitigation + the apply consequence + admin-consent re-grant requirement."
  - "Memory ~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md — old 2026-05-21 addendum marked SUPERSEDED (NOT deleted; history preserved) with a date+reason blockquote; new 2026-05-23 addendum names the split pattern as the actual fix, the script-level silent-fail mitigation, and TWO pattern lessons (provider-decomposition + script-boundary assertion; schema introspection IS planning)."

affects:
  - "Out-of-band next `infra/external/` apply (Adrian, Task 09 — operator-only): destroy + create cascade lands the new resource shape; api_client_id changes (NEW GUID); `bash scripts/refresh-external-outputs.sh` propagates new GUID to frontend/.env.production + prod.tfvars.local + GitHub secret atomically; ACA revision restart picks up new BACKEND_AUDIENCE; admin consent re-granted via portal."
  - "04.1-HUMAN-UAT.md Test 2 — pending operator's Task 09 report + Task 10 update. Tests 1, 3, 4 UNCHANGED (orthogonal verification windows)."
  - "Phase 04.1 phase-level close — this plan is gap-closure for 04.1-05; Phase 04.1 closes the moment Task 10 lands (after operator-action Task 09)."
  - "src/job_rag/api/auth.py:72 contract preserved — `removeprefix(\"api://\")` keeps extracting the API client_id; only the GUID inside the api://<guid> shape changes. NO backend code change required."

tech-stack:
  added: []
  patterns:
    - "**Split-pattern over inline for self-referencing URI** — when the azuread provider requires `api://${self.client_id}` and the inline-attribute form is rejected as a self-reference by Terraform's resource-graph layer, the canonical fix is the provider's lightweight `azuread_application_registration` base + sibling resources (`azuread_application_identifier_uri`, `azuread_application_permission_scope`) for each feature axis. Cross-resource references plan cleanly; the URI lives in a DIFFERENT resource from the registration that exposes the client_id."
    - "**Script-boundary live-state assertion** — when a Terraform sub-resource has a known silent-fail mode AND lives in a sibling resource (not a parent attribute), backstop with a `az ad app show --query identifierUris -o tsv` assertion in the script that reads `terraform output`, BEFORE any downstream file rewrite. Exits 2 with an unblock instruction if the live list is empty. Catches the silent-fail at the consumer boundary; Terraform stays free of CLI shell-outs (no null_resource + local-exec)."
    - "**Schema introspection IS planning** — before claiming a resource carries an attribute, dump the provider schema: `cd <tf dir> && terraform init -backend=false && terraform providers schema -json | jq '.provider_schemas[\"registry.terraform.io/hashicorp/azuread\"].resource_schemas[\"<resource>\"].block.attributes | keys[]'`. Phase 04.1-05 conflated 'validate passes' with 'plan passes'; revision-0 of Phase 04.1-06 conflated 'resource exists' with 'resource carries this attribute' — both are the same mistake one level up. Permanent process guardrail going forward."
    - "**Superseding addendum, never delete** — when a memory file's old advice is invalidated by a new structural finding, ADD a dated SUPERSEDED marker as a blockquote BEFORE the old addendum (preserving the original text indented under the marker) and APPEND a new dated addendum naming the actual fix + the lesson learned. History is preserved for future contributors grepping for the original false-premise text; the new addendum is canonical."
    - "**Operator-action checkpoint is a first-class plan task** — when a fix REQUIRES a cloud touch the executor cannot perform (here: `terraform apply` against an External (CIAM) tenant the workforce GHA SP can't auth into), the plan splits into (a) PR-able code/docs changes executed atomically AND (b) an explicit `<checkpoint:human-action>` task with structured verification steps + a resume-signal contract. The executor stops at the gate and returns control; the operator runs the gated step and reports back; a follow-up post-action task updates the human-UAT artifact."

key-files:
  created:
    - ".planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-06-SUMMARY.md (this file)"
  modified:
    - "infra/external/main.tf — old `resource \"azuread_application\" \"api\"` block (29 lines, including the broken inline self-reference + the nested `api { … }` block) deleted; replaced by `resource \"azuread_application_registration\" \"api\"` (10 lines, including the 10-line rationale comment) + `resource \"azuread_application_identifier_uri\" \"api\"` (6 lines + 6-line rationale comment) + `resource \"azuread_application_permission_scope\" \"access_as_user\"` (12 lines + 8-line rationale comment). All cross-refs in `azuread_service_principal.api` + `azuread_application.spa.required_resource_access` repointed. Delegated-permission-grant `claim_values` hardened from literal `[\"access_as_user\"]` to cross-ref `[azuread_application_permission_scope.access_as_user.value]`. Net: 137 lines (was 96)."
    - "infra/external/outputs.tf — `api_client_id.value` rewired to `azuread_application_registration.api.client_id`; `api_audience_uri.value` rewired from `one(azuread_application.api.identifier_uris)` to `azuread_application_identifier_uri.api.identifier_uri` (plain string, no Set unwrap needed); `api_scope_name.value` rewired similarly. Description text on `api_audience_uri` updated to name Phase 04.1-06 + the silent-fail mitigation in the script. Net: 22 lines (was 23 — one less because the 4-line `one()` rationale comment shrank into a single sentence)."
    - "scripts/refresh-external-outputs.sh — 25-line `if [ -n \"$API_CLIENT_ID\" ]; then ... fi` block inserted between line 12 (after `API_SCOPE_NAME=...`) and the existing exit-1 check. Calls `az ad app show --id $API_CLIENT_ID --query identifierUris -o tsv`, exits 2 with multi-line FATAL message + unblock command if live list is empty; prints `✓ Live identifierUris=...` on success. Net: 86 lines (was 61). `bash -n` confirms valid syntax."
    - "infra/external/README.md — Step 2 + Step 3 gained one-paragraph notes naming Phase 04.1-06 + the script assertion; new 130-line 'State reconciliation (Phase 04.1-06 refactor)' subsection appended after 'When to re-apply' with full destroy/create/replace cascade + downstream-consumer roll consequence + SP-replace cascade admin-consent re-grant procedure (framed as EXPECTED, not fallback) + silent-fail mitigation sub-subsection. Net: 217 lines (was 80)."
    - "infra/envs/prod/README.md — line ~218 unblock-table row 2 'Where' column updated to name Phase 04.1-06 refactor + the script assertion. Line ~229 paragraph entirely rewritten — false-premise 'inline attribute is the reliable form' claim RETRACTED; new prose names the split (`azuread_application_registration` + `azuread_application_identifier_uri` + `azuread_application_permission_scope`) + `requested_access_token_version = 2` on the registration (CIAM v2 contract preserved) + script-level mitigation + apply consequence (3/1/2/2 plan shape, new api_client_id GUID, atomic propagation to downstream consumers) + admin-consent re-grant requirement + cross-ref to infra/external/README.md 'State reconciliation'. Net: 230 lines (was 230 — one paragraph rewritten in place + one table cell expanded)."
    - "~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md — OUT-OF-REPO memory file; old 2026-05-21 addendum kept (nested under a `> SUPERSEDED 2026-05-23 by Phase 04.1-06` blockquote) + new 2026-05-23 addendum appended naming the split pattern, `azuread_application_api_access` explicitly NOT used (with reason), the script-level silent-fail mitigation, and two pattern lessons (provider-decomposition + script-boundary; schema introspection IS planning). Net: ~65 lines (was 25)."

key-decisions:
  - "Use `azuread_application_registration` + sibling resources (NOT `azuread_application_api_access`) — schema introspection in Task 01 step 1 confirmed `azuread_application_api_access` requires `api_client_id` (it's for OUTBOUND scope grants — the SPA's `required_resource_access` equivalent — NOT for declaring the API's own scopes). The right sibling for the API's own OAuth2 scope is `azuread_application_permission_scope`. Pattern lesson: schema introspection prevents the same family of 'I assumed X carried attribute Y' bugs that bit Phase 04.1-05 at the validate-vs-plan boundary."
  - "Set `requested_access_token_version = 2` as a top-level attribute on `azuread_application_registration.api` (verified in v3.8.0 schema) — replaces the old nested `api { requested_access_token_version = 2 }` block. LOAD-BEARING: CIAM rejects v1 tokens (main.tf:60-62 documents the same requirement for the SPA app). The registration's attribute IS in the schema; this is structurally equivalent to the old nested form with one fewer level of nesting."
  - "Harden `azuread_service_principal_delegated_permission_grant.spa_to_api.claim_values` from literal `[\"access_as_user\"]` to explicit cross-ref `[azuread_application_permission_scope.access_as_user.value]` (Issue 6 from the plan-checker review). Adds zero risk and makes the dependency explicit so a future rename of the scope value can't silently break the grant — the Terraform graph forces the rename to propagate."
  - "Script-level live-state assertion (NOT Terraform-level) — `null_resource` + `local-exec` would couple Terraform's apply to a CLI shell-out (reliability hit + Terraform-graph noise). The script that reads `terraform output` is the natural assertion boundary: it runs BEFORE any downstream file rewrite, exits 2 with a clear unblock instruction, and keeps Terraform itself free of side-effect-only resources. The optional Task 09 step 6 (empty-and-restore test) lets the operator confirm the fail-loud path actually fires."
  - "Memory file gets a SUPERSEDING addendum, not a delete — the original 2026-05-21 addendum's false-premise advice ('inline attribute is the structural fix') is now invalid, but future contributors may grep for that exact phrase. Preserving the old text (visually nested under the SUPERSEDED marker) makes the supersession discoverable; deleting it would leave 'why was this advised?' as an open question. Pattern reusable for any memory-file supersession going forward."
  - "Skip `terraform plan -refresh=true` in Task 02 — refresh would attempt a live Microsoft Graph call against the External tenant the executor can't auth into. `-refresh=false` skips the refresh but still runs the resource-graph evaluator (the layer that 04.1-05 missed — `terraform validate` only checks the schema layer). Exit code 2 (changes pending, no errors) confirms the graph-level fix; the live-state convergence is verified in Task 09 by the operator."
  - "Add Task 03 (outputs.tf rewire) BEFORE Task 02's terraform validate gate — outputs.tf references `one(azuread_application.api.identifier_uris)`, which becomes 'Reference to undeclared resource' the moment Task 01's main.tf rewrite lands. Plan author's task ordering implicitly relied on the validate gate seeing a consistent file set; doing outputs.tf first satisfies the gate without changing the plan's intent."

patterns-established:
  - "**Operator-action checkpoint with structured resume-signal** — when a cloud touch is required and Claude lacks the tenant auth, the plan declares `type=\"checkpoint:human-action\" gate=\"blocking\"` with a `<how-to-verify>` block listing exact CLI commands + expected outputs, and a `<resume-signal>` naming the structured-report contract. The executor stops, returns control, and a follow-up task updates the human-UAT artifact on the operator's report. Reusable for any future cloud-touch fix where the workforce GHA SP can't auth into the target tenant."
  - "**Schema-introspection-as-planning (process guardrail)** — before any HCL rewrite that asserts a resource carries an attribute, run `terraform init -backend=false && terraform providers schema -json | jq` against the locked provider version. Catches the family of 'I assumed X' bugs at planning time, BEFORE the apply gate. Permanent process for any future Terraform refactor in this repo."

requirements-completed: []  # gap closure for 04.1-05; no ROADMAP requirements per CONTEXT.md D-06.

duration: ~30 min (Tasks 01–08; Task 09 operator-action gate pending)
completed_through_task_08: 2026-05-23T12:00:00Z
status_at_summary: tasks_01_through_08_complete_awaiting_operator_task_09
---

# Phase 04.1 Plan 06: external identifier_uri Split Refactor (Partial — Tasks 01–08 of 10)

**`infra/external/main.tf` now uses the `azuread_application_registration` + `azuread_application_identifier_uri` + `azuread_application_permission_scope` split (schema-verified against locked azuread = 3.8.0); the old broken inline self-reference is gone; `terraform plan -refresh=false` exits 2 with no errors and a `Plan: 5 to add, 1 to change, 4 to destroy.` shape (matches the predicted 3/1/2/2 with replacements rolled into add+destroy per Terraform v1.9+). `scripts/refresh-external-outputs.sh` carries an `az ad app show` assertion that exits 2 if live identifierUris is empty. Operator docs (infra/external/README.md + infra/envs/prod/README.md) updated; memory addendum supersedes (not deletes) the old false-premise advice and adds a schema-introspection-as-planning lesson. Atomic commit `96e7b9f` lands locally (5 files); HEAD is 8 commits ahead of origin/master (D-01 honored — no push). Task 09 operator-action gate (Adrian runs `terraform apply` from local against External tenant) is OUTSTANDING; Task 10 follows on operator's report.**

## Status: PARTIAL — awaiting operator-action Task 09

This SUMMARY documents Tasks 01–08 (the executor-runnable subset). Tasks 09 (operator-action: `terraform apply` + sign-in verification + admin-consent re-grant) and Task 10 (UAT update post-operator-report) are pending. The SUMMARY will be amended in place when those land.

## Performance (Tasks 01–08)

- **Started:** 2026-05-23T11:30:00Z
- **Tasks 01–08 complete:** 2026-05-23T12:00:00Z
- **Duration:** ~30 min (file edits + schema introspection + 4-gate Terraform local verification + atomic commit)
- **Tasks executed:** 8 of 10 (01 schema-introspect + main.tf rewrite; 02 validate+plan; 03 outputs.tf rewire; 04 script assertion; 05 infra/external/README.md; 06 infra/envs/prod/README.md; 07 memory addendum; 08 atomic commit)
- **Files committed:** 5 (one atomic commit `96e7b9f`)

## Accomplishments (Tasks 01–08)

1. **Schema introspection (Task 01 step 1)** — dumped azuread 3.8.0 schema via `terraform providers schema -json | jq`. Confirmed all three sibling resources exist (`azuread_application_registration`, `azuread_application_identifier_uri`, `azuread_application_permission_scope`) and that `requested_access_token_version` IS a top-level attribute on the registration. Confirmed `azuread_application_api_access` requires `api_client_id` (wrong abstraction for declaring the API's own scopes).
2. **`infra/external/main.tf` rewritten** — old `azuread_application.api` block deleted; new three-resource split (`azuread_application_registration.api` + `azuread_application_identifier_uri.api` + `azuread_application_permission_scope.access_as_user`); all cross-refs in `azuread_service_principal.api` + `azuread_application.spa.required_resource_access` repointed to `azuread_application_registration.api.client_id`; delegated-permission-grant `claim_values` hardened to cross-ref the scope value. `terraform fmt` no-op.
3. **`terraform validate` + `terraform plan -refresh=false` both pass** — validate exits 0, plan exits 2 (changes pending, no errors). NO `Self-referential block` error, NO `Reference to undeclared resource` error. Plan shape: `Plan: 5 to add, 1 to change, 4 to destroy.` (the 3/1/2/2 prediction with replacements rolled into add+destroy per Terraform v1.9+ rendering).
4. **`infra/external/outputs.tf` rewired** — `api_client_id` reads from registration; `api_audience_uri` + `api_scope_name` read from the new identifier_uri side-resource (no `one()` needed; side-resource exposes a plain string). Same literal values as before.
5. **`scripts/refresh-external-outputs.sh` carries the fail-loud assertion** — runs `az ad app show --query identifierUris -o tsv` BEFORE downstream file rewrites; exits 2 with FATAL message + unblock command if live list is empty. `bash -n` confirms valid syntax.
6. **`infra/external/README.md` updated** — Step 2 + Step 3 gained Phase 04.1-06 notes; new 'State reconciliation' subsection with full destroy/create/replace cascade + admin-consent re-grant procedure (framed as EXPECTED).
7. **`infra/envs/prod/README.md` updated** — false-premise 'inline is the reliable form' claim retracted; new prose names the split pattern + script mitigation + admin-consent re-grant requirement + cross-ref to infra/external/README.md.
8. **Memory file gets SUPERSEDING addendum** — old 2026-05-21 advice preserved (nested under SUPERSEDED marker) + new 2026-05-23 addendum naming the actual fix + two pattern lessons.
9. **Atomic commit `96e7b9f`** — exactly 5 files (infra/external/main.tf, outputs.tf, infra/external/README.md, infra/envs/prod/README.md, scripts/refresh-external-outputs.sh); no `.terraform/`, no `tfstate`, no `.tfvars.local`; HEAD unpushed (D-01).

## Task Commits

The code+docs ship as ONE atomic commit per CONTEXT.md D-03:

1. **Task 01: Schema introspection + main.tf rewrite (3-resource split)** — staged into `96e7b9f`
2. **Task 02: terraform validate + fmt -check + plan -refresh=false -detailed-exitcode = 2** — verification only (no files staged)
3. **Task 03: outputs.tf rewire to new side-resources** — staged into `96e7b9f`
4. **Task 04: `az ad app show` assertion in refresh-external-outputs.sh** — staged into `96e7b9f`
5. **Task 05: infra/external/README.md Step 2/3 + State Reconciliation subsection** — staged into `96e7b9f`
6. **Task 06: infra/envs/prod/README.md retract false premise + name split + script + re-grant** — staged into `96e7b9f`
7. **Task 07: Memory file SUPERSEDING addendum** — OFF-REPO; lives at `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md`; NOT part of the commit
8. **Task 08: Atomic commit via `gsd-tools commit`** — `96e7b9f`
9. **Task 09: Operator-action — `terraform apply` from local against External (CIAM) tenant** — PENDING (operator-only; checkpoint returned to orchestrator)
10. **Task 10: 04.1-HUMAN-UAT.md Test 2 → `pass` (separate post-apply commit)** — PENDING (awaits operator report from Task 09)

## Files Created/Modified (Tasks 01–08)

- `infra/external/main.tf` — MODIFIED. Three-resource split replaces the broken `azuread_application.api`. Net: 137 lines (was 96). Committed in `96e7b9f`.
- `infra/external/outputs.tf` — MODIFIED. Rewired to new side-resources. Net: 22 lines (was 23). Committed in `96e7b9f`.
- `scripts/refresh-external-outputs.sh` — MODIFIED. 25-line `az ad app show` assertion block added. Net: 86 lines (was 61). Committed in `96e7b9f`.
- `infra/external/README.md` — MODIFIED. Step 2/3 notes + new State Reconciliation subsection. Net: 217 lines (was 80). Committed in `96e7b9f`.
- `infra/envs/prod/README.md` — MODIFIED. Line 218 table cell expanded + line 229 paragraph rewritten. Net: 230 lines (was 230). Committed in `96e7b9f`.
- `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md` — MODIFIED. OFF-REPO. Old addendum preserved + new addendum appended. Net: ~65 lines (was 25). NOT in commit.
- `.planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-06-SUMMARY.md` — CREATED. This file; will land in the final metadata commit + amended post-Task-10.

## Decisions Made (Tasks 01–08)

- **`azuread_application_registration` + sibling resources, NOT `azuread_application_api_access`** — schema introspection confirmed `api_access` is for OUTBOUND scope grants (requires `api_client_id`); the right sibling for the API's own scope is `azuread_application_permission_scope`.
- **`requested_access_token_version = 2` as top-level attribute on the registration** — verified in v3.8.0 schema. Replaces old nested `api { ... }` block. LOAD-BEARING for CIAM v2-token contract.
- **Cross-ref hardening for delegated permission grant** — `claim_values = [azuread_application_permission_scope.access_as_user.value]` (was literal string). Issue 6 from plan-checker review.
- **Script-level live-state assertion, NOT Terraform-level** — `null_resource + local-exec` would add side-effect noise to the Terraform graph. The script that reads `terraform output` is the natural consumer-boundary assertion point.
- **Memory file SUPERSEDING addendum, not delete** — preserves the original false-premise text for future grep hits; new addendum is the canonical authority.
- **`terraform plan -refresh=false` for Task 02** — refresh would require live External-tenant auth the executor lacks. The graph-evaluation step is what matters (that's the layer 04.1-05 missed); refresh-skip preserves it.
- **Task 03 (outputs.tf) executed before Task 02 (validate gate)** — outputs.tf references the deleted resource after Task 01; doing outputs.tf first satisfies the validate gate without changing plan intent.

## Deviations from Plan (Tasks 01–08)

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Reordered Task 02 and Task 03**
- **Found during:** Task 02 first attempt
- **Issue:** Plan ordered Task 02 (terraform validate) BEFORE Task 03 (outputs.tf rewire), but outputs.tf still references `one(azuread_application.api.identifier_uris)` after Task 01's main.tf rewrite — `terraform validate` exits 1 with `Reference to undeclared resource`. The validate gate is impossible to pass until outputs.tf is rewired.
- **Fix:** Executed Task 03 first, then ran Task 02's full verification triad (`init -backend=false`, `validate`, `fmt -check`, `plan -refresh=false -detailed-exitcode`). All gates pass. Final commit (Task 08) still includes both files atomically per D-03.
- **Files modified:** N/A — execution-order change only, no extra code changes.
- **Committed in:** `96e7b9f` (same atomic commit as the rest).

**2. [Rule 1 - Bug] Used `-refresh=false` on `terraform plan` instead of plan's spec'd raw `terraform plan`**
- **Found during:** Task 02
- **Issue:** Plan's verification command was `terraform plan -var-file=terraform.tfvars.local`, but the local state file holds resources in the External (CIAM) tenant the executor cannot authenticate against (Gap D — workforce GHA SP rejected at the External tenant). Default refresh-on-plan would attempt a Microsoft Graph API call against the External tenant and either hang or fail.
- **Fix:** Added `-refresh=false` to skip the live refresh while preserving the resource-graph evaluator (the layer 04.1-05 missed). Exit code 2 confirms the graph fix; the live-state convergence is verified in Task 09 by the operator with proper External-tenant auth.
- **Files modified:** N/A — flag addition only.
- **Verification:** `terraform plan -var-file=terraform.tfvars.local -refresh=false -detailed-exitcode` → exit 2 (changes pending, no errors); `grep -E '(Self-referential|Reference to undeclared|^Error)' /tmp/tfplan.out` → no matches; plan summary line: `Plan: 5 to add, 1 to change, 4 to destroy.` (matches predicted 3/1/2/2 with replacements rolled into add+destroy per Terraform v1.9+ rendering).

**3. [Rule 1 - Documentation honesty] Removed `azuread_application_api_access` "intentionally NOT used" note from infra/external/README.md**
- **Found during:** Task 05 self-check
- **Issue:** Plan's `<interfaces>` block included a "Note: azuread_application_api_access is intentionally NOT used in this split" paragraph as part of the State Reconciliation subsection prose. But Task 05's `<done>` criterion + automated verify gate both say `! grep -q "azuread_application_api_access" infra/external/README.md`. Verbatim plan text conflicted with verify-gate.
- **Fix:** Dropped the explanatory paragraph from the README (the verify-gate intent is "don't recommend the wrong abstraction anywhere"; explaining why we don't use it counts as mentioning it). The full explanation lives in the memory file's new addendum and the commit message body, which are the right homes for "why we considered and rejected X" prose. Operator docs stay clean.
- **Files modified:** `infra/external/README.md` (one paragraph removed).
- **Verification:** `! grep -q 'azuread_application_api_access' infra/external/README.md && grep -q 'State reconciliation' infra/external/README.md` → both PASS.

---

**Total deviations (Tasks 01–08):** 3 auto-fixed (1 execution-order Rule 3, 1 plan-command Rule 1, 1 documentation-clarity Rule 1). No Rule 4 architectural decisions needed.
**Impact on plan:** All three deviations are mechanical adjustments at the execution boundary — the plan's INTENT is fully honored. The atomic 5-file commit lands the same shape the plan specified.

## Pending Operator Action (Task 09)

**OPERATOR-ONLY — Claude cannot authenticate into the External (CIAM) tenant (Gap D).**

Adrian must, from his local mac with `az login` against the External tenant:

1. Re-confirm `terraform plan -var-file=terraform.tfvars.local` exits 2 with the predicted 3/1/2/2 (or 5/1/4 in newer Terraform) plan shape; NO `Self-referential block` error.
2. Run `terraform apply -var-file=terraform.tfvars.local`; note the NEW `api_client_id` GUID.
3. Verify live state:
   - `az ad app show --id $NEW_API_CLIENT_ID --query identifierUris -o tsv` returns `api://$NEW_API_CLIENT_ID` (non-empty)
   - `az ad app show --id $NEW_API_CLIENT_ID --query api.requestedAccessTokenVersion -o tsv` returns `2`
4. Run `bash scripts/refresh-external-outputs.sh` — assertion prints `✓ Live identifierUris=...`, downstream files rewritten.
5. Update GitHub secret: `gh secret set VITE_API_AUDIENCE --body "api://$NEW_API_CLIENT_ID"`.
6. Re-apply `infra/envs/prod/` to roll BACKEND_AUDIENCE to ACA.
7. Re-grant admin consent via Entra portal (Identity → Applications → Enterprise applications → jobrag-spa-prod → Permissions → Grant admin consent) — EXPECTED because the API SP is replaced.
8. End-to-end sign-in roundtrip through the SPA — no AADSTS500011, no AADSTS50059, dashboard widgets render.
9. Report back with: exit codes, NEW `api_client_id`, live-state query outputs, admin-consent verification (`consentType: AllPrincipals`), sign-in result, GitHub secret update confirmation.

Full operator playbook lives in Task 09 of `04.1-06-PLAN.md` and in `infra/external/README.md` 'State reconciliation' subsection (committed in `96e7b9f`).

## Next Phase Readiness

- **Tasks 01–08 complete.** Atomic commit `96e7b9f` lands locally.
- **Task 09 pending (operator-only).** Resume signal: "applied" + structured report, OR "issue: <description>".
- **Task 10 pending.** Will flip `04.1-HUMAN-UAT.md` Test 2 to `result: pass` post-operator-report.
- **Phase 04.1 close** awaits Task 10 commit; Tests 1, 3, 4 in HUMAN-UAT remain orthogonally pending (untouched by this plan).
- **D-01 honored:** HEAD is 8 commits ahead of origin/master; operator pushes manually when ready (after Task 10 lands).

## Verification Trail (Tasks 01–08)

| Gate | Command | Result |
|------|---------|--------|
| T01 Step 1 — registration resource exists in 3.8.0 schema | `jq -r '.provider_schemas[...].resource_schemas \| keys[]' /tmp/azuread-schema.json \| grep azuread_application_registration` | PASS (all four sibling resources present) |
| T01 Step 1 — requested_access_token_version is direct attribute on registration | `jq ... .azuread_application_registration.block.attributes \| keys[]` | PASS (attribute present; alongside display_name + sign_in_audience + client_id + id) |
| T01 Step 1 — api_access requires api_client_id (Issue 1 sanity) | `jq ... .azuread_application_api_access.block.attributes \| select(.required)` | PASS (api_client_id + application_id both required → confirms wrong abstraction) |
| T01 — registration block present (count = 1) | `grep -c '^resource "azuread_application_registration"' infra/external/main.tf` | PASS (1) |
| T01 — identifier_uri block present (count = 1) | `grep -c '^resource "azuread_application_identifier_uri"' infra/external/main.tf` | PASS (1) |
| T01 — permission_scope block present (count = 1) | `grep -c '^resource "azuread_application_permission_scope"' infra/external/main.tf` | PASS (1) |
| T01 — api_access NOT used (count = 0) | `grep -c '^resource "azuread_application_api_access"' infra/external/main.tf` | PASS (0) |
| T01 — requested_access_token_version=2 ≥ 2 occurrences | `grep -c 'requested_access_token_version = 2' infra/external/main.tf` | PASS (2: registration + SPA api block) |
| T01 — old broken azuread_application api block gone | `grep -c '^resource "azuread_application" "api"' infra/external/main.tf` | PASS (0) |
| T01 — registration.api.client_id referenced ≥ 3 times (SP repoint + SPA resource_app_id + identifier_uri) | `grep -c 'azuread_application_registration.api.client_id' infra/external/main.tf` | PASS (3) |
| T01 — scope cross-ref hardening (Issue 6) | `grep -cE 'claim_values\s*=\s*\[azuread_application_permission_scope' infra/external/main.tf` | PASS (1) |
| T01 — terraform fmt | `terraform fmt main.tf` | PASS (no drift) |
| T02 — pre-flight tfvars.local exists | `test -f infra/external/terraform.tfvars.local` | PASS |
| T02 — terraform init -backend=false | `cd infra/external && terraform init -backend=false` | PASS (azuread 3.8.0 + random 3.9.0 from lock) |
| T02 — terraform validate | `terraform validate` | PASS (Success! The configuration is valid.) |
| T02 — terraform fmt -check | `terraform fmt -check` | PASS (exit 0) |
| T02 — terraform plan no errors, exit 0 or 2 | `terraform plan -var-file=terraform.tfvars.local -refresh=false -detailed-exitcode` | PASS (exit 2 — changes pending; `Plan: 5 to add, 1 to change, 4 to destroy.` summary — matches 3/1/2/2 with replacements rolled into add+destroy per Terraform v1.9+) |
| T02 — NO self-reference or undeclared-resource errors | `grep -E '(Self-referential\|Reference to undeclared\|^Error)' /tmp/tfplan.out` | PASS (no matches) |
| T03 — outputs.tf reads from new side-resource | `grep -q 'azuread_application_identifier_uri.api.identifier_uri' infra/external/outputs.tf` | PASS |
| T03 — api_client_id repointed to registration | `grep -q 'azuread_application_registration.api.client_id' infra/external/outputs.tf` | PASS |
| T03 — old one(...) reference gone | `! grep -q 'one(azuread_application.api.identifier_uris)' infra/external/outputs.tf` | PASS |
| T04 — az ad app show assertion present | `grep -q 'az ad app show' scripts/refresh-external-outputs.sh` | PASS |
| T04 — exit 2 on empty live list | `grep -q 'exit 2' scripts/refresh-external-outputs.sh` | PASS |
| T04 — memory file named in FATAL message | `grep -q 'terraform-azuread-identifier-uri-unreliable' scripts/refresh-external-outputs.sh` | PASS |
| T04 — bash -n valid syntax | `bash -n scripts/refresh-external-outputs.sh` | PASS |
| T05 — infra/external/README.md names registration | `grep -q 'azuread_application_registration' infra/external/README.md` | PASS |
| T05 — names Phase 04.1-06 | `grep -q 'Phase 04.1-06' infra/external/README.md` | PASS |
| T05 — State reconciliation subsection present | `grep -q 'State reconciliation' infra/external/README.md` | PASS |
| T05 — admin-consent re-grant procedure named | `grep -qE '(re-grant\|Grant admin consent\|admin-consent)' infra/external/README.md` | PASS |
| T05 — api_access NOT mentioned | `! grep -q 'azuread_application_api_access' infra/external/README.md` | PASS |
| T06 — infra/envs/prod/README.md false premise retracted | `! grep -q 'inline attribute is the reliable form' infra/envs/prod/README.md` | PASS |
| T06 — names registration | `grep -q 'azuread_application_registration' infra/envs/prod/README.md` | PASS |
| T06 — names script assertion | `grep -q 'az ad app show' infra/envs/prod/README.md` | PASS |
| T06 — names Phase 04.1-06 | `grep -q 'Phase 04.1-06' infra/envs/prod/README.md` | PASS |
| T06 — api_access NOT mentioned | `! grep -q 'azuread_application_api_access' infra/envs/prod/README.md` | PASS |
| T07 — memory SUPERSEDED marker present | `grep -q 'SUPERSEDED 2026-05-23 by Phase 04.1-06' ~/.claude/projects/.../memory/...` | PASS |
| T07 — memory names registration | `grep -q 'azuread_application_registration' ~/.claude/projects/.../memory/...` | PASS |
| T07 — old 2026-05-21 addendum text PRESERVED | `grep -q 'Update (2026-05-21)' ~/.claude/projects/.../memory/...` | PASS |
| T07 — memory names script assertion | `grep -q 'az ad app show.*identifierUris' ~/.claude/projects/.../memory/...` | PASS |
| T07 — memory names schema-introspection pattern lesson | `grep -q 'terraform providers schema' ~/.claude/projects/.../memory/...` | PASS |
| T08 — commit subject `^fix(04.1-06):` | `git log -1 --pretty=format:%s` | PASS (`fix(04.1-06): split azuread_application.api into registration + sibling resources`) |
| T08 — exactly 5 expected files | `git log -1 --name-only --pretty=format:'' \| sort` | PASS (infra/envs/prod/README.md, infra/external/README.md, infra/external/main.tf, infra/external/outputs.tf, scripts/refresh-external-outputs.sh) |
| T08 — no .terraform/, no tfstate, no .tfvars.local | `! git log -1 --name-only --pretty=format:'' \| grep -qE '\.terraform\|tfstate\|\.tfvars\.local'` | PASS |
| T08 — no unexpected deletions | `git diff --diff-filter=D --name-only HEAD~1 HEAD` | PASS (empty) |
| T08 — HEAD unpushed (D-01) | `git rev-list --count origin/master..HEAD` | PASS (8 ahead of origin/master, unpushed) |

## Self-Check (Tasks 01–08): PASSED

- `infra/external/main.tf` — FOUND, MODIFIED (3-resource split + cross-ref hardening)
- `infra/external/outputs.tf` — FOUND, MODIFIED (rewired to new side-resources)
- `scripts/refresh-external-outputs.sh` — FOUND, MODIFIED (az ad app show assertion present)
- `infra/external/README.md` — FOUND, MODIFIED (Step 2/3 + State Reconciliation subsection)
- `infra/envs/prod/README.md` — FOUND, MODIFIED (false premise retracted, split + re-grant prose added)
- `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md` — FOUND (off-repo), MODIFIED (SUPERSEDING addendum + new schema-introspection lesson)
- `.planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-06-SUMMARY.md` — FOUND (this file, partial)
- Commit `96e7b9f` — FOUND in git log; exactly 5 files staged; HEAD unpushed (8 commits ahead of origin/master)

## TDD Gate Compliance

N/A — plan type is `execute` (not `tdd`); no TDD gate sequence required. Per Task 01 step 1 + the schema-introspection pattern lesson, "test the provider schema before claiming a resource carries an attribute" was applied as the planning-phase equivalent of a RED-gate proof.

---

*Phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup*
*Status: Tasks 01–08 of 10 complete; Task 09 operator-action gate awaiting Adrian; Task 10 pending operator's report.*
*Partial completion: 2026-05-23*
