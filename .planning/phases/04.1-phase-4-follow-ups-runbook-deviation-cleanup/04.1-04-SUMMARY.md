---
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
plan: 04
subsystem: infra-docs
tags: [docs, infra, bootstrap, ciam, entra-external, runbook]

requires:
  - phase: 04-frontend-shell-auth
    provides: "04-06-SUMMARY.md deviation #6 (CIAM customer namespace ≠ B2B guest namespace) and 04-06-RUNBOOK.md Checkpoint 5 (KV set → ACA restart → live browser sign-in → AUTH-04/07 verification) — the canonical Phase 4 close artifacts the new bootstrap section cross-references."
  - phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
    provides: "04.1-03 landed the BootErrorFallback (ee4a1e5 + 7f1b6d8); serial-execution chain (D-04) means plan 04 was unblocked the moment 03 committed. 04.1-01's init_db() rebridge is referenced from the new Step 5 so operators know rotation auto-applies on ACA restart."
provides:
  - "infra/bootstrap/README.md gains a new top-level 'Step 5 — Bootstrap a first-login customer/Member account (~3 min)' section between the existing Step 4 (terraform import) and the 'Knowingly-accepted security trade-offs' section. The section explains the CIAM customer-vs-B2B-guest namespace disjointness, provides the exact `az ad user create` + `az ad user show` commands against the External tenant, and tells operators how the captured OID feeds the KV `seeded-user-entra-oid` secret + `users.entra_oid` UPDATE downstream."
  - "infra/envs/prod/README.md 'Phase 4 close (2026-05-21)' section's `B2B-guest-vs-customer namespace` paragraph now cross-references `infra/bootstrap/README.md` Step 5 (Phase 04.1 fix 4) as the canonical walkthrough instead of embedding the long-form `az ad user create` command inline."
affects:
  - "Future operators bootstrapping a fresh Entra External tenant — they now hit the customer/Member-creation step in the right place (top-to-bottom read of `infra/bootstrap/README.md`) instead of discovering it only after a failed Phase 4 sign-in attempt."
  - "Multi-user expansion path — the new section's 'When to repeat this step' subsection names AUTH-06 (single-OID equality) as the gate that needs to become an IN-set or role check when v2 ships."
  - "Memory `ciam-customer-vs-b2b-guest.md` — dated addendum points future Claude sessions at `infra/bootstrap/README.md` Step 5 as the canonical doc for the trap."

tech-stack:
  added: []
  patterns:
    - "Canonical-doc-in-source-runbook pattern — Phase 4 close-out captured deviation #6 with a one-paragraph note in `infra/envs/prod/README.md`, but the canonical source-of-truth for tenant creation is `infra/bootstrap/README.md`. The fix pattern: keep the warning in the prod README (operators flipping through Phase 4 close still get the heads-up) but move the long-form walkthrough to the bootstrap README and cross-reference it. Future deviations involving tenant-creation steps should land in bootstrap first, prod second."
    - "Phase-cross-referenced docs pattern (carried from 04.1-02/03) — the new Step 5 section names both `.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md deviation #6` and `04-06-RUNBOOK.md Checkpoint 5` so future readers find the deviation context + the runbook walkthrough without grepping the planning tree. Also names 'Phase 04.1 fix 1' so operators know the init_db rebridge auto-applies on ACA restart."

key-files:
  created: []
  modified:
    - "infra/bootstrap/README.md — gained a 94-line 'Step 5 — Bootstrap a first-login customer/Member account (~3 min)' section between existing Step 4 and the 'Knowingly-accepted security trade-offs' section. Includes: customer-vs-B2B-guest namespace explanation, `az login --tenant` switch instruction, `az ad user create` command, `az ad user show` OID capture, downstream KV+init_db wiring, 'When to repeat this step' subsection."
    - "infra/envs/prod/README.md — one-line edit on line 223. The `B2B-guest-vs-customer namespace` paragraph's embedded `az ad user create` command was replaced with a cross-reference to `infra/bootstrap/README.md` → Step 5 (Phase 04.1 fix 4)."

key-decisions:
  - "Section placement BETWEEN Step 4 (terraform import) and 'Knowingly-accepted security trade-offs' rather than as a trailing note. Reasoning: the bootstrap operator reads top-to-bottom; user creation logically follows tenant existence (Step 1) and Terraform bootstrap (Step 2), and precedes the Phase 4 sign-in flow (`04-06-RUNBOOK.md`). Placing it as 'Step 5' makes it part of the normal tenant-creation walkthrough rather than a footnote."
  - "Keep the SHORT warning paragraph in `infra/envs/prod/README.md` (just point to bootstrap) instead of deleting it entirely. Operators triaging Phase 4 sign-in failures land on the prod README — they still need the namespace-disjointness heads-up before being redirected to the canonical walkthrough. Cuts the embedded long-form CLI to a single line + cross-reference."
  - "Use `docs(...)` commit prefix not `fix(...)`. This is a pure documentation change with no code, no infra, no test — matches project convention (e.g., `docs(04-06): flip Phase 4 to COMPLETE in STATE + ROADMAP + prod README`). PLAN task 04 already specified this prefix explicitly."

patterns-established:
  - "Two-file canonical-docs cross-reference pattern — when a deviation has a Phase 3 root cause (tenant bootstrap) but a Phase 4 manifestation (sign-in failure), the canonical doc lives in the Phase 3 source-of-truth (bootstrap README) and the Phase 4 doc (prod README) points back. Avoids drift: there's exactly one authoritative copy of the walkthrough; the warning sentence in the other doc is intentionally short so it doesn't fall out of sync."
  - "Explicit `--tenant` context switching in operator-facing docs — any new `az ...` command in a bootstrap or runbook README should be preceded by an `az account show --query tenantId` confirmation step + the `az login --tenant <id>` switch if needed. The new Step 5 follows this pattern; future infra docs should adopt it."

requirements-completed: []  # Phase 04.1 follow-ups have no AUTH-*/SHEL-* requirements mapped — these are durable-tax cleanup, not new requirements (D-06).

duration: ~22m (cross-context — see Performance section)
completed: 2026-05-21
---

# Phase 04.1 Plan 04: CIAM Customer/Member Bootstrap Documentation Summary

**`infra/bootstrap/README.md` now contains the canonical 'Step 5 — Bootstrap a first-login customer/Member account' section, and `infra/envs/prod/README.md` cross-references it. Future operators bootstrapping an Entra External tenant land on the customer-creation step at the natural moment (tenant-creation time), instead of discovering it after a ~1h B2B-guest-can't-sign-in diagnosis during Phase 4.**

## Performance

- **Tasks:** 4 (executed in order: bootstrap-README edit → prod-README edit → memory addendum → atomic commit)
- **Files modified:** 2 (one atomic commit)
- **Note on duration:** the executor agent for this plan was interrupted by a transport-layer drop after the atomic commit landed (`0b6f0ea`). All file edits and the commit succeeded before the drop; the SUMMARY.md / STATE.md / ROADMAP.md bookkeeping was completed by the orchestrator in a follow-up step. Effective work-time end-to-end was on the order of 20 minutes.

## Accomplishments

- `infra/bootstrap/README.md`: gained 94 new lines (file went from 97 → 191 lines). New 'Step 5 — Bootstrap a first-login customer/Member account (~3 min)' section between existing Step 4 and the 'Knowingly-accepted security trade-offs' section. Section includes:
  - 'Why this isn't optional (B2B guest vs customer namespace)' subsection — explains the disjoint namespaces inside the External tenant.
  - Explicit `az login --tenant <External-tenant-GUID-from-Step-1>` context switch step with `az account show --query tenantId -o tsv` confirmation probe.
  - The full `az ad user create --user-principal-name adrian@jobrag.onmicrosoft.com --display-name "Adrian Zaplata" --password '<strong-temp-password>' --force-change-password-next-sign-in false` command.
  - The `az ad user show --id ... --query id -o tsv` OID capture command.
  - 'Where this OID is used downstream' subsection — names both the KV `seeded-user-entra-oid` secret + the `users.entra_oid` row UPDATE via `init_db()` (cross-referencing Phase 04.1 fix 1).
  - 'When to repeat this step' subsection — names fresh-prod, multi-user expansion (AUTH-06 needs to expand from single-OID equality), and OID rotation.
- `infra/envs/prod/README.md`: line 223 replaced. The `B2B-guest-vs-customer namespace` paragraph's embedded long-form CLI is gone; a cross-reference to `infra/bootstrap/README.md` → "Step 5 — Bootstrap a first-login customer/Member account" (Phase 04.1 fix 4) takes its place. The warning intent is preserved so operators triaging Phase 4 sign-in failures still see the namespace-disjointness heads-up.
- `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/ciam-customer-vs-b2b-guest.md`: dated `**Update (2026-05-21):**` addendum noting the structural doc landed in `infra/bootstrap/README.md` Step 5 + naming federation IdPs as the deferred future path.

## Task Commits

The whole plan ships as ONE atomic commit per CONTEXT.md D-03:

1. **Task 01: Insert 'Step 5' section into `infra/bootstrap/README.md`** — staged into `0b6f0ea`
2. **Task 02: Update `infra/envs/prod/README.md` line 223 cross-reference** — staged into `0b6f0ea`
3. **Task 03: Append memory addendum** — memory store, not git-tracked
4. **Task 04: Atomic commit via `gsd-tools commit`** — `0b6f0ea` (docs)

**Plan metadata:** _(this SUMMARY.md + STATE/ROADMAP update is the final docs commit, separate from `0b6f0ea`)_

## Files Created/Modified

- `infra/bootstrap/README.md` — MODIFIED. +94 lines of new 'Step 5' section. Committed in `0b6f0ea`.
- `infra/envs/prod/README.md` — MODIFIED. 1 line replaced (line 223). Committed in `0b6f0ea`.

## Decisions Made

- **Section placement between Step 4 and 'Knowingly-accepted security trade-offs'**. The bootstrap operator reads top-to-bottom; user creation belongs in the normal tenant-creation walkthrough, not as a footnote. Plan task 01 made this insertion point explicit; followed verbatim.
- **Keep the SHORT warning paragraph in `infra/envs/prod/README.md`** instead of deleting it. Operators triaging Phase 4 sign-in failures still need the namespace-disjointness heads-up before being redirected. The new sentence is one line — short enough not to drift, long enough to flag the trap.
- **`docs(...)` commit prefix** matches project convention for pure documentation changes (e.g., `docs(04-06): flip Phase 4 to COMPLETE in STATE + ROADMAP + prod README`). PLAN task 04 specified this prefix explicitly.

## Deviations from Plan

None at the file-edit level — both README edits match PLAN tasks 01 and 02 verbatim (acceptance criteria below all PASS). The only divergence is operational: the executor agent's transport connection dropped after `0b6f0ea` landed but before SUMMARY.md / STATE.md / ROADMAP.md were written. The orchestrator finished the bookkeeping in a follow-up step. All artifacts (the 2 README edits, the memory addendum, the atomic commit, this SUMMARY) are present.

---

**Total deviations:** 0 (file-edit deviations)
**Impact on plan:** Plan executed exactly as written at the file-edit level. The bookkeeping split into a second authoring step is operational, not semantic.

## Issues Encountered

- **Transport-layer drop on the executor agent** after `0b6f0ea` was committed. Bookkeeping (SUMMARY.md, STATE.md, ROADMAP.md) was completed by the orchestrator. No work was lost; the atomic commit landed cleanly.

## User Setup Required

None — pure docs. The new section documents an operator step (`az ad user create`); running it is the next operator's job during the next fresh-tenant bootstrap, NOT part of this phase.

## Next Phase Readiness

- Plan 04 of Phase 04.1 complete.
- **Next:** Plan 04.1-05 (refactor `infra/external/main.tf` to drop the TF `identifier_uri` workaround). Depends on 04.1-04 per the serial-execution chain (D-04). 1/5 plans of phase 04.1 remain after this.
- **Pending on Adrian:** Nothing for plan 04. Docs ship dormant on the next push to `master` (D-01 forbids `git push` from this phase, so this lands locally + waits for Adrian's batch-push of all 5 fixes).
- **No blockers** for 04.1-05.

## Verification Trail

| Gate | Command | Result |
|------|---------|--------|
| New 'Step 5' section header present | `grep -q "^## Step 5" infra/bootstrap/README.md` | PASS |
| customer/Member account terminology | `grep -q "customer/Member account\|customer.*Member account" infra/bootstrap/README.md` | PASS |
| B2B guest explanation | `grep -q "B2B guest" infra/bootstrap/README.md` | PASS |
| `az ad user create` command present | `grep -q "az ad user create" infra/bootstrap/README.md` | PASS |
| Exact CLI form (UPN flag) | `grep -q "az ad user create --user-principal-name" infra/bootstrap/README.md` | PASS |
| `force-change-password-next-sign-in false` flag | `grep -q "force-change-password-next-sign-in false" infra/bootstrap/README.md` | PASS |
| OID capture command | `grep -q "az ad user show" infra/bootstrap/README.md` | PASS |
| Downstream KV / env var | `grep -q "seeded-user-entra-oid\|SEEDED_USER_ENTRA_OID" infra/bootstrap/README.md` | PASS |
| Phase 4 close cross-references | `grep -q "04-06-RUNBOOK.md\|04-06-SUMMARY.md" infra/bootstrap/README.md` | PASS |
| Phase 04.1 fix 1 cross-reference | `grep -q "Phase 04.1 fix 1\|init_db" infra/bootstrap/README.md` | PASS |
| Existing Step 4 + trade-offs sections unchanged | `grep -q "^## Step 4" && grep -q "^## Knowingly-accepted security trade-offs"` | PASS |
| Line-count grew substantially | `[ "$(wc -l < infra/bootstrap/README.md)" -gt 137 ]` | PASS (191 lines) |
| prod README cross-reference landed | `grep -q "canonical customer/Member bootstrap step is now documented in.*infra/bootstrap/README.md" infra/envs/prod/README.md` | PASS |
| Phase 04.1 fix 4 reference | `grep -q "Phase 04.1 fix 4" infra/envs/prod/README.md` | PASS |
| Step 5 name in prod README | `grep -q "Step 5 — Bootstrap a first-login customer/Member account" infra/envs/prod/README.md` | PASS |
| Embedded long-form CLI gone | `! grep -q "Create a local Member via .az ad user create --user-principal-name" infra/envs/prod/README.md` | PASS |
| Warning header preserved | `grep -q "B2B-guest-vs-customer namespace" infra/envs/prod/README.md` | PASS |
| Phase 4 close section unchanged | `grep -q "## Phase 4 close (2026-05-21)" infra/envs/prod/README.md` | PASS |
| ACA smoke note unchanged | `grep -q "ACA deploy-api.yml smoke check" infra/envs/prod/README.md` | PASS |
| Memory addendum present | `grep -q "Phase 04.1 plan 04\|04.1-04" memory/ciam-customer-vs-b2b-guest.md` | PASS |
| Commit subject matches `^docs\(04\.1-04\):` | `git log -1 --pretty=format:%s` on `0b6f0ea` | PASS (`docs(04.1-04): document customer/Member bootstrap in tenant runbook`) |
| Commit lists exactly 2 files | `git log -1 --stat 0b6f0ea` | PASS (infra/bootstrap/README.md, infra/envs/prod/README.md) |

## Self-Check: PASSED

- `.planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-04-SUMMARY.md` — FOUND (this file)
- `infra/bootstrap/README.md` — FOUND (MODIFIED, +94 lines)
- `infra/envs/prod/README.md` — FOUND (MODIFIED, 1 line replaced)
- Commit `0b6f0ea` — FOUND (`git log` shows the matching subject + 2-file stat)

---
*Phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup*
*Completed: 2026-05-21*
