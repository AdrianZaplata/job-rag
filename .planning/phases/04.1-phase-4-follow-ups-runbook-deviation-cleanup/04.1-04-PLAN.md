---
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
plan: 04
type: execute
wave: 3
depends_on: [04.1-03-PLAN.md]
files_modified:
  - infra/bootstrap/README.md
  - infra/envs/prod/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "infra/bootstrap/README.md gains a new top-level section (suggested title 'Step 5 — Bootstrap a first-login customer/Member account') that documents the customer-vs-B2B-guest CIAM namespace disjointness AND the exact `az ad user create` command for the External tenant"
    - "The new section explains WHY B2B guests don't work (CIAM user flow only resolves customer-namespace identities; B2B guests need federation IdP config not in v1 scope)"
    - "The new section provides the exact CLI command: `az ad user create --user-principal-name <name>@jobrag.onmicrosoft.com --display-name '<Name>' --password '<temp-password>' --force-change-password-next-sign-in false` (with explicit `--tenant <External-tenant-id>` or a note that the operator must be `az login`'d to the External tenant first)"
    - "The new section tells the operator where to capture the resulting OID (Azure Portal user blade OR `az ad user show --id <upn> --query id -o tsv`) and how it feeds the downstream KV `seeded-user-entra-oid` secret + `users.entra_oid` UPDATE (cross-references Phase 4 close runbook)"
    - "The new section cross-references `.planning/phases/04-frontend-shell-auth/04-06-RUNBOOK.md` Checkpoint 5 and `.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md` deviation #6"
    - "infra/envs/prod/README.md 'Phase 4 close' section gains a one-line cross-reference pointing operators back to `infra/bootstrap/README.md` for the canonical customer-bootstrap step (instead of leaving the bootstrap snippet only in the prod README)"
    - "No code change. No infra apply. No Entra resource mutation. Pure docs."
    - "`python -c \"import re,sys; sys.exit(0 if re.search(r'az ad user create', open('infra/bootstrap/README.md').read()) else 1)\"` exits 0"
    - "One atomic commit lands with message matching `docs(04.1-04): document customer/Member bootstrap` (or close paraphrase) — only the 2 README files in the commit"
  artifacts:
    - path: "infra/bootstrap/README.md"
      provides: "canonical customer/Member bootstrap step in Phase 3 tenant-creation runbook"
      contains: "az ad user create"
    - path: "infra/envs/prod/README.md"
      provides: "cross-reference to bootstrap README so prod-runbook operators find the customer-bootstrap step"
      contains: "infra/bootstrap/README.md"
  key_links:
    - from: "infra/bootstrap/README.md new section"
      to: ".planning/phases/04-frontend-shell-auth/04-06-RUNBOOK.md"
      via: "operator runs the bootstrap, then follows the Phase 4 close runbook for KV/DB seeding"
      pattern: "04-06-RUNBOOK\\.md|04-06-SUMMARY\\.md"
---

<objective>
Fix 4 of 5 (Phase 04.1) — Document the customer/Member bootstrap step in
`infra/bootstrap/README.md` (the Phase 3 tenant-creation runbook), so future
operators creating a fresh Entra External tenant get the customer-account
creation step in the right place — at tenant-creation time, NOT after they
hit the B2B-guest-can't-sign-in dead-end during Phase 4 sign-in attempts.

**Why:** Phase 4 close-out deviation #6 cost ~1h of diagnosis because Adrian's
mental model was "I'm a B2B guest in this tenant, I should be able to sign
in" — but CIAM user flows only resolve customer-namespace identities, and
B2B guests need explicit federation IdP config that v1 doesn't ship. The
fix is documented at the prod README ("Phase 4 close" section line 223) as
a one-paragraph note, but the canonical source-of-truth for tenant creation
is `infra/bootstrap/README.md`, and operators read that file top-to-bottom
when bootstrapping a new tenant. The doc needs to live there.

**Source of truth for this deviation:**
`.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md` deviation #6
("CIAM customer namespace ≠ B2B guest namespace") + memory
`ciam-customer-vs-b2b-guest.md`.

**Output:** New "Step 5" section in `infra/bootstrap/README.md` + one-line
cross-reference in `infra/envs/prod/README.md` "Phase 4 close" section + one
atomic commit. Pure docs. No code, no infra, no cloud.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-CONTEXT.md
@.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md
@.planning/phases/04-frontend-shell-auth/04-06-RUNBOOK.md
@infra/bootstrap/README.md
@infra/envs/prod/README.md
@CLAUDE.md

<phase_constraints>
**Phase-wide constraints (from 04.1-CONTEXT.md):**
- D-01: No `git push`. Local atomic commit only.
- D-02: No cloud touches. No `az ad user create` execution — pure documentation. Verification is `grep` for required anchors.
- D-03: One atomic commit per fix.
- D-04: Serial execution — this is Plan 04 of 5; depends on 04.1-03.
- D-20/D-21/D-22/D-23: bootstrap README is primary, prod README gets a one-line pointer, no actual `az` execution, federation IdP path stays deferred.
</phase_constraints>

<interfaces>
**Existing `infra/bootstrap/README.md` structure** (97 lines):
- "Prerequisites" (lines 9-13)
- "Step 1 — Create the Entra External tenant (manual portal click-path, ~5 min)" (lines 15-29)
- "Step 2 — Run bootstrap Terraform" (lines 31-65)
- "Step 3 — Capture outputs into envs/{prod,dev}/backend.tf" (lines 67-85)
- "Step 4 — terraform import the External tenant (per D-05)" (lines 87-91)
- "Knowingly-accepted security trade-offs" (lines 93-97)

**Where the new section goes:** As a new "Step 5 — Bootstrap a first-login customer/Member account (~3 min)" inserted BETWEEN current Step 4 and "Knowingly-accepted security trade-offs". This is the right insertion point because:
1. Tenant must exist (Step 1) and bootstrap must have run (Step 2) before the user creation can succeed.
2. It naturally precedes the Phase 4 sign-in flow (which lives in `04-06-RUNBOOK.md`).
3. It groups with the other tenant-creation steps rather than appearing as a trailing trade-off note.

**New section content** (suggested — executor adjusts wording if it reads better, but the listed key information MUST be present):

```markdown
## Step 5 — Bootstrap a first-login customer/Member account (~3 min)

Once the tenant exists (Step 1) and the storage backend is provisioned (Step 2),
create a customer/Member account that can actually sign in via the CIAM user
flow. **Do NOT skip this in favor of inviting a B2B guest** — see the warning
below.

### Why this isn't optional (B2B guest vs customer namespace)

Entra External ID has two disjoint identity namespaces inside the same tenant:

- **Customer/Member accounts** (this step) — created via `az ad user create` or
  the External-tenant self-service signup flow. These are the ONLY identities
  the CIAM user flow resolves at sign-in.
- **B2B guests** — created via the Entra portal "Invite external user" flow.
  They show up in `az ad user list`, but the CIAM user flow rejects them with
  "We couldn't find an account with this email address or password" because
  B2B guests authenticate against their **home tenant** via federation IdPs,
  which are NOT configured in v1.

Phase 4's first sign-in attempt cost ~1h diagnosing this exact trap (see
`.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md` deviation #6 and
memory `ciam-customer-vs-b2b-guest.md`). Configuring federation IdPs to
accept B2B guests is a future Phase-4-revision path; the simpler v1 fix is
to create a Member account directly in the External tenant.

### Create the Member account

You must be `az login`'d to the **External tenant** for this step. Switch
context if you bootstrapped earlier in the workforce tenant:

```bash
# Confirm which tenant az is currently pointed at.
az account show --query tenantId -o tsv

# If it's NOT the External tenant ID from Step 1, switch:
az login --tenant <External-tenant-GUID-from-Step-1>
```

Then create the user (replace `adrian` and the password with your own — and
pick a strong throwaway, then rotate it on first sign-in):

```bash
az ad user create \
  --user-principal-name adrian@jobrag.onmicrosoft.com \
  --display-name "Adrian Zaplata" \
  --password '<strong-temp-password>' \
  --force-change-password-next-sign-in false
```

Capture the resulting object ID (this is the `oid` claim that Phase 4's
AUTH-06 allowlist gate matches against):

```bash
az ad user show \
  --id adrian@jobrag.onmicrosoft.com \
  --query id -o tsv
# → e.g. 18d774c1-62ac-4416-8945-b5eca715e9ed
```

### Where this OID is used downstream

The captured OID feeds two places in the prod runbook:

1. **Key Vault secret** `seeded-user-entra-oid` — set via
   `az keyvault secret set --vault-name jobrag-prod-kv --name seeded-user-entra-oid --value <oid>`.
   The ACA backend reads this via `secretRef` as the `SEEDED_USER_ENTRA_OID`
   env var; AUTH-06 rejects any JWT with a different `oid`.
2. **`users.entra_oid` row** — bridged from the seeded UUID to the real OID
   via `init_db()` on container boot (idempotent every-boot UPDATE — see
   Phase 04.1 fix 1). After Phase 04.1 fix 1 lands, an ACA revision restart
   re-runs the bridge automatically.

For the full first-sign-in walkthrough (KV set → ACA restart → live browser
sign-in → AUTH-04/07 verification), follow
`.planning/phases/04-frontend-shell-auth/04-06-RUNBOOK.md` Checkpoint 5.

### When to repeat this step

- **Fresh prod environment.** Every new External tenant + prod-env build pair.
- **Multi-user expansion (future).** Same `az ad user create` per user. AUTH-06
  needs to expand from single-OID equality to a `IN (...)` set or a role check
  (out of scope for v1).
- **OID rotation.** If the Member account is deleted + recreated, OID changes
  — re-run the KV `secret set` + restart ACA so `init_db()` re-bridges.
```

**Where the prod README cross-reference goes** (existing `infra/envs/prod/README.md` line 223):

The current text:
```markdown
**B2B-guest-vs-customer namespace** — the CIAM user flow only resolves customer-namespace identities. The Entra-portal "Invite external user" flow creates B2B guests that CANNOT sign in via the user flow even though they show up in `az ad user list`. Create a local Member via `az ad user create --user-principal-name <name>@jobrag.onmicrosoft.com --password '...' --force-change-password-next-sign-in false` against the External tenant.
```

Replace with (keep the warning intent; defer the canonical walkthrough):
```markdown
**B2B-guest-vs-customer namespace** — the CIAM user flow only resolves customer-namespace identities. The Entra-portal "Invite external user" flow creates B2B guests that CANNOT sign in via the user flow even though they show up in `az ad user list`. The canonical customer/Member bootstrap step is now documented in `infra/bootstrap/README.md` → "Step 5 — Bootstrap a first-login customer/Member account" (Phase 04.1 fix 4). Run that step BEFORE attempting first sign-in.
```

This keeps the warning visible in the prod runbook (operators flipping through Phase 4 close still get the heads-up) while pointing the long-form canonical walkthrough back to bootstrap (where new-tenant operators land naturally).
</interfaces>
</context>

<tasks>

<task id="01" type="execute">
  <action>Insert the new "Step 5 — Bootstrap a first-login customer/Member account (~3 min)" section into `infra/bootstrap/README.md` BETWEEN the existing "Step 4 — terraform import the External tenant (per D-05)" section (currently ends at line 91) and the "Knowingly-accepted security trade-offs" section (currently starts at line 93).

  Use the exact section content shown in `<interfaces>` "New section content" — the executor MAY adjust prose wording for readability but MUST preserve:
  - The header `## Step 5 — Bootstrap a first-login customer/Member account (~3 min)` (or equivalent — see acceptance criteria for required anchor strings).
  - The "Why this isn't optional (B2B guest vs customer namespace)" explanation with the disjoint-namespace framing.
  - The exact `az ad user create --user-principal-name adrian@jobrag.onmicrosoft.com …` command.
  - The exact `az ad user show … --query id -o tsv` capture command.
  - Cross-references to `04-06-RUNBOOK.md` Checkpoint 5 AND `04-06-SUMMARY.md` deviation #6.
  - Cross-reference to "Phase 04.1 fix 1" for the `init_db()` re-bridge (so operators know the migration trap is fixed).
  - The "When to repeat this step" subsection (multi-user expansion, OID rotation).

  Do NOT modify Steps 1-4 or the "Knowingly-accepted security trade-offs" section. Do NOT reorder existing sections. The change is purely additive: one new H2 section in the middle.

  Whitespace: preserve a blank line between Step 4's last line and the new `## Step 5 …` header; preserve a blank line between the new section's last line and the existing `## Knowingly-accepted security trade-offs` header.
  </action>
  <read_first>
    - infra/bootstrap/README.md (full file — to confirm exact insertion point and to match heading-level + code-block style of existing sections)
    - .planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md (deviation #6 source)
    - ~/.claude/projects/-Users-adrian-Developer-job-rag/memory/ciam-customer-vs-b2b-guest.md (existing memory body — match its framing/terminology)
    - .planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-CONTEXT.md (D-20)
  </read_first>
  <acceptance_criteria>
    - `grep -q "^## Step 5" infra/bootstrap/README.md`
    - `grep -q "customer/Member account\|customer.*Member account" infra/bootstrap/README.md`
    - `grep -q "B2B guest" infra/bootstrap/README.md`
    - `grep -q "az ad user create" infra/bootstrap/README.md`
    - `grep -q "az ad user create --user-principal-name" infra/bootstrap/README.md` (exact command form)
    - `grep -q "force-change-password-next-sign-in false" infra/bootstrap/README.md`
    - `grep -q "az ad user show" infra/bootstrap/README.md` (OID capture command)
    - `grep -q "seeded-user-entra-oid\|SEEDED_USER_ENTRA_OID" infra/bootstrap/README.md` (downstream KV use)
    - `grep -q "04-06-RUNBOOK.md\|04-06-SUMMARY.md" infra/bootstrap/README.md` (cross-reference to Phase 4 close artifacts)
    - `grep -q "Phase 04.1 fix 1\|init_db" infra/bootstrap/README.md` (cross-reference to the now-fixed init_db rebridge)
    - `grep -q "^## Step 4" infra/bootstrap/README.md && grep -q "^## Knowingly-accepted security trade-offs" infra/bootstrap/README.md` (existing sections unchanged)
    - Line count of `infra/bootstrap/README.md` grows by at least 40 lines (substantial new section, not a one-line patch): `[ "$(wc -l < infra/bootstrap/README.md)" -gt 137 ]`
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <action>Update `infra/envs/prod/README.md` line 223 — the existing **B2B-guest-vs-customer namespace** paragraph — to point at the new canonical bootstrap section. Replace the current line with:

  ```markdown
  **B2B-guest-vs-customer namespace** — the CIAM user flow only resolves customer-namespace identities. The Entra-portal "Invite external user" flow creates B2B guests that CANNOT sign in via the user flow even though they show up in `az ad user list`. The canonical customer/Member bootstrap step is now documented in `infra/bootstrap/README.md` → "Step 5 — Bootstrap a first-login customer/Member account" (Phase 04.1 fix 4). Run that step BEFORE attempting first sign-in.
  ```

  Do NOT touch any other line in the "Phase 4 close" section. Do NOT touch the table on lines 215-221. Do NOT touch the "Pre-existing CI failures" line (225) or the "ACA deploy-api.yml smoke check" line (227).
  </action>
  <read_first>
    - infra/envs/prod/README.md (lines 200-227 — the Phase 4 close section)
    - .planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-CONTEXT.md (D-21)
  </read_first>
  <acceptance_criteria>
    - `grep -q "canonical customer/Member bootstrap step is now documented in.*infra/bootstrap/README.md" infra/envs/prod/README.md`
    - `grep -q "Phase 04.1 fix 4" infra/envs/prod/README.md`
    - `grep -q "Step 5 — Bootstrap a first-login customer/Member account" infra/envs/prod/README.md`
    - `! grep -q "Create a local Member via .az ad user create --user-principal-name" infra/envs/prod/README.md` (the embedded long-form CLI is gone — replaced by the cross-reference)
    - `grep -q "B2B-guest-vs-customer namespace" infra/envs/prod/README.md` (the warning header is preserved)
    - `grep -q "## Phase 4 close (2026-05-21)" infra/envs/prod/README.md` (section unchanged)
    - `grep -q "ACA deploy-api.yml smoke check" infra/envs/prod/README.md` (other Phase 4 close notes unchanged)
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <action>Append a brief addendum to memory file
  `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/ciam-customer-vs-b2b-guest.md`
  noting the documentation landed:

  ```markdown

  **Update (2026-05-21):** Phase 04.1 plan 04 landed the canonical
  customer/Member bootstrap step in `infra/bootstrap/README.md` Step 5 — see
  also `.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md` deviation #6.
  Federation IdPs for B2B guests remain a future Phase-4-revision path; the
  simpler v1 fix (customer-bootstrap via `az ad user create`) is now the
  documented default.
  ```
  </action>
  <read_first>
    - ~/.claude/projects/-Users-adrian-Developer-job-rag/memory/ciam-customer-vs-b2b-guest.md (existing body)
  </read_first>
  <acceptance_criteria>
    - `grep -q "Phase 04.1 plan 04\|04.1-04" ~/.claude/projects/-Users-adrian-Developer-job-rag/memory/ciam-customer-vs-b2b-guest.md`
    - `grep -q "infra/bootstrap/README.md Step 5\|Step 5" ~/.claude/projects/-Users-adrian-Developer-job-rag/memory/ciam-customer-vs-b2b-guest.md`
  </acceptance_criteria>
</task>

<task id="04" type="execute">
  <action>Commit the 2 README files as one atomic commit:

  ```bash
  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
    "docs(04.1-04): document customer/Member bootstrap in tenant runbook" \
    --files "infra/bootstrap/README.md" \
    --files "infra/envs/prod/README.md"
  ```

  Verify:
  ```bash
  git log -1 --stat
  ```
  Shows exactly 2 files: `infra/bootstrap/README.md`, `infra/envs/prod/README.md`.

  Note the commit subject prefix is `docs(…)` not `fix(…)` since this is a pure doc change (matches project convention — see existing commits like `docs(04-06): flip Phase 4 to COMPLETE in STATE + ROADMAP + prod README`).
  </action>
  <read_first>
    - .planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-CONTEXT.md (D-01, D-03)
  </read_first>
  <acceptance_criteria>
    - `git log -1 --pretty=format:%s` matches `^docs\(04\.1-04\):`
    - `git log -1 --stat` lists exactly: `infra/bootstrap/README.md`, `infra/envs/prod/README.md`
    - `git status` shows no staged or unstaged changes in either README
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -q "^## Step 5" infra/bootstrap/README.md && grep -q "az ad user create --user-principal-name" infra/bootstrap/README.md` — new section landed
2. `grep -q "canonical customer/Member bootstrap step is now documented in.*infra/bootstrap/README.md" infra/envs/prod/README.md` — cross-reference landed
3. `grep -q "Phase 04.1 fix 4" infra/envs/prod/README.md` — phase reference present
4. `git log -1 --pretty=format:%s` matches `^docs\(04\.1-04\):`
5. `git log -1 --stat` lists exactly the 2 README files
</verification>
