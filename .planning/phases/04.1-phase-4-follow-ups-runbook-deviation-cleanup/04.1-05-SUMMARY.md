---
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
plan: 05
subsystem: infra
tags: [terraform, azuread, entra-external, identifier-uri, refactor, runbook]

requires:
  - phase: 04-frontend-shell-auth
    provides: "04-06-SUMMARY.md deviation #7 (azuread_application_identifier_uri silently leaves identifierUris empty in Azure) + 04-06-RUNBOOK.md row 2 (manual `az ad app update --identifier-uris …` workaround) — the canonical Phase 4 artefacts the refactor structurally fixes."
  - phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
    provides: "Plans 04.1-01..04.1-04 landed serially per D-04 chain (init_db rebridge, deploy-api smoke widen, main.tsx try/catch, CIAM bootstrap docs). Plan 05 was unblocked the moment 04 committed (`0b6f0ea` + `e392934` metadata)."
provides:
  - "infra/external/main.tf — dedicated `azuread_application_identifier_uri.api` resource block removed; `identifier_uris = [\"api://${azuread_application.api.client_id}\"]` set inline on `azuread_application.api` with an inline rationale comment naming SUMMARY deviation #7 + the memory file."
  - "infra/external/outputs.tf — `api_audience_uri` + `api_scope_name` outputs now read from `one(azuread_application.api.identifier_uris)` instead of the deleted `azuread_application_identifier_uri.api.identifier_uri`. Single-element invariant enforced via Terraform's `one()` function so future drift (>1 URI) fails loud rather than silent."
  - "infra/envs/prod/README.md — new operator paragraph appended to 'Phase 4 close (2026-05-21)' section explaining that the next `infra/external/` apply will plan (a) a destroy of `azuread_application_identifier_uri.api` + (b) an update-in-place of `azuread_application.api` adding `identifier_uris`. Both must be approved."
  - "Memory `~/.claude/projects/.../memory/terraform-azuread-identifier-uri-unreliable.md` — dated 2026-05-21 addendum names Phase 04.1 plan 05 as the structural fix and proposes the next escalation (a post-apply `az ad app show … --query identifierUris -o tsv` assertion in `scripts/refresh-external-outputs.sh`) if the inline attribute ALSO exhibits silent-fail behaviour on the next apply."
affects:
  - "Out-of-band next `infra/external/` apply (Adrian) — Terraform will plan a destroy + update pair; operator must approve both."
  - "`scripts/refresh-external-outputs.sh` — still reads `terraform output -raw api_audience_uri` + `api_scope_name`; outputs.tf changes preserve identical string values, so the script keeps working unchanged."
  - "Future fresh-tenant bootstraps — no more 1h diagnosis of AADSTS500011 caused by the silently-empty identifierUris list."
  - "Phase 04.1 phase-level close — this was the final plan (5/5); STATE.md flips to phase complete and ROADMAP.md progress hits 5/5."

tech-stack:
  added: []
  patterns:
    - "Inline `identifier_uris` (Set of String) on `azuread_application` over the dedicated `azuread_application_identifier_uri` resource — reliable form per azuread provider ~> 3.x. Lesson recurring across the project: when a provider exposes both an inline attribute and a sibling 'side resource', the inline attribute survives provider quirks better because it's part of the resource's primary apply transaction."
    - "`one(<set>)` for single-element-Set unwrapping in outputs.tf — fails loud on the empty case (0 elements) and on accidental duplication (>1 element), both of which would silently degrade downstream consumers that expect a single string. Reusable for any future Set-typed Terraform attribute exposed via output."
    - "Carry-forward of the two-file canonical-docs pattern from 04.1-04 — the operator note on the next-apply plan lives in `infra/envs/prod/README.md` (where operators triaging Phase 4 issues land); the deep deviation context stays in `04-06-SUMMARY.md` deviation #7 + the memory file. Single-paragraph cross-reference avoids drift."
    - "Out-of-band state-reconciliation pattern — when a Terraform refactor moves an attribute between resource types, the in-phase commit ships ONLY the code change; the live state reconciliation (destroy + update-in-place) is intentionally deferred to the next operator apply session and documented as an operator note. Lets the refactor commit stay reviewable and pushable without touching cloud."

key-files:
  created: []
  modified:
    - "infra/external/main.tf — dedicated resource block (original lines 41-47 + 2 comment lines) deleted; new inline `identifier_uris = [\"api://${azuread_application.api.client_id}\"]` attribute added inside `azuread_application.api` between `sign_in_audience` and the `api { … }` nested block, preceded by a 6-line rationale comment naming SUMMARY deviation #7 + the memory file. Net change: 95 lines (was 96). `terraform fmt` no-op (already canonical)."
    - "infra/external/outputs.tf — `api_audience_uri.value` (line 13) and `api_scope_name.value` (line 18) now read from `one(azuread_application.api.identifier_uris)` instead of `azuread_application_identifier_uri.api.identifier_uri`. The 4-line rationale comment on `api_audience_uri` explains the dual fix-and-invariant intent of `one()`."
    - "infra/envs/prod/README.md — appended after line 227 (the ACA deploy-api smoke note); new paragraph explains the next-apply plan (destroy + update-in-place), names Phase 04.1 fix 5 as the source, and reminds operators that the manual `az ad app update --identifier-uris` workaround in row 2 of the unblock table is still needed for fresh re-bootstraps until the apply runs. Section header (`## Phase 4 close (2026-05-21)`) and the unblock-fixes table both unchanged."

key-decisions:
  - "Use `one(azuread_application.api.identifier_uris)` (not `tolist(...)[0]` or `[for u in ... : u][0]`) to unwrap the Set in outputs.tf. `one()` is the documented Terraform form for the single-element-Set invariant — it fails loud if the Set ends up empty (0 elements) OR has accidental duplicates (>1 element), exactly the silent-failure mode the refactor is meant to prevent. `tolist(...)[0]` would silently return the wrong (or no) value in the empty/multi case."
  - "Rule 1 deviation — include `infra/external/outputs.tf` in the atomic commit even though the plan specified only `infra/external/main.tf` + `infra/envs/prod/README.md`. Without the outputs.tf edit, `terraform validate` exits 1 with 'Reference to undeclared resource' on `azuread_application_identifier_uri.api`, which violates the plan's must-have-truth gate. The 3-file commit is the smallest change that satisfies both Task 01's grep gates AND Task 02's `terraform validate` gate. Plan author missed the cross-reference."
  - "Use `refactor(...)` commit prefix (not `fix(...)`) — pure structural change, same final identifier_uri string value, no behaviour change. Matches the project's commit-convention spirit (refactor = structure change without behaviour change). Plan task 05 specified this prefix explicitly."
  - "Keep `# Application ID URI must be set AFTER application exists (chicken-and-egg avoidance — client_id only exists post-create). Per RESEARCH line 1950-1952.` block REMOVED entirely (not retained as historical context). The new inline-comment block already cross-references SUMMARY deviation #7 + the memory file, which carry the original-resource history forward; duplicating it in main.tf would add noise without value."

patterns-established:
  - "**Inline-attribute-preferred over side-resource pattern** — when the azuread (or any) provider exposes both `resource \"foo_attr\" \"x\"` AND an inline attribute on the parent resource for the same setting, prefer the inline attribute. Provider 'side resources' have a known failure mode where state reports created but the live attribute stays empty; inline attributes are part of the parent's primary apply transaction and have no separate code path to fail silently. Recorded as a project-wide convention going forward."
  - "**`one()` for single-element-Set unwrap in outputs** — any future output that needs to expose a single string from a Set-typed Terraform attribute should use `one(<set>)` rather than indexing. Fails loud on the invariant violation; matches Terraform's stated idiom for the case."
  - "**Out-of-band state reconciliation note pattern** — when a Terraform refactor changes attribute-resource shapes and the apply is deferred, the operator-facing README gains a single paragraph listing the exact plan-and-approve actions ('destroy X', 'update-in-place Y') the next apply will surface. Operators learn the expected diff before running plan; surprises avoided."
  - "**Cross-reference comment in the file under refactor** — the new inline `identifier_uris` attribute carries a 6-line comment naming SUMMARY deviation #7 + the memory file. Future contributors grepping for `identifier_uri` in main.tf land on the rationale immediately. Reusable pattern: any structural refactor driven by a documented deviation should leave a comment trail pointing back."

requirements-completed: []  # Phase 04.1 follow-ups carry no AUTH-*/SHEL-* requirements per D-06 (durable-tax cleanup, not new requirements).

duration: ~3min
completed: 2026-05-21
---

# Phase 04.1 Plan 05: identifier_uri Inline Refactor Summary

**`infra/external/main.tf` now sets `identifier_uris` inline on `azuread_application.api` (the dedicated `azuread_application_identifier_uri.api` resource is gone); `infra/external/outputs.tf` reads from `one(...identifier_uris)` for the single-element-Set invariant; `infra/envs/prod/README.md` documents the next-apply destroy+update-in-place plan operators must approve. Final plan of Phase 04.1 — phase complete (5/5).**

## Performance

- **Duration:** ~3 min (file edits + 3-gate Terraform verification + atomic commit)
- **Started:** 2026-05-21T21:54:00Z
- **Completed:** 2026-05-21T21:57:00Z
- **Tasks:** 5 (executed in order: main.tf edit → terraform init/validate/fmt verification → prod README append → memory addendum → atomic commit)
- **Files modified:** 3 (one atomic commit `7e0718c`)

## Accomplishments

- `infra/external/main.tf`: dedicated `resource "azuread_application_identifier_uri" "api"` block removed; new inline `identifier_uris = ["api://${azuread_application.api.client_id}"]` attribute on `azuread_application.api`, preceded by a 6-line rationale comment naming SUMMARY deviation #7 + the memory file. `terraform fmt` reports no-op (already canonical). `terraform validate` exit 0.
- `infra/external/outputs.tf`: `api_audience_uri.value` + `api_scope_name.value` rewired from the deleted resource to `one(azuread_application.api.identifier_uris)`. Downstream consumers (`scripts/refresh-external-outputs.sh`, `infra/envs/prod/prod.tfvars.local` `backend_audience`, `frontend/.env.production` `VITE_API_AUDIENCE`) receive identical string values.
- `infra/envs/prod/README.md`: new operator paragraph appended to the 'Phase 4 close (2026-05-21)' section (after the ACA smoke check line) explaining the next-apply plan (destroy of `azuread_application_identifier_uri.api` + update-in-place of `azuread_application.api`), naming Phase 04.1 fix 5 as the source, and reminding operators that the manual `az ad app update --identifier-uris` workaround in the unblock table is still required for fresh re-bootstraps until the apply runs.
- `~/.claude/projects/-Users-adrian-Developer-job-rag/memory/terraform-azuread-identifier-uri-unreliable.md`: dated 2026-05-21 addendum names Phase 04.1 plan 05 as the structural fix; flags the next escalation if the inline attribute also exhibits silent-fail behaviour (`az ad app show … --query identifierUris -o tsv` assertion inside `scripts/refresh-external-outputs.sh`).
- Atomic commit `7e0718c` lists exactly 3 files; no `.terraform/` or `terraform.tfstate*` leaked into the commit.

## Task Commits

The whole plan ships as ONE atomic commit per CONTEXT.md D-03:

1. **Task 01: Move `identifier_uris` inline on `azuread_application.api` in main.tf** — staged into `7e0718c`
2. **Task 02: `terraform init -backend=false` + `terraform validate` + `terraform fmt -check`** — verification only (no files staged)
3. **Task 03: Append operator note to prod README** — staged into `7e0718c`
4. **Task 04: Append memory addendum** — memory store, not git-tracked
5. **Task 05: Atomic commit via `gsd-tools commit`** — `7e0718c` (refactor)

**Plan metadata:** _(this SUMMARY.md + STATE/ROADMAP update is the final docs commit, separate from `7e0718c`)_

## Files Created/Modified

- `infra/external/main.tf` — MODIFIED. -8 lines (deleted resource + 2 comment lines), +7 lines (new comment block + inline attribute). Net 95 lines (was 96). Committed in `7e0718c`.
- `infra/external/outputs.tf` — MODIFIED (Rule 1 deviation, see below). 2 output value expressions rewired from the deleted resource to `one(azuread_application.api.identifier_uris)`. +4 lines (rationale comment) on `api_audience_uri`. Net 23 lines (was 19). Committed in `7e0718c`.
- `infra/envs/prod/README.md` — MODIFIED. +2 lines (1 blank + 1 paragraph) appended at the end of the Phase 4 close section. Net 229 lines (was 227). Committed in `7e0718c`.

## Decisions Made

- **`one()` over `tolist()[0]` for Set unwrap** in outputs.tf. `one()` is the documented Terraform form for the single-element-Set invariant and fails loud on empty (0 elements) or duplicate (>1 elements) — exactly the silent-fail mode the refactor exists to prevent. `tolist(...)[0]` would silently degrade downstream consumers in the empty/multi case.
- **3-file commit (vs 2-file as plan specified)** — `infra/external/outputs.tf` had to be included to satisfy the `terraform validate` exit-0 gate (must-have truth). Per Rule 1 (auto-fix bugs caused by current task's changes); see Deviations section below.
- **`refactor(...)` commit prefix** (not `fix(...)`) — pure structural change, same final string value, no behaviour change. Plan task 05 specified this prefix explicitly; project convention reserves `fix(...)` for bug fixes with behaviour delta.
- **Original chicken-and-egg comment fully removed** (not retained as historical context) — the new inline-comment block already cross-references SUMMARY deviation #7 + the memory file, which carry the original-resource history forward.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added `infra/external/outputs.tf` to the atomic commit**
- **Found during:** Task 02 (`terraform validate`)
- **Issue:** `outputs.tf` lines 13 + 18 reference `azuread_application_identifier_uri.api.identifier_uri` — the resource that Task 01 deletes. After Task 01, `terraform validate` exits 1 with `Error: Reference to undeclared resource. A managed resource "azuread_application_identifier_uri" "api" has not been declared in the root module.` Plan author missed the cross-reference; without the outputs.tf fix, the plan's `terraform validate exit 0` must-have-truth gate would fail.
- **Fix:** Rewrote both output value expressions to read from `one(azuread_application.api.identifier_uris)`. `one()` enforces the single-element-Set invariant (fails loud on empty or duplicate Set states). Added a 4-line rationale comment on `api_audience_uri` explaining both the source change AND the invariant intent. Downstream consumers (refresh script, prod tfvars, frontend `.env.production`) receive identical string values.
- **Files modified:** `infra/external/outputs.tf` (2 expressions rewritten + 4-line comment).
- **Verification:** `cd infra/external && terraform validate` → `Success! The configuration is valid.` exit 0. `terraform fmt -check` exit 0. Output description text unchanged so consumer documentation stays accurate.
- **Committed in:** `7e0718c` (same atomic commit as the main.tf + prod README edits).

---

**Total deviations:** 1 auto-fixed (1 bug — broken reference in `outputs.tf` after main.tf resource deletion).
**Impact on plan:** The outputs.tf fix is strictly necessary for correctness — without it, the plan's `terraform validate` gate fails and the refactor leaves a broken `infra/external/` directory. Adding 1 file to the commit (3 instead of 2) is the smallest change that satisfies all gates. No scope creep — the fix is mechanically derivable from the main.tf edit. Documented for the verifier so the 3-file commit isn't flagged as out-of-spec.

## Issues Encountered

- **Plan-level `<verification>` Gate 1 grep false-negative.** The verification block uses `! grep -q "azuread_application_identifier_uri" infra/external/main.tf` which also matches the new 6-line rationale comment block (which legitimately names the old resource by string to explain why it was removed). The stricter Task 01 acceptance criterion (`! grep -q "resource \"azuread_application_identifier_uri\""` — with the `resource "..."` prefix) DID pass cleanly and is the semantically correct check. Verifier should treat the Task 01 grep as authoritative; the `<verification>` Gate 1 check is a slightly loose paraphrase. No code change needed.

## User Setup Required

None — the in-phase work is pure code/docs. The next `infra/external/` apply IS user-facing work but is intentionally deferred per D-25 (no `terraform apply` in this phase). When Adrian runs the next apply, Terraform will plan:
1. **Destroy** of `azuread_application_identifier_uri.api` (orphaned in state file by this refactor).
2. **Update-in-place** of `azuread_application.api` (`+ identifier_uris = ["api://<api_client_id>"]`).

Both should be approved. The `infra/envs/prod/README.md` operator note appended in this plan documents this exact plan shape for the apply session.

## Next Phase Readiness

- **Plan 05 of Phase 04.1 complete.** Final plan of the phase.
- **Phase 04.1 complete (5/5 plans).** All five Phase 4 follow-ups landed serially per D-04:
  - 04.1-01: init_db rebridge (commit `b81a53e`)
  - 04.1-02: deploy-api smoke widen (commit `7fd0f61`)
  - 04.1-03: main.tsx try/catch + BootErrorFallback (commit `ee4a1e5`)
  - 04.1-04: CIAM customer-bootstrap docs (commit `0b6f0ea`)
  - 04.1-05: identifier_uri inline refactor (commit `7e0718c`) ← THIS PLAN
- **Pending on Adrian (out-of-band):**
  - `git push origin master` to land all 5 fixes (D-01 forbids push from this phase).
  - Next `cd infra/external && terraform apply` to reconcile state (destroy + update-in-place — both expected, both should be approved).
  - Next API deploy will exercise the Plan 04.1-01 init_db rebridge live (no manual `az containerapp exec` needed for SEEDED_USER_ENTRA_OID rotation).
  - Next API deploy will exercise the Plan 04.1-02 deploy-api smoke widen live (no more cosmetic CI red on `RunningAtMaxScale`).
- **Next phase:** Phase 5 (Dashboard) — depends only on Phase 4 (now closed AND its durable tax cleared). Phases 5/6/7 are parallel-eligible.

## Verification Trail

| Gate | Command | Result |
|------|---------|--------|
| T01 AC1: dedicated `resource "azuread_application_identifier_uri"` removed | `! grep -q 'resource "azuread_application_identifier_uri"' infra/external/main.tf` | PASS |
| T01 AC2: old chicken-and-egg comment removed | `! grep -q "Application ID URI must be set AFTER application exists" infra/external/main.tf` | PASS |
| T01 AC3: inline `identifier_uris` attribute present | `grep -q 'identifier_uris = \["api://\${azuread_application.api.client_id}"\]' infra/external/main.tf` | PASS |
| T01 AC4: rationale comment names Phase 04.1 fix 5 | `grep -q "Phase 04.1 fix 5" infra/external/main.tf` | PASS |
| T01 AC5: cross-reference to deviation #7 present | `grep -q 'deviation #7\|deviation 7' infra/external/main.tf` | PASS |
| T01 AC6: api app reg still defined (count=1) | `grep -c 'resource "azuread_application" "api"' infra/external/main.tf` = 1 | PASS |
| T01 AC7: api SP unchanged (count=1) | `grep -c 'resource "azuread_service_principal" "api"' infra/external/main.tf` = 1 | PASS |
| T01 AC8: SPA app reg unchanged (count=1) | `grep -c 'resource "azuread_application" "spa"' infra/external/main.tf` = 1 | PASS |
| T01 AC9: SPA SP unchanged (count=1) | `grep -c 'resource "azuread_service_principal" "spa"' infra/external/main.tf` = 1 | PASS |
| T01 AC10: delegated permission grant unchanged (count=1) | `grep -c 'resource "azuread_service_principal_delegated_permission_grant"' infra/external/main.tf` = 1 | PASS |
| T02 AC1: `terraform init -backend=false` exit 0 | `cd infra/external && terraform init -backend=false` | PASS (azuread v3.8.0 + random v3.9.0 reused from lock file) |
| T02 AC2: `terraform validate` exit 0 | `cd infra/external && terraform validate` | PASS (`Success! The configuration is valid.`) |
| T02 AC3: `terraform fmt -check` exit 0 | `cd infra/external && terraform fmt -check` | PASS |
| T02 AC4: `.terraform/` gitignored | `git check-ignore -v infra/external/.terraform/` | PASS (matches `infra/external/.gitignore:4:.terraform/`) |
| T03 AC1: TF unreliability paragraph present | `grep -q "azuread_application_identifier_uri.* Terraform unreliability" infra/envs/prod/README.md` | PASS |
| T03 AC2: Phase 04.1 fix 5 reference | `grep -q "Phase 04.1 fix 5" infra/envs/prod/README.md` | PASS |
| T03 AC3: destroy mention | `grep -q "destroy.*azuread_application_identifier_uri" infra/envs/prod/README.md` | PASS |
| T03 AC4: identifier_uris attribute mention | `grep -q "identifier_uris" infra/envs/prod/README.md` | PASS |
| T03 AC5: update-in-place mention | `grep -q "update-in-place" infra/envs/prod/README.md` | PASS |
| T03 AC6: existing ACA smoke line preserved | `grep -q "ACA deploy-api.yml smoke check" infra/envs/prod/README.md` | PASS |
| T03 AC7: section header preserved | `grep -q "## Phase 4 close (2026-05-21)" infra/envs/prod/README.md` | PASS |
| T04 AC1: memory addendum names plan 05 | `grep -q "Phase 04.1 plan 05\|04.1-05" ~/.claude/projects/.../memory/terraform-azuread-identifier-uri-unreliable.md` | PASS |
| T04 AC2: memory addendum names identifier_uris | `grep -q "identifier_uris" ~/.claude/projects/.../memory/terraform-azuread-identifier-uri-unreliable.md` | PASS |
| T04 AC3: memory addendum names "inline" | `grep -q "inline" ~/.claude/projects/.../memory/terraform-azuread-identifier-uri-unreliable.md` | PASS |
| T05 AC1: commit subject matches `^refactor\(04\.1-05\):` | `git log -1 --pretty=format:%s` | PASS (`refactor(04.1-05): move identifier_uri inline on azuread_application`) |
| T05 AC2: commit lists exactly the 3 expected files (Rule 1 deviation accepted) | `git log -1 --stat 7e0718c` | PASS (infra/envs/prod/README.md, infra/external/main.tf, infra/external/outputs.tf) |
| T05 AC3: no `.terraform/` or `tfstate` in commit | `git log -1 --name-only 7e0718c \| grep -E '\\.terraform\|tfstate'` | PASS (no matches) |
| T05 AC4: working tree clean post-commit (excl. pre-existing `.claude/`) | `git status --short` shows only `?? .claude/` | PASS |

## Self-Check: PASSED

- `.planning/phases/04.1-phase-4-follow-ups-runbook-deviation-cleanup/04.1-05-SUMMARY.md` — FOUND (this file)
- `infra/external/main.tf` — FOUND (MODIFIED — inline `identifier_uris` attribute on `azuread_application.api`, dedicated resource gone)
- `infra/external/outputs.tf` — FOUND (MODIFIED — `one(azuread_application.api.identifier_uris)` in 2 output value expressions)
- `infra/envs/prod/README.md` — FOUND (MODIFIED — operator note paragraph appended to Phase 4 close section)
- Commit `7e0718c` — FOUND (`git log` shows the matching subject + 3-file stat)

---
*Phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup*
*Completed: 2026-05-21*
