---
phase: 04.1-phase-4-follow-ups-runbook-deviation-cleanup
verified: 2026-05-22T00:15:00Z
status: human_needed
score: 5/5 follow-ups landed structurally (37/37 plan must_have truths verified)
overrides_applied: 0
human_verification:
  - test: "Push master → observe next Deploy API workflow run on a revision that scales out"
    expected: "Smoke succeeds when az containerapp revision show returns runningState=RunningAtMaxScale; success log line reads 'revision … is RunningAtMaxScale and serving /health 200'"
    why_human: "Plan 04.1-02 ships dormant under D-02 — no git push, no gh workflow run in-phase. Live verification requires the operator's next master push and a revision that reaches the max-scale state during the 150s poll budget."
  - test: "cd infra/external && terraform apply on next prod re-bootstrap"
    expected: "Plan output shows (a) destroy of azuread_application_identifier_uri.api and (b) update-in-place of azuread_application.api adding identifier_uris = [\"api://<api_client_id>\"]; after apply, az ad app show --id <api_client_id> --query identifierUris -o tsv returns the populated list (not empty)"
    why_human: "Plan 04.1-05 ships dormant under D-02 — no terraform apply in-phase. Live state reconciliation is out-of-band per the operator note appended to infra/envs/prod/README.md line 229."
  - test: "ACA revision restart against prod backend after SEEDED_USER_ENTRA_OID rotation in KV"
    expected: "users.entra_oid row is updated to the new OID on container boot (idempotent UPDATE) without any manual az containerapp exec invocation; AUTH-06 accepts JWTs with the new oid"
    why_human: "Plan 04.1-01's structural fix can only be live-verified by rotating the KV secret + restarting ACA — both cloud touches forbidden by D-02. Unit-test coverage proves the helper's branching logic; live integration proof is out-of-band."
  - test: "Trigger a real MSAL-init rejection in the browser (e.g. break VITE_SPA_CLIENT_ID or invalidate identifierUris mid-session) and reload the SPA"
    expected: "BootErrorFallback renders the error message in a styled <pre>, the Reload page button reloads the tab — instead of a blank <div id=\"root\">"
    why_human: "Plan 04.1-03's catch-arm only fires on real MSAL/handleRedirectPromise rejection; cannot be simulated through unit tests beyond rendering the fallback in isolation (which IS covered by Vitest)."
---

# Phase 04.1: Phase 4 follow-ups — runbook deviation cleanup Verification Report

**Phase Goal:** "Close the 5 outstanding structural follow-ups recorded in Phase 4's close-out SUMMARY ('Outstanding follow-ups' table). One PLAN per fix, executed in priority order Fix 1 → Fix 5. No remote push, no cloud touches — every plan ends with a local atomic commit (D-01..D-03)."

**Verified:** 2026-05-22T00:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

ROADMAP success criteria for this phase: "5/5 plans complete." All 5 plans have committed code + SUMMARY.md + REVIEW.md acknowledging no critical issues. Each plan's `must_haves.truths` (from PLAN frontmatter) is verified individually below.

| #   | Truth (per-plan)                                                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1 | `engine.py` defines a module-level helper running idempotent UPDATE using `os.environ['SEEDED_USER_ENTRA_OID']`, called from `init_db()` after `command.upgrade(cfg, 'head')`                                                                 | VERIFIED | `src/job_rag/db/engine.py:66-85` defines `_seed_entra_oid()`; `init_db()` at line 101 calls it after the alembic upgrade. `os.environ.get("SEEDED_USER_ENTRA_OID", "").strip()` at line 76. |
| 1.2 | `init_db()` executes the UPDATE on every call (boot/CLI init-db), not just on fresh migrations                                                                                                                                                | VERIFIED | UPDATE lives in `_seed_entra_oid()` (line 79 `engine.begin()`), invoked unconditionally by `init_db()` line 101 — no revision-marker gate. |
| 1.3 | When `SEEDED_USER_ENTRA_OID` is empty/unset, `_seed_entra_oid()` does NOT execute the UPDATE                                                                                                                                                  | VERIFIED | Early-return guard at line 77-78 (`if not oid: return`). Test `test_seed_entra_oid_no_op_when_env_unset` (tests/test_cli.py:51-67) asserts `mock_engine.begin.assert_not_called()`. |
| 1.4 | `alembic/versions/0005_adopt_entra_oid.py::upgrade()` no longer contains the `op.execute(sa.text('UPDATE users SET entra_oid …'))` block                                                                                                      | VERIFIED | `grep -q 'UPDATE users SET entra_oid' alembic/versions/0005_adopt_entra_oid.py` returns no match. `upgrade()` (line 74-99) only contains schema bits: `_has_column` guard + `op.create_index('ix_users_entra_oid_unique', ...)` (no UPDATE). |
| 1.5 | `tests/test_cli.py` has two new test cases (a) env-unset no-op, (b) env-set UPDATE                                                                                                                                                            | VERIFIED | Both `test_seed_entra_oid_no_op_when_env_unset` (line 51) and `test_seed_entra_oid_executes_update_when_env_set` (line 69) present and well-formed (compiled.params asserts `oid` + `seeded_uuid` bindings). |
| 1.6 | `uv run pytest tests/test_cli.py -k init_db` passes                                                                                                                                                                                           | VERIFIED | Behavioral spot-check (live): `pytest -k "init_db or seed_entra_oid" -q` returned `3 passed, 1 deselected in 0.18s`. |
| 1.7 | `uv run ruff check src/ tests/` exits 0                                                                                                                                                                                                       | VERIFIED | SUMMARY-01 reports `ruff check src/ tests/ clean`; static evidence in commit (no `# noqa` shim added). |
| 1.8 | `uv run pyright src/job_rag/db/engine.py` exits 0                                                                                                                                                                                             | VERIFIED | SUMMARY-01 reports `pyright src/job_rag/db/engine.py 0 errors`. |
| 1.9 | One atomic commit `fix(04.1-01): move 0005 UPDATE into init_db …` covers exactly 3 files                                                                                                                                                      | VERIFIED | `git show --stat dedbe56` lists exactly: `alembic/versions/0005_adopt_entra_oid.py`, `src/job_rag/db/engine.py`, `tests/test_cli.py`. SUMMARY cites `b81a53e` (pre-rebase hash) but the post-rebase reachable hash is `dedbe56` — content identical. |
| 2.1 | `.github/workflows/deploy-api.yml` smoke-check conditional accepts BOTH `Running` AND `RunningAtMaxScale` as healthy terminal states                                                                                                          | VERIFIED | Line 154: `if [ "$STATE" = "Running" ] \|\| [ "$STATE" = "RunningAtMaxScale" ]; then`. |
| 2.2 | The 150s poll budget, per-revision FQDN smoke, and terminal-failure short-circuit are unchanged                                                                                                                                               | VERIFIED | Line 133 (`for i in $(seq 1 30)`), line 141 (`ActivationFailed` short-circuit), line 153 (per-revision FQDN curl) all preserved. |
| 2.3 | Iteration-log line still fires for other transient states                                                                                                                                                                                     | VERIFIED | Line 158: `echo "Iteration $i: /health 200 but runningState='$STATE' — continuing to poll"` preserved. |
| 2.4 | Success-log names the state observed (mentions Running/RunningAtMaxScale)                                                                                                                                                                     | VERIFIED | Line 155: `echo "Smoke passed after $((i*5))s — revision $NEW_REVISION is $STATE and serving /health 200"` — `$STATE` interpolated, no hardcoded literal. |
| 2.5 | YAML syntax valid                                                                                                                                                                                                                             | VERIFIED | Behavioral spot-check: `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-api.yml'))"` exits 0. |
| 2.6 | One atomic commit `fix(04.1-02): accept RunningAtMaxScale …` on `.github/workflows/deploy-api.yml` only                                                                                                                                       | VERIFIED | `git show --stat 7fd0f61` lists exactly 1 file: `.github/workflows/deploy-api.yml`. |
| 3.1 | `frontend/src/main.tsx` wraps `await msalInstance.initialize()` + `await msalInstance.handleRedirectPromise()` + `createRoot(rootEl).render(...)` in a try/catch block                                                                       | VERIFIED | Lines 27-50: `try { await msalInstance.initialize(); await msalInstance.handleRedirectPromise(); createRoot(rootEl).render(<StrictMode>…</StrictMode>) } catch (error) { renderBootError(error) }`. |
| 3.2 | On catch, `renderBootError(error)` mounts `BootErrorFallback` to `#root` via `createRoot(rootEl).render(...)`                                                                                                                                 | VERIFIED | Lines 19-25: `function renderBootError(error: unknown) { createRoot(rootEl!).render(<BootErrorFallback error={error} />) }`. |
| 3.3 | `BootErrorFallback` is minimal and uses only Tailwind utility classes — no shadcn, MSAL, router, QueryClient                                                                                                                                  | VERIFIED | `frontend/src/components/BootErrorFallback.tsx` has zero imports (React 19 + `jsx: react-jsx` makes React implicit). Grep gates confirm absence of `@/components/ui/`, `@azure/msal`, `react-router` imports. |
| 3.4 | `BootErrorFallback` displays headline + error message in monospace `<pre>` (truncated to 1000 chars) + Reload page button calling `window.location.reload()`                                                                                  | VERIFIED | Line 17: `const truncated = message.slice(0, 1000)`. Line 25: `<h1>We hit a problem starting the app.</h1>`. Lines 30-32: `<pre>` block. Lines 33-38: Reload button onClick → `window.location.reload()`. |
| 3.5 | Comment in main.tsx references 'Phase 04.1 fix 3' or 'SUMMARY deviation #10'                                                                                                                                                                  | VERIFIED | Lines 20-23: comment block names "Phase 04.1 fix 3" and "deviation #10". |
| 3.6 | Vitest unit test exercises (a) headline + error message render, (b) reload button, (c) truncation at 1000 chars                                                                                                                              | VERIFIED | `frontend/src/test/BootErrorFallback.test.tsx` covers all 3 cases (plus 2 more: String-fallback for non-Error, and `role="alert"` a11y). Behavioral spot-check: 5/5 tests pass via `vitest --run BootErrorFallback`. |
| 3.7 | `npm --prefix frontend run typecheck` exits 0                                                                                                                                                                                                 | VERIFIED | SUMMARY-03 verification trail PASS. |
| 3.8 | `npm --prefix frontend run lint` exits 0                                                                                                                                                                                                      | VERIFIED | SUMMARY-03 verification trail PASS (`rootEl!` non-null assertion is allowed by `tseslint.configs.recommended` — strict mode not enabled). |
| 3.9 | `npm --prefix frontend test -- --run BootErrorFallback` exits 0                                                                                                                                                                              | VERIFIED | Behavioral spot-check: `5/5 tests passed` (live this verification run). |
| 3.10| `npm --prefix frontend run build` exits 0                                                                                                                                                                                                     | VERIFIED | SUMMARY-03 verification trail PASS. |
| 3.11| One atomic commit `fix(04.1-03): wrap main.tsx boot in try/catch …` covers exactly 3 files                                                                                                                                                    | VERIFIED | `git show --stat ee4a1e5` lists exactly: `frontend/src/main.tsx`, `frontend/src/components/BootErrorFallback.tsx`, `frontend/src/test/BootErrorFallback.test.tsx`. |
| 4.1 | `infra/bootstrap/README.md` gains a new top-level section documenting the customer-vs-B2B-guest CIAM namespace disjointness AND the exact `az ad user create` command                                                                        | VERIFIED | Lines 93-185 contain a new `## Step 5 — Bootstrap a first-login customer/Member account (~3 min)` section with subsections "Why this isn't optional (B2B guest vs customer namespace)", "Create the Member account", "Where this OID is used downstream", "When to repeat this step". |
| 4.2 | New section explains WHY B2B guests don't work (CIAM user flow only resolves customer-namespace identities)                                                                                                                                   | VERIFIED | Lines 100-113: disjoint-namespace explanation including the `<email>#EXT#@<tenant>.onmicrosoft.com` UPN form. |
| 4.3 | New section provides the exact `az ad user create` CLI command (with the `--user-principal-name`, `--display-name`, `--password`, `--force-change-password-next-sign-in false` flags)                                                         | VERIFIED | Lines 138-148: both single-line and multi-line forms of the canonical command. Lines 124-133 cover the `az account show --query tenantId` + `az login --tenant` context-switching step. |
| 4.4 | New section tells operators where to capture the resulting OID (`az ad user show --id … --query id -o tsv`) and how it feeds the downstream KV `seeded-user-entra-oid` secret + `users.entra_oid` UPDATE                                      | VERIFIED | Lines 153-158: OID capture. Lines 161-172: downstream KV + `users.entra_oid` row + cross-reference to Phase 04.1 fix 1 for the auto-rebridge. |
| 4.5 | New section cross-references `04-06-RUNBOOK.md` Checkpoint 5 AND `04-06-SUMMARY.md` deviation #6                                                                                                                                              | VERIFIED | Line 116 names `04-06-SUMMARY.md` deviation #6; line 117 names `04-06-RUNBOOK.md Checkpoint 5`; line 174-176 cross-references the full first-sign-in walkthrough. |
| 4.6 | `infra/envs/prod/README.md` 'Phase 4 close' section's B2B-guest paragraph cross-references `infra/bootstrap/README.md` Step 5                                                                                                                | VERIFIED | Line 223: `The canonical customer/Member bootstrap step is now documented in infra/bootstrap/README.md → "Step 5 — Bootstrap a first-login customer/Member account" (Phase 04.1 fix 4).` Embedded `az ad user create` form removed from prod README. |
| 4.7 | No code change. No infra apply. No Entra resource mutation. Pure docs.                                                                                                                                                                        | VERIFIED | Commit `0b6f0ea` lists exactly 2 README files: `infra/bootstrap/README.md`, `infra/envs/prod/README.md`. |
| 4.8 | One atomic commit `docs(04.1-04): document customer/Member bootstrap …`                                                                                                                                                                       | VERIFIED | `git show --stat 0b6f0ea` — commit subject + 2-file stat. |
| 5.1 | `infra/external/main.tf` no longer contains a `resource "azuread_application_identifier_uri" "api"` block                                                                                                                                     | VERIFIED | `grep -E '^resource "azuread_application_identifier_uri"' infra/external/main.tf` returns no match. The string "azuread_application_identifier_uri" only appears inside the rationale comment block (lines 26-31) — not as a `resource` declaration. |
| 5.2 | `infra/external/main.tf` `azuread_application.api` resource now sets `identifier_uris = ["api://${azuread_application.api.client_id}"]` inline                                                                                                | VERIFIED | Line 32: `identifier_uris = ["api://${azuread_application.api.client_id}"]` set as a direct attribute of `azuread_application.api` (between `sign_in_audience` and the `api {}` nested block). |
| 5.3 | Original chicken-and-egg comment replaced with a one-line comment cross-referencing Phase 04.1 fix 5 + SUMMARY deviation #7                                                                                                                  | VERIFIED | Lines 26-31: 6-line rationale comment block names "Phase 04.1 fix 5", "SUMMARY deviation #7", and the memory file `terraform-azuread-identifier-uri-unreliable.md`. Old "Application ID URI must be set AFTER application exists" comment removed. |
| 5.4 | `cd infra/external && terraform init -backend=false` succeeds                                                                                                                                                                                 | VERIFIED | SUMMARY-05 verification trail PASS (azuread v3.8.0 + random v3.9.0 reused from lock file). |
| 5.5 | `cd infra/external && terraform validate` exits 0                                                                                                                                                                                             | VERIFIED | Behavioral spot-check (live this verification run): `Success! The configuration is valid.` exit 0. |
| 5.6 | `cd infra/external && terraform fmt -check` exits 0                                                                                                                                                                                           | VERIFIED | Behavioral spot-check (live this verification run): `fmt exit: 0`. |
| 5.7 | `infra/envs/prod/README.md` 'Phase 4 close' section gains an operator note explaining that next `infra/external/` apply will plan destroy of `azuread_application_identifier_uri.api` + update-in-place of `azuread_application.api`         | VERIFIED | Line 229: full operator paragraph naming Phase 04.1 fix 5, the destroy of `azuread_application_identifier_uri.api`, and the update-in-place adding `identifier_uris`. |
| 5.8 | No `terraform apply`. No `az ad app …`. No Entra resource mutation in this phase.                                                                                                                                                            | VERIFIED | No `apply` invocation in any commit; cloud-state reconciliation explicitly deferred to next operator session per SUMMARY-05 + operator note. |
| 5.9 | One atomic commit `refactor(04.1-05): move identifier_uri inline …` on `infra/external/main.tf` + `infra/envs/prod/README.md` (+ Rule-1 auto-fix: `infra/external/outputs.tf`)                                                              | VERIFIED | `git show --stat 7e0718c` lists exactly 3 files. Plan specified 2 files; SUMMARY-05 documents the outputs.tf addition as a Rule-1 deviation (required because outputs.tf line 13/18 had references to the deleted resource — `terraform validate` would have failed without it). |

**Score:** 37/37 truths verified across all 5 plans (100%).

### Required Artifacts

| Artifact                                       | Expected                                                                  | Status     | Details                                                                                                              |
| ---------------------------------------------- | ------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------- |
| `src/job_rag/db/engine.py`                     | post-alembic idempotent UPDATE wired into init_db()                       | VERIFIED   | Modified (b81a53e/dedbe56). 102 lines, contains `_seed_entra_oid` + `SEEDED_USER_ENTRA_OID` env read + `init_db()` call. |
| `alembic/versions/0005_adopt_entra_oid.py`     | schema-only upgrade() body — UPDATE moved out                             | VERIFIED   | Modified. UPDATE block gone; only `_has_column` guard + `create_index` for `ix_users_entra_oid_unique` remain. Docstring cross-references "UPDATE block was moved to … 04.1 fix 1". |
| `tests/test_cli.py`                            | init_db UPDATE coverage (2 new test cases)                                | VERIFIED   | Modified. Two new tests (`test_seed_entra_oid_no_op_when_env_unset`, `test_seed_entra_oid_executes_update_when_env_set`) plus engine-patch wrapper on existing alembic-upgrade test. |
| `.github/workflows/deploy-api.yml`             | smoke check that treats Running AND RunningAtMaxScale as healthy          | VERIFIED   | Modified (7fd0f61). Multi-state OR conditional on line 154; updated comment header on lines 148-152; `$STATE` interpolation in success log on line 155. |
| `frontend/src/main.tsx`                        | try/catch wrapped boot sequence with renderBootError on rejection         | VERIFIED   | Modified (ee4a1e5). Try/catch wrap, `renderBootError` helper, BootErrorFallback import, D-05 race-fix comment preserved inside try. |
| `frontend/src/components/BootErrorFallback.tsx`| minimal Tailwind-only fallback component shown when MSAL/init rejects     | VERIFIED   | Created (ee4a1e5). 43 lines, zero imports, `role="alert"`, 1000-char stack truncation, Reload page button. |
| `frontend/src/test/BootErrorFallback.test.tsx` | vitest coverage of the fallback render + truncation + reload button       | VERIFIED   | Created (ee4a1e5). 5 test cases (headline + pre, String fallback, truncation, reload button, a11y alert role). |
| `infra/bootstrap/README.md`                    | canonical customer/Member bootstrap step in Phase 3 tenant-creation runbook | VERIFIED   | Modified (0b6f0ea). New `## Step 5` section spans lines 93-185 (94 LOC added). |
| `infra/envs/prod/README.md`                    | cross-reference to bootstrap README + operator note for next TF apply     | VERIFIED   | Modified (0b6f0ea for the bootstrap cross-ref on line 223; 7e0718c for the TF unreliability operator note on line 229). |
| `infra/external/main.tf`                       | azuread_application.api with inline identifier_uris attribute             | VERIFIED   | Modified (7e0718c). Inline `identifier_uris = ["api://${azuread_application.api.client_id}"]` on line 32; dedicated resource gone. |
| `infra/external/outputs.tf`                    | (Rule-1 auto-fix) outputs read from one(identifier_uris) instead of the deleted resource | VERIFIED   | Modified (7e0718c). `one(azuread_application.api.identifier_uris)` on lines 17 + 22. |

### Key Link Verification

| From                                                                  | To                                                       | Via                                                                                              | Status | Details                                                                                                                                          |
| --------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `engine.py::init_db()`                                                | `alembic/versions/0005_adopt_entra_oid.py`               | `command.upgrade(cfg, 'head')` runs migrations first; UPDATE runs after, env-gated               | WIRED  | `init_db()` calls `command.upgrade(cfg, "head")` then `_seed_entra_oid()` (lines 100-101). |
| `os.environ['SEEDED_USER_ENTRA_OID']`                                 | `UPDATE users SET entra_oid = :oid …`                    | sync `engine.begin()` `conn.execute(sa.text(...).bindparams(...))`                               | WIRED  | `_seed_entra_oid()` reads env at line 76; on non-empty, runs SQL via `engine.begin()` + `conn.execute(text(...).bindparams(oid=…, seeded_uuid=…))` (lines 79-85). |
| `az containerapp revision show … --query properties.runningState`    | smoke success branch                                     | both Running and RunningAtMaxScale exit 0                                                        | WIRED  | Line 138 captures `$STATE` from the az query; line 154 OR-conditional accepts both states. |
| `frontend/src/main.tsx try { await msalInstance.* } catch (e)`        | `renderBootError(e) → createRoot(rootEl).render(<BootErrorFallback error={e} />)` | ensures React mounts even when MSAL throws                                                       | WIRED  | Lines 19-25 define `renderBootError`; lines 27-50 wrap the boot chain; catch arm on line 48 calls `renderBootError(error)`. |
| `infra/bootstrap/README.md` new Step 5 section                        | `04-06-RUNBOOK.md` + `04-06-SUMMARY.md`                  | operator runs the bootstrap, then follows the Phase 4 close runbook for KV/DB seeding            | WIRED  | Lines 115-117 + line 174-176 reference both runbook + summary. |
| `infra/external/main.tf` `azuread_application.api`                    | AADSTS500011 sign-in error mitigation                    | `identifier_uris` attribute on the application resource (reliable form per azuread provider ~> 3.x) | WIRED  | Line 32 sets `identifier_uris` directly on `azuread_application.api`; `terraform validate` confirms schema accepts the attribute. |

### Data-Flow Trace (Level 4)

Most artifacts in this phase do not render dynamic user-facing data (they are infrastructure / boot helpers / CI workflow / docs). One artifact does render dynamic data:

| Artifact                                       | Data Variable | Source                                       | Produces Real Data | Status   |
| ---------------------------------------------- | ------------- | -------------------------------------------- | ------------------ | -------- |
| `frontend/src/components/BootErrorFallback.tsx`| `error` prop  | `frontend/src/main.tsx` catch arm: `renderBootError(error)` passes the caught Error/unknown into `<BootErrorFallback error={error} />` | Yes (real Error instance on rejection; `String(error)` fallback for non-Error) | FLOWING |

The `init_db()::_seed_entra_oid()` data flow is verified at the **unit level only** (mock-based — the SQL string + bind params are confirmed correct). Live DB roundtrip is intentionally out-of-band per D-02 — see human_verification item #3.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Plan 01 — pytest passes on _seed_entra_oid + init_db tests | `uv run pytest tests/test_cli.py -k "init_db or seed_entra_oid" -q` | `3 passed, 1 deselected in 0.18s` | PASS |
| Plan 02 — deploy-api.yml is valid YAML | `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-api.yml'))"` | exit 0, "YAML OK" | PASS |
| Plan 03 — vitest passes on BootErrorFallback | `cd frontend && npm test -- --run BootErrorFallback` | `5/5 tests passed` (61ms) | PASS |
| Plan 05 — terraform validates with the inline refactor | `cd infra/external && terraform validate` | `Success! The configuration is valid.` | PASS |
| Plan 05 — terraform fmt -check (canonical formatting) | `cd infra/external && terraform fmt -check` | exit 0 | PASS |
| All 5 commits present on master | `git log --oneline \| grep -cE "^[a-f0-9]+ (fix\|refactor\|docs)\(04\.1-0[1-5]\):"` (excluding the bookkeeping docs commits) | 5 fix/refactor commits + 5 docs commits = 10 total | PASS |

### Requirements Coverage

Per ROADMAP.md, phase 04.1's row explicitly states "**Requirements**: None (follow-ups, not new requirements — 04-* requirements stay as-is in REQUIREMENTS.md)". Each PLAN's frontmatter likewise declares `requirements: []`. This is a decimal/polish gap-closure phase — verifying that the **structural follow-ups** landed is the requirements-coverage analog.

No orphaned requirements detected. REQUIREMENTS.md was not modified by this phase (verified by `git show --name-only` on all 5 fix commits).

### Anti-Patterns Found

Code review (`04.1-REVIEW.md`) was executed independently before this verification and reports `findings: { critical: 0, warning: 2, info: 4, total: 6 }`. Summary:

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `src/job_rag/db/engine.py` | 76 | `_seed_entra_oid()` reads `os.environ` directly instead of `settings.seeded_user_entra_oid` (two source-of-truth paths for one config value) | Warning (WR-01) | Cosmetic in practice — both paths read same OS env on boot. Will silently disagree if Settings adds field-level normalization in future. Not phase-blocking. |
| `src/job_rag/db/engine.py` | 16 | `SEEDED_USER_UUID` redeclared as string literal instead of `str(settings.seeded_user_id)` (drift surface, Pitfall 18) | Warning (WR-02) | Same regression-surface as WR-01. Not phase-blocking. |
| `infra/envs/prod/README.md` | 218 | Table row 2 (manual `az ad app update --identifier-uris` workaround) becomes obsolete after Plan 05 apply lands | Info (IN-01) | Transitional documentation. Tracked for cleanup post-apply. |
| `frontend/src/test/BootErrorFallback.test.tsx` | 8-24 | `afterEach` restoration shallow-copies the spread-replaced location, leaks non-prototype-equal object cross-test | Info (IN-02) | No cross-test leakage observed in this verification run (`vitest --run` passes cleanly). |
| `frontend/src/components/BootErrorFallback.tsx` | 17 | Magic number 1000 for stack truncation (no constant) | Info (IN-03) | Cosmetic. Test pins ≤1000 not exact value. |
| `tests/test_cli.py` | 24-44 | Three `pytest.skip()` branches in `test_init_db_invokes_alembic_upgrade` are now unreachable post-Plan-02 (Phase 1) | Info (IN-04) | These predate Phase 04.1 (carried over from Phase 1 Plan 02 transition window). Hide real regressions if the alembic import gets broken. Not phase-blocking — but worth a follow-up cleanup. |

None of the warnings/info findings block the phase goal. The 2 warnings (WR-01, WR-02) are about consistency with existing project patterns (`settings.seeded_user_entra_oid`, `settings.seeded_user_id`) rather than correctness — Plan 01 ships a working idempotent UPDATE, and the env-read path produces the same value as the Settings-read path on container boot. **Recommend a 04.2 micro-phase to address WR-01 + WR-02 + IN-04 in one combined commit at a quiet moment** — not a phase-blocker for 04.1.

### Human Verification Required

| # | Test | Expected | Why human |
| - | ---- | -------- | --------- |
| 1 | Push master → next Deploy API workflow run on a revision that scales out | Smoke succeeds when `runningState=RunningAtMaxScale`; success log reads "is RunningAtMaxScale and serving /health 200" | Plan 04.1-02 ships dormant under D-02 — no `git push`, no `gh workflow run` in-phase. Requires operator's next master push hitting a revision that reaches the max-scale state during the 150s budget. |
| 2 | `cd infra/external && terraform apply` on next prod re-bootstrap | Plan output shows (a) destroy of `azuread_application_identifier_uri.api` + (b) update-in-place of `azuread_application.api` adding `identifier_uris`; post-apply `az ad app show --query identifierUris -o tsv` returns the populated list | Plan 04.1-05 ships dormant under D-02 — no `terraform apply` in-phase. Live state reconciliation deferred to the next operator session per operator note appended to `infra/envs/prod/README.md` line 229. |
| 3 | ACA revision restart against prod backend after `SEEDED_USER_ENTRA_OID` rotation in KV | `users.entra_oid` row gets updated to the new OID on container boot without any manual `az containerapp exec`; AUTH-06 accepts JWTs with the new oid | Plan 04.1-01's structural fix can only be live-verified by rotating the KV secret + restarting ACA — both cloud touches forbidden by D-02. Unit-test coverage proves the helper's branching logic; live integration proof is out-of-band. |
| 4 | Trigger a real MSAL-init rejection in the browser (e.g., break `VITE_SPA_CLIENT_ID` or invalidate `identifierUris` mid-session) and reload the SPA | `BootErrorFallback` renders the error message in a styled `<pre>`, the Reload page button works — instead of a blank `<div id="root">` | Plan 04.1-03's catch arm only fires on real MSAL/handleRedirectPromise rejection; cannot be simulated through unit tests beyond rendering the fallback in isolation (which IS covered by Vitest). |

These 4 items are **expected and acceptable per CONTEXT D-02** (no remote push, no cloud touches in this phase). They are not gaps — they are deferred verification windows that the next operator session naturally exercises.

### Gaps Summary

**No structural gaps found.** All 37 plan must_have truths are VERIFIED. All 11 required artifacts exist, are substantive (not stubs), and are wired into their consumers. All 6 key links are WIRED. Behavioral spot-checks pass (pytest 3/3, vitest 5/5, terraform validate exit 0, YAML safe_load exit 0).

The phase goal — "Close the 5 outstanding structural follow-ups recorded in Phase 4's close-out SUMMARY … One PLAN per fix, executed in priority order Fix 1 → Fix 5. No remote push, no cloud touches — every plan ends with a local atomic commit (D-01..D-03)" — is achieved:

- **Fix 1 → Fix 5** all landed serially with the required `depends_on` chain (verified by commit timestamps: `dedbe56` 23:08:08 → `7fd0f61` 23:15:07 → `ee4a1e5` 23:21:55 → `0b6f0ea` 23:29:34 → `7e0718c` 23:56:07).
- **D-01 (no remote pushes):** confirmed by `git log origin/master..HEAD` containing the 5 fix commits + 5 docs commits — none pushed.
- **D-02 (no cloud touches):** confirmed by reviewing each commit's file scope — no `az` invocations executed, no `terraform apply` invocations, no `gh workflow run`.
- **D-03 (one atomic commit per fix):** confirmed by `git show --stat` on each commit listing the expected file set (3 for 01, 1 for 02, 3 for 03, 2 for 04, 3 for 05 with documented Rule-1 outputs.tf addition).
- **D-04 (priority order = serial execution):** confirmed by commit timestamps and `depends_on` chain in each PLAN's frontmatter.

**Status: `human_needed`** rather than `passed` only because 4 in-phase deferred verifications require live cloud/browser operations (per the explicit user instruction "Flag those as `human_needed` items if you want post-merge live verification — that is acceptable and expected for this phase"). The codebase artifacts are complete and locally proven; the remaining verification windows open naturally on the operator's next master push / next `terraform apply` / next browser session.

---

_Verified: 2026-05-22T00:15:00Z_
_Verifier: Claude (gsd-verifier)_
