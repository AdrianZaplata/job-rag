---
phase: 06-chat
plan: 05
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - .planning/phases/06-chat/06-UAT.md
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06]
tags: [chat, uat, manual, live-azure, demo-evidence]

must_haves:
  truths:
    - "Adrian executes 6 M-marker UAT scenarios against live SWA + deployed ACA stack"
    - "M1 (happy path): one full chat turn renders question → optional tool_start chip → tool_end chip with output → streamed synthesis → final; transcript visible end-to-end"
    - "M2 (cold-start UX): after 4+ hour ACA idle, first chat shows \"Thinking…\" at T=0s then \"Warming up the agent — this can take ~4 minutes after idle\" at T=10s+; tokens stream when fetch finally resolves"
    - "M3 (tool chip live): click chip header → expands → \"Show full output\" opens Dialog with full unsanitized search_jobs result"
    - "M4 (Stop mid-stream): submit a long-running query → click Stop during streaming → AbortError caught → current assistant text marked \"(stopped)\" → composer re-enables → next submit works"
    - "M5 (refresh = zero residue): submit + complete a turn → refresh page → DevTools Application tab shows ZERO chat-related keys in localStorage/sessionStorage/IndexedDB → EmptyState renders"
    - "M6 (≤30s demo recording): one chat turn captured as screen recording in <30 seconds (ROADMAP §Phase 6 success criterion 5)"
    - ".planning/phases/06-chat/06-UAT.md captures evidence (screenshots, timestamps, DevTools captures) for all 6 markers"
    - "All 6 CHAT-* requirements (CHAT-01..06) verified PASS in UAT"
  artifacts:
    - path: ".planning/phases/06-chat/06-UAT.md"
      provides: "6 M-marker UAT evidence package + per-marker PASS/FAIL + 6 CHAT-* requirement closure"
      contains: "M1\nM2\nM3\nM4\nM5\nM6"
  key_links:
    - from: "06-UAT.md M1 (happy path)"
      to: "CHAT-01 + CHAT-02 + CHAT-05 + ROADMAP §Phase 6 success criterion 1"
      via: "manual evidence — Adrian verifies tokens stream incrementally in DevTools EventStream tab"
      pattern: "tokens stream"
    - from: "06-UAT.md M2 (cold-start)"
      to: "D-19 + UI-SPEC §12 + memory/aca-cold-start-profile"
      via: "manual evidence — 4+ hour idle + screenshot showing \"Warming up…\" copy at T=11s"
      pattern: "Warming up the agent"
    - from: "06-UAT.md M3 (tool chip)"
      to: "CHAT-03 + CHAT-04 + FEATURES.md \"Inspectable agent tool calls\""
      via: "manual evidence — click chip + Show full output → Dialog renders full output"
      pattern: "Dialog opens"
    - from: "06-UAT.md M5 (zero residue)"
      to: "CHAT-06 + ROADMAP §Phase 6 success criterion 4"
      via: "manual evidence — DevTools Application tab capture proving 0 chat-* keys after refresh"
      pattern: "no localStorage residue"
---

<objective>
Live UAT runbook (autonomous: false) — Adrian executes 6 M-marker test scenarios against the deployed SWA + ACA stack with real Entra JWT, real LangGraph agent, real search_jobs tool. Captures evidence (screenshots, DevTools EventStream captures, Application-tab inspections, screen recordings) into `.planning/phases/06-chat/06-UAT.md`. Closes all 6 CHAT-* requirements + the 5 ROADMAP §Phase 6 success criteria + the ≤30s demoable screen recording.

Purpose: The ONLY way to validate cold-start UX honesty (D-19) is against a genuinely-cold-from-zero ACA replica — local dev with `npm run dev` skips the 4-min wait. Unit tests cover code paths; UAT covers integration realities (Envoy heartbeat preservation, real search_jobs output shape, real MSAL token refresh timing under streaming, DevTools storage inspection for CHAT-06 zero-residue beyond what spies catch).

Output: 1 file (`06-UAT.md`) populated with 6 marker tables + screenshots + pass/fail + per-marker timestamps. Plan 05's commit is the UAT report. Phase 6 closes after Adrian marks all 6 markers PASS.

Task 1 is the CHECKPOINT — Adrian runs the 6 manual scenarios. Task 2 then writes the evidence file from the observations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-chat/06-CONTEXT.md
@.planning/phases/06-chat/06-UI-SPEC.md
@.planning/phases/06-chat/06-RESEARCH.md
@.planning/phases/06-chat/06-VALIDATION.md
@.planning/phases/06-chat/06-01-SUMMARY.md
@.planning/phases/06-chat/06-02-SUMMARY.md
@.planning/phases/06-chat/06-03-SUMMARY.md
@.planning/phases/06-chat/06-04-SUMMARY.md
@.planning/phases/05-dashboard/05-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (CHECKPOINT — Adrian-driven UAT): 6 M-markers against live SWA + ACA</name>
  <what-built>
    Phase 6 chat surface end-to-end:
    - Backend: POST `/agent/stream` accepts `AgentQuery(query: str)` body (Plan 02 mutated routes.py 3 lines).
    - Frontend: `streamAgent(query, signal)` typed helper (Plan 03) → `useChatStream` hook (Plan 03 — owns transcript reducer + AbortController + cold-start timer + SSE event loop) → 5 presentation components (Plan 04 — ChatTranscript / ChatMessage / ToolChip / ChatComposer / ChatEmptyState) wired through `frontend/src/routes/Chat.tsx`.
    - Wave 0 foundation (Plan 01): 2 shadcn primitives (collapsible + textarea) + types module + blink CSS + 4 skip scaffolds + DebugAgentStream Pitfall H closure.

    All 4 prior plans green: pytest + ruff + pyright + frontend typecheck + lint + vitest + Vite build all exit 0. CI deployment workflows (deploy-api.yml + deploy-spa.yml) should already have shipped the backend + SPA to production by the time this task runs. If not, Adrian deploys first (see Setup section in `<how-to-verify>` below).
  </what-built>
  <how-to-verify>
    ### Setup (do once before starting M-markers)

    1. **Confirm production deployment up-to-date:**
       ```bash
       gh run list --workflow deploy-api.yml --limit 1
       gh run list --workflow deploy-spa.yml --limit 1
       ```
       Both runs should be successful + reference the Phase 6 commits (Plans 01-04). If older, push/redeploy first.

    2. **Confirm ACA cold-start state for M2** (decide DURING the UAT session):
       - M2 (cold-start UX) requires a 4+ hour ACA idle window. Schedule it intentionally — best done if Adrian has not used the deployed app for 4+ hours.
       - Quick check via `az containerapp revision show ...` for `provisioningState: Provisioned, activeRevisionsMode: Single, properties.replicas: 0` → indicates scaled-to-zero. If currently running, wait 4 hours OR force-scale to zero (`az containerapp update --min-replicas 0` + verify; usually NOT needed since min=0 already).

    3. **Prep a quiet desktop** for the screen recording (M6) — close noisy tabs, hide menu bars, use the production SWA URL (`https://witty-flower-065dac003.7.azurestaticapps.net/chat` or current SWA FQDN per Phase 4 STATE).

    ---

    ### M1 — Happy path (one full chat turn)

    **Requirement coverage:** CHAT-01, CHAT-02, CHAT-05; ROADMAP §Phase 6 success criterion 1.

    1. Sign in at the production SWA URL using Adrian's customer Entra account (`adrian@jobrag.onmicrosoft.com` per Phase 4 STATE).
    2. Navigate to `/chat`. Verify `<ChatEmptyState>` renders (4 sample chips visible; MessageSquare icon; heading "Ask the agent about the job-market corpus").
    3. Click sample chip "What's the top must-have skill in Berlin?" → verify the composer Textarea populates with the chip text + cursor focused at end of textarea.
    4. (Optional) Edit the query.
    5. Open DevTools → Network tab → filter "EventStream" (or `text/event-stream` MIME).
    6. Hit Enter → verify:
       - POST `/agent/stream` request fires (status 200; `Authorization: Bearer ...` header attached)
       - Network tab "EventStream" sub-tab shows discrete `event: token` / `event: tool_start` / `event: tool_end` / `event: final` frames arriving — NOT a single buffered response (this is the Pitfall E preservation check)
       - In the UI: user-message renders → assistant-text bubble appears with "Thinking..." → first token swaps it to streaming content with blinking `▍` cursor → if agent calls `search_jobs`, tool chip appears collapsed (running state pulse `·`) → tool returns → chip fills with output preview → assistant-text continues streaming → final closes the bubble (cursor disappears) → composer re-enables
       - Composer Send button reappears after final
    7. **Capture:** Network tab EventStream sub-tab screenshot + UI screenshot of the completed turn.
    8. **PASS criteria:** Tokens streamed incrementally (NOT buffered) AND at least one tool chip appeared AND final marked the bubble complete AND composer re-enabled.

    ---

    ### M2 — Cold-start UX (the portfolio differentiator)

    **Requirement coverage:** D-19 + UI-SPEC §12 + memory/aca-cold-start-profile.

    **Prerequisite:** ACA replica is genuinely scaled-to-zero (4+ hour idle, or just-restarted). If you cannot achieve this, document the wait in 06-UAT.md and re-run after the window.

    1. Sign in at the production SWA URL (cold-start window: this initial page load might already trigger a cold-start; if you see "Warming up" on the page-load fetch, that's NOT this test — wait until that one completes).
    2. Navigate to `/chat`. Verify EmptyState renders.
    3. Open DevTools → Console (clear any logs).
    4. **Time T=0s:** Click a sample chip or type a query; hit Enter.
    5. **Observe T=0s:** Assistant text bubble appears with **"Thinking"** + three pulsing dots.
    6. **Observe T=10s:** Label swaps to **"Warming up the agent — this can take ~4 minutes after idle"** (verbatim per D-19; em-dash U+2014).
    7. **Observe T=10s–225s:** Label stays steady on "Warming up..."; no further changes.
    8. **Observe T≈225s (or whenever fetch resolves):** First event arrives; label disappears; tokens start streaming OR tool chip appears + content streams normally.
    9. **Capture:** Screenshots at T=2s ("Thinking..."), T=12s ("Warming up..."), T=stream-start (cursor appears with content).
    10. **PASS criteria:** "Thinking..." renders at T<10s AND "Warming up the agent — this can take ~4 minutes after idle" renders at T≥10s AND eventually streams successfully (does NOT timeout at 60s on the asyncio side — cold-start latency is on the fetch establishment, not the agent generation).

    ---

    ### M3 — Tool chip live: expand + "Show full output" Dialog

    **Requirement coverage:** CHAT-03, CHAT-04, FEATURES.md "Inspectable agent tool calls" portfolio differentiator.

    **Prerequisite:** A real `search_jobs` tool call from M1 (or trigger a fresh one with a query like "Find postings for Senior Python engineers in Berlin").

    1. After a turn that triggered `search_jobs` (or any tool), locate the collapsed chip in the transcript.
    2. Verify collapsed appearance: `→ search_jobs {"query":"...","seniority":"senior"}` with a `▶` chevron on the right.
    3. Click anywhere on the chip header (NOT just the chevron — full-row click target per D-10).
    4. Verify chip expands: chevron rotates 90° to `▼`; body shows `args` block (pretty-printed JSON) + `output` block (preview text, first 200 chars).
    5. Determine if "Show full output" Button is visible (only renders if `output.length > 200`).
    6. If visible: click "Show full output" → Dialog opens with title "Tool output", description "Full output of `search_jobs`", and the FULL output text in a scrollable `<pre>` block.
    7. Verify Dialog focus trap (Tab cycles inside dialog; first tab lands on first focusable element); press Escape → Dialog closes; focus returns to "Show full output" trigger.
    8. Click chip header again → chip collapses (chevron back to `▶`).
    9. **Capture:** Screenshots of (a) collapsed chip with args preview, (b) expanded chip with truncated output + Show full button, (c) Dialog open with full output, (d) Dialog closed → chip stays expanded.
    10. **PASS criteria:** Chip toggles open/closed on header click AND args render as pretty-printed JSON AND output preview truncates ≥200 chars with `…` AND Dialog opens with full output AND Escape closes Dialog.

    ---

    ### M4 — Stop mid-stream cancels cleanly

    **Requirement coverage:** D-16 + Pitfall B (live verification of unit test).

    1. Submit a query likely to trigger a longer tool call + synthesis (e.g., "Summarize the top 20 skills across all Berlin senior postings").
    2. As soon as Stop button appears (replaces Send), click Stop **before** the first tool chip or before all tokens stream in.
    3. Verify:
       - Streaming halts immediately (no further tokens append to the current assistant-text bubble)
       - Current assistant-text bubble gets `(stopped)` suffix in muted-foreground after its accumulated content
       - Stop button disappears; Send button reappears (composer re-enables)
       - NO top-of-route "Server error" Alert appears (Pitfall B verification — AbortError is NOT a network error)
       - networkError stays null (verify via DevTools React DevTools → useChatStream hook state, or simply by absence of the top-of-route Alert)
    4. Re-submit a fresh query → verify new stream works normally (proves AbortController is fresh per submit; no orphaned signal).
    5. **Capture:** Screenshot of `(stopped)` suffix on truncated assistant text + Send button visible + transcript scrolling normally.
    6. **PASS criteria:** Stop click halted streaming AND `(stopped)` suffix rendered AND composer re-enabled AND no false network-error Alert AND next submit worked.

    ---

    ### M5 — Refresh = zero residue (CHAT-06)

    **Requirement coverage:** CHAT-06; ROADMAP §Phase 6 success criterion 4.

    1. After at least one completed turn from M1 (transcript has user-message + assistant-text + tool-call items), open DevTools → Application tab.
    2. Inspect each storage section:
       - **Local Storage** for the SWA origin: expand → list all keys
       - **Session Storage** for the SWA origin: expand → list all keys
       - **IndexedDB** for the SWA origin: expand → list all databases
    3. Verify: no keys/databases match patterns like `chat-*`, `transcript-*`, `agent-*`, `message-*`. The only allowed keys are:
       - `theme` (Phase 4 D-20 — light/dark mode preference)
       - MSAL-related keys (e.g., `msal.account.keys`, `msal.token.keys.*`, `msal.cache`, etc. — Phase 4 D-06 sessionStorage cache)
       - Sonner / shadcn-related keys (if any — typically none)
    4. **Capture:** Application-tab screenshot showing the exhaustive key list (annotated to call out absence of chat-related keys).
    5. Refresh the browser (F5 / Cmd+R) → verify EmptyState renders again (transcript wiped). Click a sample chip → composer populates (proves React-only state).
    6. **PASS criteria:** Zero `chat-*` / `transcript-*` / `agent-*` / `message-*` keys in localStorage / sessionStorage / IndexedDB AND refresh wipes transcript entirely AND post-refresh state is clean EmptyState.

    ---

    ### M6 — ≤30s demoable screen recording

    **Requirement coverage:** ROADMAP §Phase 6 success criterion 5.

    1. Use macOS Screenshot (Cmd+Shift+5 → Record Selected Portion) or QuickTime → New Screen Recording. Resolution: at least 1080p.
    2. Start recording.
    3. Navigate to `/chat` → EmptyState renders.
    4. Click sample chip "What's the top must-have skill in Berlin?" (or another that produces a tool call).
    5. Hit Enter.
    6. Wait for: assistant Thinking → tool_start chip → tool_end with output preview → streaming synthesis → final.
    7. Click the tool chip → it expands to show args + output.
    8. (Optional) Click "Show full output" → Dialog opens → close.
    9. Stop recording. Trim to ≤30 seconds total.
    10. **Capture:** Save as `06-chat-demo.mp4` (or .mov / .gif as appropriate) in `.planning/phases/06-chat/`. Add screenshot stills to UAT.md if recording is too large for git.
    11. **PASS criteria:** Recording is ≤30 seconds AND captures one full chat turn AND tool chip expansion is visible AND demoable as-is (no edits needed; the streaming UX speaks for itself).

    ---

    ### After all 6 markers complete:

    Hand the observations to Task 2 (Claude will write the structured `06-UAT.md` file from your notes). Provide:
    - PASS/FAIL per marker
    - Observed values (cold-start latency, tool name, output length, recording duration)
    - Screenshot file paths
    - Any deviations / FAIL details

    Resume signal: type **"all markers pass"** with a brief per-marker summary OR **"issue: <description>"** with FAIL details.

    If any marker FAILS:
    - Document the failure in the marker's Notes column with screenshot
    - If hotfix is small (one-line bug; CSS tweak): fix + commit + re-test the marker; update result to PASS
    - If hotfix is large (architectural; multi-file): pause UAT + spawn a gap-closure plan (`/gsd-plan-phase 6 --gaps`) + re-run UAT post-merge
    - If marker depends on cold-start environment (M2) and cannot be exercised: document the constraint + schedule a later session

    Phase 6 closes ONLY when all 6 markers are PASS.
  </how-to-verify>
  <resume-signal>Type "all markers pass" with brief summary OR "issue: <description>" with FAIL details. On all-pass: Claude runs Task 2 to write 06-UAT.md from the observations.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write .planning/phases/06-chat/06-UAT.md capturing the 6 M-marker results from Task 1</name>
  <files>.planning/phases/06-chat/06-UAT.md</files>
  <read_first>
    - .planning/phases/05-dashboard/05-UAT.md (analog UAT file from Phase 5; mirror the structure)
    - .planning/phases/06-chat/06-VALIDATION.md §"Manual-Only Verifications" (the M-markers seed list)
    - .planning/ROADMAP.md §Phase 6 "Success Criteria" (5 ROADMAP criteria the UAT closes)
    - .planning/REQUIREMENTS.md §"Chat — streaming (CHAT)" (6 CHAT-* requirements the UAT closes)
    - Adrian's resume-signal output from Task 1 (the observations to transcribe)
  </read_first>
  <action>
    Create `.planning/phases/06-chat/06-UAT.md` with the following structure. Fill EVERY `<fill>` placeholder from Adrian's Task 1 observations. The structure below is the canonical Phase 6 UAT shape:

    ```markdown
    ---
    phase: 6
    slug: chat
    status: complete
    date: <YYYY-MM-DD from Adrian's session>
    uat_environment: production SWA + deployed ACA
    swa_origin: <production SWA FQDN — pull from infra/envs/prod outputs or Phase 4 STATE>
    aca_fqdn: <production ACA FQDN — pull from infra/envs/prod outputs>
    build_sha: <git SHA at time of UAT — git rev-parse HEAD>
    ---

    # Phase 6 — Live UAT Evidence

    > Per VALIDATION.md "Manual-Only Verifications" + ROADMAP §Phase 6 success
    > criteria. 6 M-markers; each must PASS or be documented as FAIL-with-note.
    > Closes 6 CHAT-* requirements + 5 ROADMAP success criteria.

    ## Environment

    - SWA origin: <fill>
    - Backend ACA FQDN: <fill>
    - Adrian's signed-in oid: 18d774c1-62ac-4416-8945-b5eca715e9ed (seeded user per Phase 4)
    - Browser: <fill, e.g. Chrome 130 / Safari 18>
    - Date / time (ISO): <fill>
    - Build SHA: <fill — git rev-parse HEAD>
    - ACA cold-start state at UAT start: <warm / cold (4+ hour idle)>

    ## M1 — Happy path (one full chat turn)

    | Property | Value |
    |----------|-------|
    | CHAT-* coverage | CHAT-01, CHAT-02, CHAT-05 |
    | ROADMAP criterion | #1 (tokens stream incrementally) |
    | Result | <PASS / FAIL> |
    | First token latency | <fill, e.g. ~1.2s on warm container> |
    | Tokens streamed incrementally (EventStream tab discrete frames) | <YES / NO> |
    | Tool chip appeared mid-stream | <YES / NO; tool name if YES> |
    | Final closed bubble + composer re-enabled | <YES / NO> |
    | Evidence | screenshots/m1-network.png, screenshots/m1-ui.png |
    | Notes | <fill> |

    ## M2 — Cold-start UX (D-19 + UI-SPEC §12)

    | Property | Value |
    |----------|-------|
    | D-19 coverage | UI-SPEC §12 cold-start swap at T=10s |
    | Result | <PASS / FAIL / DEFERRED (no cold-start window achievable)> |
    | Cold-start latency observed (submit → first event) | <fill, e.g. ~215s> |
    | "Thinking..." rendered at T<10s | <YES / NO> |
    | "Warming up the agent — this can take ~4 minutes after idle" rendered at T≥10s | <YES / NO> |
    | Em-dash U+2014 verbatim verified | <YES / NO> |
    | Eventually streams successfully (no false 60s timeout) | <YES / NO> |
    | Evidence | screenshots/m2-t0.png, screenshots/m2-t12.png, screenshots/m2-stream-start.png |
    | Notes | <fill> |

    ## M3 — Tool chip live + "Show full output" Dialog

    | Property | Value |
    |----------|-------|
    | CHAT-* coverage | CHAT-03, CHAT-04 |
    | ROADMAP criterion | #2 (tool_start chip → tool_end with 200-char preview + Show full) |
    | Result | <PASS / FAIL> |
    | Tool called | <fill, e.g. search_jobs> |
    | Output length (chars) | <fill> |
    | Output > 200 chars (Show full visible) | <YES / NO> |
    | Chip header click toggles expand/collapse | <YES / NO> |
    | Dialog opens with full output | <YES / NO> |
    | Dialog Escape closes + focus returns to "Show full output" trigger | <YES / NO> |
    | args rendered as pretty-printed JSON | <YES / NO> |
    | Evidence | screenshots/m3-collapsed.png, screenshots/m3-expanded.png, screenshots/m3-dialog.png |
    | Notes | <fill> |

    ## M4 — Stop mid-stream cancels cleanly

    | Property | Value |
    |----------|-------|
    | D-16 + Pitfall B coverage | yes |
    | ROADMAP criterion | #3 (final marks bubble complete; submit-during-stream blocked — Stop is the inverse path) |
    | Result | <PASS / FAIL> |
    | Streaming halted on Stop click | <YES / NO> |
    | "(stopped)" suffix rendered on truncated assistant text | <YES / NO> |
    | Top-of-route network-error Alert appeared | <NO (correct) / YES (FAIL — Pitfall B regression)> |
    | networkError stayed null | <YES / NO> |
    | Composer Send button reappeared | <YES / NO> |
    | Next submit worked normally | <YES / NO> |
    | Evidence | screenshots/m4-stopped.png |
    | Notes | <fill> |

    ## M5 — Refresh = zero residue (CHAT-06)

    | Property | Value |
    |----------|-------|
    | CHAT-* coverage | CHAT-06 |
    | ROADMAP criterion | #4 (refresh clears transcript; zero localStorage/IndexedDB residue) |
    | Result | <PASS / FAIL> |
    | Chat-related keys in localStorage | <0 (correct) / N (FAIL with key list)> |
    | Chat-related keys in sessionStorage | <0 / N> |
    | Chat-related IndexedDB databases | <0 / N> |
    | Allowed keys present (theme, msal.*) | <list, e.g. theme, msal.account.keys, msal.cache.b2c_account.*> |
    | Refresh wipes transcript entirely | <YES / NO> |
    | Post-refresh state = EmptyState | <YES / NO> |
    | Evidence | screenshots/m5-application-tab.png |
    | Notes | <fill> |

    ## M6 — ≤30s demoable screen recording

    | Property | Value |
    |----------|-------|
    | ROADMAP criterion | #5 (≤30s demoable recording) |
    | Result | <PASS / FAIL> |
    | Recording duration | <fill, e.g. 24s> (must be ≤30s) |
    | Recording captures one full chat turn (question → tool chip → stream → final) | <YES / NO> |
    | Tool chip expansion visible in recording | <YES / NO> |
    | File location | .planning/phases/06-chat/06-chat-demo.mp4 (or screenshots/m6-frames/) |
    | Demoable as-is (no edits needed) | <YES / NO> |
    | Notes | <fill> |

    ## Phase 6 ROADMAP success criteria closure

    | # | Criterion | Result | Evidence |
    |---|-----------|--------|----------|
    | 1 | Tokens stream incrementally (DevTools EventStream tab discrete frames, not buffered) | <PASS / FAIL> | M1 |
    | 2 | tool_start chip collapsed → tool_end chip expanded with 200-char preview + "Show full" affordance | <PASS / FAIL> | M3 |
    | 3 | `final` marks bubble complete + re-enables composer; submit-during-stream blocked | <PASS / FAIL> | M1 (final + composer re-enable) + M4 (Stop inverse) |
    | 4 | Refresh clears transcript entirely; zero localStorage/IndexedDB residue | <PASS / FAIL> | M5 |
    | 5 | ≤30s demoable screen recording of one chat turn | <PASS / FAIL> | M6 |

    ## CHAT-* requirement closure

    | ID | Description | Status | Marker |
    |----|-------------|--------|--------|
    | CHAT-01 | Chat React page consumes `/agent/stream` via `fetch` + `ReadableStream` | <PASS / FAIL> | M1 |
    | CHAT-02 | `token` events render incrementally into assistant bubble | <PASS / FAIL> | M1 |
    | CHAT-03 | `tool_start` events render as collapsed chip with name + JSON args preview | <PASS / FAIL> | M3 |
    | CHAT-04 | `tool_end` expands chip with output preview (≥200 chars truncated with "expand" affordance) | <PASS / FAIL> | M3 |
    | CHAT-05 | `final` marks bubble complete + re-enables composer; attempting submit-during-stream blocked | <PASS / FAIL> | M1 + M4 |
    | CHAT-06 | Single-turn only; refreshing clears conversation; no history persistence | <PASS / FAIL> | M5 |

    ## Issues encountered / hotfixes applied during UAT

    <Free-form list. If any markers FAILED and a hotfix landed during the UAT session, list:
    - Commit SHA of the hotfix
    - Brief description
    - Marker that was re-tested + final result
    OR write "None — all 6 markers PASS on first attempt.">

    ## Overall Phase 6 verdict

    <PASS / PASS WITH MINOR DEVIATIONS / FAIL>

    ## Deviations from spec (if any)

    <List any observed UI deviations vs UI-SPEC §6 / class strings / copy / a11y attributes.
    Cross-reference the affected D-XX decision so future readers can trace the divergence.
    Common harmless deviations: 1-2 px spacing tweaks observed live but not noticed in unit tests, etc.
    OR write "None — UI matches UI-SPEC §6 verbatim.">

    ## Next

    Run `/gsd-verify-work 6` to formalize Phase 6 closure. Then either:
    - Proceed to Phase 7 (Profile & Resume Upload) — parallel-eligible per ROADMAP §"Phase Ordering Notes"
    - OR Phase 8 (Eval & Documentation) — terminal phase
    ```

    Notes for executor (Claude):
    - Read Adrian's Task 1 resume-signal output and use it to fill EVERY `<fill>` placeholder
    - 24-hour ISO timestamps where time is recorded (e.g., `2026-05-XX 14:32`)
    - Reference exact screenshot paths under `.planning/phases/06-chat/screenshots/` (subfolder; the binary files themselves may be excluded by gitignore per Phase 5 precedent — only the path references go in the markdown)
    - If a marker FAILS, the deviation MUST be triaged in "Deviations from spec" with one of: (a) accepted-as-is + reason, (b) opened-as-Phase-6-gap-closure plan, (c) deferred to Phase 8 polish
    - The `swa_origin` and `aca_fqdn` values come from `infra/envs/prod/` Terraform outputs OR from Phase 4 STATE.md (Phase 4 records `https://witty-flower-065dac003.7.azurestaticapps.net` as the production SWA)
    - The `build_sha` is `git rev-parse HEAD` at the time the UAT runs (typically the Plan 04 final commit OR a Plan-04-plus-hotfix commit)
    - Commit the file + any screenshots:

      ```bash
      git add .planning/phases/06-chat/06-UAT.md
      git add .planning/phases/06-chat/screenshots/ 2>/dev/null  # if present
      git add .planning/phases/06-chat/06-chat-demo.* 2>/dev/null  # if size-acceptable
      git commit -m "$(cat <<'EOF'
      docs(06-uat): live UAT — 6 M-markers PASS (Phase 6 close)

      Closes all 6 CHAT-* requirements + 5 ROADMAP §Phase 6 success criteria.
      Includes ≤30s demo recording per criterion 5.

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      EOF
      )"
      ```
  </action>
  <verify>
    <automated>test -f .planning/phases/06-chat/06-UAT.md && grep -c "^## M[1-6]" .planning/phases/06-chat/06-UAT.md | awk '$1 >= 6' | grep -q . && grep -q "phase: 6" .planning/phases/06-chat/06-UAT.md && grep -q "ROADMAP success criteria closure" .planning/phases/06-chat/06-UAT.md && grep -q "CHAT-\* requirement closure" .planning/phases/06-chat/06-UAT.md && grep -q "Overall Phase 6 verdict" .planning/phases/06-chat/06-UAT.md && ! grep -q "<fill>" .planning/phases/06-chat/06-UAT.md && grep -cE "PASS|FAIL" .planning/phases/06-chat/06-UAT.md | awk '$1 >= 12' | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-chat/06-UAT.md` exits 0
    - `grep -c "^## M[1-6]" .planning/phases/06-chat/06-UAT.md` >= 6 (M1 through M6 sections present)
    - `grep -c "phase: 6" .planning/phases/06-chat/06-UAT.md` >= 1 (frontmatter present)
    - `grep -c "ROADMAP success criteria closure" .planning/phases/06-chat/06-UAT.md` >= 1
    - `grep -c "CHAT-\* requirement closure" .planning/phases/06-chat/06-UAT.md` >= 1
    - `grep -c "Overall Phase 6 verdict" .planning/phases/06-chat/06-UAT.md` >= 1
    - `grep -c "<fill>" .planning/phases/06-chat/06-UAT.md` equals 0 (no remaining placeholders)
    - `grep -cE "(PASS|FAIL)" .planning/phases/06-chat/06-UAT.md` >= 12 (6 marker results + 5 ROADMAP closures + 6 CHAT-* closures = 17 verdict cells; minimum 12 accounts for some "DEFERRED" or "DOCUMENTED" entries)
    - Each of the 6 M-markers has a Result row with PASS or FAIL or DEFERRED
    - All 6 CHAT-* requirements have a Status entry
    - All 5 ROADMAP criteria have a Result entry
    - File committed via `git log --oneline -1 .planning/phases/06-chat/06-UAT.md` shows the commit
  </acceptance_criteria>
  <done>06-UAT.md exists with 6 M-marker sections + ROADMAP criteria closure + CHAT-* requirement closure + Overall verdict; no `<fill>` placeholders remain; each marker has a verdict; file committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live production stack | All M-markers exercise the deployed SPA + ACA + Postgres + Entra path; no test stubs |
| browser DevTools inspection | M5 manual storage-tab inspection IS the integration verification CHAT-06 needs (unit-test spies cover code; DevTools confirms NO other code path leaks chat-related state) |
| screen recording (M6) | No security impact; portfolio artifact |
| ACA cold-start (M2) | Genuine 4+ hour idle reveals the live UX honesty contract (D-19); cannot be simulated locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Cold-start UX masking real outage | mitigate (via M2 verification) | M2 verifies the "Warming up — ~4 minutes after idle" copy renders at T=10s. If it does NOT appear (timer leak, missing CSS, wrong copy), the user has no honest signal during the 225s wait — degrades into the "looks broken" anti-pattern. M2 is the live gate. |
| T-06-05-02 | Information Disclosure | CHAT-06 chat-state leak via OTHER subsystem (MSAL, Sonner, future widget) | mitigate (via M5 manual inspection) | Plan 03 storage spy enforces useChatStream itself never writes; Plan 04 grep enforces no localStorage in components/chat/. But M5 confirms NO OTHER code path (Phase 4 MSAL, Phase 5 widgets, third-party libs) leaks chat-related state into storage. Application-tab inspection is the integration verification. |
| T-06-05-03 | DoS | Rapid Stop+Submit cycles racing AbortController | mitigate (via M4 verification + Plan 03 unit tests) | M4 verifies the live UX: Stop click halts streaming + re-enables composer + next submit works. If Plan 03 Pitfall B unit test passed but M4 reveals a race (e.g., orphaned reader, double-fire on rapid clicks), it's a live-only failure mode requiring gap closure. |
| T-06-05-04 | A11y regression (screen reader) | accept (manual spot-check) | Plan 04 acceptance criteria verify `aria-live="polite"`, `aria-relevant="additions"`, `aria-hidden` on cursor, `role="log"` etc. M-markers don't include a full VoiceOver/NVDA exercise (out of scope for v1 single-user demo path); revisit if Phase 8 expands a11y testing. |
| T-06-05-05 | Demoability subjective failure | accept | M6's "≤30s demoable" is a portfolio claim verified by Adrian as the demo author. If the recording is technically valid but reads as boring/unclear, that's a Phase 8 polish concern, not a Phase 6 blocker. |
</threat_model>

<verification>
After Adrian completes Task 1's resume signal AND Claude completes Task 2's write:

1. **Verify 06-UAT.md committed:**
   ```bash
   git log --oneline -5 .planning/phases/06-chat/06-UAT.md
   ls -la .planning/phases/06-chat/06-UAT.md
   ```

2. **Verify all 6 markers have a verdict:**
   ```bash
   grep -cE "^.\| Result \| (PASS|FAIL|DEFERRED)" .planning/phases/06-chat/06-UAT.md
   # Expected: 6 (one per M-marker table)
   ```

3. **Verify all CHAT-* requirements have a verdict:**
   ```bash
   grep -cE "^\| CHAT-0[1-6] \|.*\| (PASS|FAIL)" .planning/phases/06-chat/06-UAT.md
   # Expected: 6
   ```

4. **Verify all 5 ROADMAP success criteria have a verdict:**
   ```bash
   grep -cE "^\| [1-5] \|.*\| (PASS|FAIL)" .planning/phases/06-chat/06-UAT.md | head -5
   # Expected: 5
   ```

5. **Verify zero remaining placeholders:**
   ```bash
   grep -c "<fill>" .planning/phases/06-chat/06-UAT.md
   # Expected: 0
   ```

If all PASS: Phase 6 closes. Adrian runs `/gsd-verify-work 6` to formalize.
If any FAIL: file gap-closure plans per the FAIL details OR re-run individual markers after fix.
</verification>

<success_criteria>
- `.planning/phases/06-chat/06-UAT.md` committed with 6 M-marker tables filled (no `<fill>` placeholders remaining)
- All 6 markers (M1 happy path, M2 cold-start, M3 tool chip + Dialog, M4 Stop mid-stream, M5 zero residue, M6 ≤30s recording) have a verdict (PASS / FAIL / DEFERRED)
- All 6 CHAT-* requirements (CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06) have a Status entry in the closure table
- All 5 ROADMAP §Phase 6 success criteria have a Result entry
- ≤30s screen recording captured (M6 artifact in `.planning/phases/06-chat/` or referenced screenshots)
- Application-tab screenshot proving zero chat-related storage keys (M5 artifact)
- Adrian's resume signal received ("all markers pass" OR "issue: <details>")
- Overall verdict reflects the marker statuses (PASS / PASS WITH MINOR DEVIATIONS / FAIL)
- Phase 6 ready for `/gsd-verify-work 6` formal closure (when all markers PASS)
</success_criteria>

<output>
After Task 2 commits 06-UAT.md, no further Claude-authored artifacts beyond the UAT file itself.

If all markers PASS: Update ROADMAP.md Phase 6 status from `[ ]` to `[x]` with completion date + link to 06-UAT.md. Update Progress Table row for Phase 6 to "✓ Plans complete, UAT PASSED <date> (pending /gsd-verify-work)". Update STATE.md current focus to reflect Phase 6 completion + Phase 7 (Profile & Resume Upload) as the next parallel-eligible phase.

If any marker FAILS: Document the failure mode in 06-UAT.md + create a gap closure ticket OR direct gap-closure plan via `/gsd-plan-phase 6 --gaps`.

Phase 6 closes on all-PASS confirmation.
</output>
</content>