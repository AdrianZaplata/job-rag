---
phase: 2
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - .planning/phases/02-corpus-cleanup/02-04-SUMMARY.md
autonomous: false
requirements:
  - CORP-01
  - CORP-02
  - CORP-03
  - CORP-04
must_haves:
  truths:
    - "Pre-flight `pg_dump $DATABASE_URL > .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql` produced a non-empty SQL file before reextract ran (manual safety net per Open Q3 / Claude's Discretion)"
    - "`job-rag reextract --dry-run` immediately after Plan 03 reported `Selected: 108` (every existing posting is stale because Plan 03 bumped PROMPT_VERSION to 2.0 + migration 0004 left 108 rows at prompt_version='1.1')"
    - "`job-rag reextract` (default — no --all) ran to completion against the 108-posting corpus; per-posting commit pattern means partial failures are reported but do not abort"
    - "The 4 SQL sanity checks from VALIDATION.md / 02-RESEARCH.md (CORP-01..CORP-04) all pass against the post-reextract dev DB"
    - "CORP-01: zero rows in job_requirements where lower(skill) is in REJECTED_SOFT_SKILLS"
    - "CORP-02: skill_category column has exactly 3 distinct values (hard/soft/domain), no NULL bucket"
    - "CORP-03: every job_postings row has either location_country populated OR location_country IS NULL with location_region populated; both-null bucket = 0"
    - "CORP-04: `job-rag list --stats` prints `prompt_version=2.0: 108` and no other version rows"
    - "FastAPI lifespan startup, when triggered post-reextract, logs `prompt_version_check_clean` (info) — proves the drift surface flips clean once the corpus is current"
    - "Phase 2 SUMMARY documents the actual cost (typer-echoed total cost from the reextract command), the actual succeeded/failed counts, any per-posting failures with their error strings, and any deviations from the dry-run plan"
  artifacts:
    - path: ".planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql"
      provides: "pg_dump safety net (NOT committed to git — local artifact only; .gitignore should already exclude .planning/phases/**/*.sql or it gets added in this plan)"
      contains: "PostgreSQL database dump"
    - path: ".planning/phases/02-corpus-cleanup/02-04-SUMMARY.md"
      provides: "Final phase 2 closure document with run results, sanity check outputs, and any deviations"
      contains: "## Run Results"
  key_links:
    - from: ".planning/phases/02-corpus-cleanup/02-04-SUMMARY.md"
      to: "src/job_rag/services/extraction.py::reextract_stale"
      via: "documents the actual ReextractReport this run produced"
      pattern: "ReextractReport"
---

<objective>
Execute the corpus refresh: run `pg_dump` for safety, run `job-rag reextract --dry-run` to verify Selected==108, run `job-rag reextract` against the 108 existing postings, then validate via the 4 SQL sanity checks (CORP-01..CORP-04). Capture run output, costs, and any per-posting failures in `02-04-SUMMARY.md`. This is the only plan in Phase 2 that actually mutates the corpus content; the previous three plans put the schema, prompt, service, and CLI surfaces in place.

Purpose: ROADMAP §Phase 2 success criteria 1-4 close here. CORP-01 (no soft-skill noise), CORP-02 (skill_category distribution), CORP-03 (structured location), CORP-04 (every row at PROMPT_VERSION='2.0'). After this plan: the dashboard plan (Phase 5 / DASH-01..DASH-06) has the clean structured corpus it expects.

Output: 1 backup file (gitignored), 1 SUMMARY document with the actual run numbers + the 4 SQL sanity check outputs.
</objective>

<execution_context>
@C:/Users/adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-CONTEXT.md
@C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-RESEARCH.md
@C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-VALIDATION.md
@C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-01-PLAN.md
@C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-02-PLAN.md
@C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-03-PLAN.md
@C:/Users/adria/Documents/Repo/Career/job-rag/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight pg_dump backup of the dev DB before any reextract runs</name>
  <files>.planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql</files>
  <action>Adrian runs the `pg_dump` command shown in &lt;how-to-verify&gt; from the project root (`C:/Users/adria/Documents/Repo/Career/job-rag`), then types the resume-signal once verified. Claude does NOT run pg_dump itself (network + DB credentials live in Adrian's environment).</action>
  <verify>Adrian visually confirms the file size, header content, and gitignore status per the steps in &lt;how-to-verify&gt;.</verify>
  <done>Adrian replied "backup-OK" and the file exists at .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql.</done>
  <what-built>
    Plan 03 has landed: dev DB is on alembic 0004, all 108 postings are at prompt_version='1.1' (now considered stale because PROMPT_VERSION="2.0" in code), location_country/city/region columns exist but are NULL across all rows, skill_type/skill_category columns are populated (rename + SQL CASE backfill ran during the migration). The reextract service is wired and tested.

    Before any LLM-driven write hits the dev DB, this checkpoint ensures Adrian has a `pg_dump` snapshot to roll back to. The dump captures the post-0004 schema with 108 rows whose location_* columns are still NULL — a known-good "after-migration, before-reextract" state.
  </what-built>
  <how-to-verify>
    Adrian runs the following from the project root (`C:/Users/adria/Documents/Repo/Career/job-rag`):

    ```bash
    pg_dump "$DATABASE_URL" > .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql
    ```

    On Windows where `$DATABASE_URL` may need cmd-style expansion or the password contains URL-special chars (Phase 1 lesson — Adrian's password contains `&%!$`), Adrian may instead use the explicit form:

    ```bash
    pg_dump --host=localhost --port=5432 --username=postgres --dbname=job_rag \
        --no-password \
        > .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql
    ```

    (substituting actual host/port/dbname from his `.env`).

    Then verify:
    1. The file exists and is non-empty: `ls -la .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql` shows a size > 100 KB (108 postings + raw_text → ~1-5 MB typical).
    2. The file contains expected schema: `head -50 .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql` shows `CREATE TABLE` statements for `job_postings`, `job_requirements`, `users`, `user_profile`, `job_chunks`, `alembic_version`.
    3. The `.gitignore` excludes the file (or this plan adds an entry). Confirm via `git check-ignore .planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql` — should print the path (= ignored). If NOT ignored, append `*.sql` (or the specific path) to `.gitignore` before proceeding.

    Confirmation message to type: "backup-OK" once steps 1-3 verified.
  </how-to-verify>
  <resume-signal>Type "backup-OK" once the pg_dump file exists, is non-empty, contains the expected CREATE TABLE statements, and is gitignored. If pg_dump fails (auth, connection, or disk space), describe the error and Claude will help resolve before proceeding.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run `job-rag reextract --dry-run` to verify Selected==108 (the migration left every row stale; expected baseline before mutating writes)</name>
  <files>(no file modifications — read-only verification)</files>
  <read_first>
    - C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-03-PLAN.md (Task 3 — confirms reextract subcommand and --dry-run flag exist)
    - C:/Users/adria/Documents/Repo/Career/job-rag/src/job_rag/services/extraction.py (Plan 03 produced; sanity-check the function exists)
  </read_first>
  <action>
    From the project root:

    ```bash
    cd C:/Users/adria/Documents/Repo/Career/job-rag
    uv run job-rag reextract --dry-run
    ```

    Capture the entire stdout. Expected shape (per Plan 03 Task 3 echo statements):

    ```
    Re-extraction complete (PROMPT_VERSION=2.0):
      Selected:    108
      Succeeded:   0
      Failed:      0
      Skipped:     108
      Total cost:  $0.0000
    ```

    Hard requirement: `Selected: 108` (every row in dev DB is stale relative to PROMPT_VERSION="2.0"). If `Selected: 0`, something went wrong with PROMPT_VERSION bump in Plan 02 OR the migration didn't apply OR the dev DB is empty — abort and investigate before Task 3.

    If `Selected: <X>` where 0 < X < 108, that means a fraction of postings are already at "2.0" (e.g., a partial reextract from a previous attempt). This is acceptable — `reextract` is idempotent by design (D-14). Proceed but note the discrepancy in the SUMMARY.

    Save the captured output to a temp variable / scratch file in your context for Task 4's SUMMARY synthesis.
  </action>
  <verify>
    <automated>cd C:/Users/adria/Documents/Repo/Career/job-rag &amp;&amp; uv run job-rag reextract --dry-run | grep -E "^\s*(Selected|Skipped):" | head -2</automated>
  </verify>
  <done>
    - `job-rag reextract --dry-run` exits 0.
    - Output contains `Selected: 108` (or a documented smaller number with reason).
    - Output contains `Skipped: <Selected>` (dry-run skips equals selected).
    - No mutations made to the DB (dry_run=True path in services/extraction.py never reaches the per-row UPDATE branch).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run `job-rag reextract` (default — stale-only, NOT --all) against the 108-posting corpus; expect ~3-5 minutes wall time and ~€0.20 total LLM cost</name>
  <files>(no file modifications — runs the LLM pipeline against the dev DB)</files>
  <read_first>
    - C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-RESEARCH.md §Open Question 2 (lines 1437-1440 — use DEFAULT not --all for the corpus refresh; idempotent and predictable) and Gotcha G (lines 1423-1430 — sequential by design under 60-RPM)
  </read_first>
  <action>
    From the project root, run the full corpus reextract using DEFAULT selection (NOT `--all`):

    ```bash
    cd C:/Users/adria/Documents/Repo/Career/job-rag
    uv run job-rag reextract 2>&1 | tee .planning/phases/02-corpus-cleanup/reextract-run.log
    ```

    The `tee` captures the full structlog output + the final typer-echo'd report block to a log file (NOT committed; .gitignore should exclude it via the same `*.log` rule that already exists for Phase 1 artifacts — confirm with `git check-ignore` if uncertain).

    Wall time: ~3-5 minutes (108 postings × 1-3s LLM round-trip per posting; sequential). If a single posting takes >30s, tenacity retries kick in (3 attempts, exp backoff 1-10s) — wait it out. The log lines `reextract_started`, `reextract_posting_complete` (one per success), `reextract_failed` (one per failure), `reextract_complete` (final summary) all flow to the log.

    On completion, capture:
    - The final typer-echo'd report:
      ```
      Re-extraction complete (PROMPT_VERSION=2.0):
        Selected:    <N>     # should match Task 2's dry-run number
        Succeeded:   <S>
        Failed:      <F>
        Skipped:     0       # not a dry-run
        Total cost:  $<C>    # expect ~$0.20 ± $0.10 for 108 postings
      ```
    - The `Failures:` section if `F > 0`, with each `<posting_id>: <error>` line.

    Per Gotcha F: do NOT run `job-rag ingest` or `job-rag agent --stream` in another shell while reextract is running — share the B1ms/local-postgres connection pool sequentially.

    If `F > 0` (any per-posting failures): the loop continued past them by design (D-16). Re-running the SAME `job-rag reextract` command will idempotently retry only the still-stale rows (the failed ones, since they didn't UPDATE prompt_version). Adrian can manually run again until F=0 or accept the residual failures with a documented reason in the SUMMARY (e.g., a malformed posting raw_text that the LLM cannot parse even on retry).

    If `F` exceeds 10: stop, investigate the failures (likely an LLM/prompt issue or a Pydantic validation problem with the new Location/skill_type schema), and roll back from `pre-phase-2-backup.sql` if needed. Do NOT keep firing retries blindly.

    Note: the structlog console output may include a fair amount of `reextract_posting_complete` lines (one per successful posting). This is intentional observability per the project's logging convention — tail or pipe to `wc -l` for a count.
  </action>
  <verify>
    <automated>cd C:/Users/adria/Documents/Repo/Career/job-rag &amp;&amp; uv run psql "$DATABASE_URL" -t -A -c "SELECT COUNT(*) FROM job_postings WHERE prompt_version='2.0'"</automated>
  </verify>
  <done>
    - `job-rag reextract` exited 0.
    - The captured report shows `Selected == Succeeded + Failed`. Skipped == 0.
    - The verification psql command returns a number ≥ (Selected - Failed). Ideally 108 (full success); acceptable ≥ 100 with a documented reason for residual failures in the SUMMARY.
    - Per-posting failures (if any) recorded in `02-04-SUMMARY.md` along with their error strings.
    - Total cost reported by the CLI is captured for the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run the 4 SQL sanity checks (CORP-01..CORP-04) against the post-reextract dev DB; capture the outputs into 02-04-SUMMARY.md</name>
  <files>.planning/phases/02-corpus-cleanup/02-04-SUMMARY.md</files>
  <read_first>
    - C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-VALIDATION.md (lines 81-84 — Manual-Only Verifications table with the 4 SQL queries)
    - C:/Users/adria/Documents/Repo/Career/job-rag/.planning/phases/02-corpus-cleanup/02-RESEARCH.md (lines 1318-1357 — the 4 SQL Sanity Checks block with EXPECTED outcomes)
    - C:/Users/adria/.claude/get-shit-done/templates/summary.md (the SUMMARY template the executor uses)
  </read_first>
  <action>
    Run each of the 4 SQL queries below against the dev DB and record the actual output. Use `uv run psql "$DATABASE_URL"` if `psql` is on PATH; otherwise use the equivalent Python:

    ```bash
    uv run python -c "
    from sqlalchemy import create_engine, text
    from job_rag.config import settings
    eng = create_engine(settings.database_url, pool_pre_ping=True, future=True)
    with eng.connect() as c:
        rows = c.execute(text(\"\"\"<sql>\"\"\")).all()
        for row in rows: print(row)
    "
    ```

    **CORP-01 (REJECTED_SOFT_SKILLS — expect zero rows):**
    ```sql
    SELECT skill, COUNT(*)
    FROM job_requirements
    WHERE skill ILIKE ANY(ARRAY[
        'communication', 'teamwork', 'problem-solving', 'problem solving',
        'analytical thinking', 'critical thinking', 'time management',
        'work ethic', 'ownership mindset', 'ownership',
        'attention to detail', 'detail-oriented',
        'self-motivated', 'self-starter',
        'customer focus', 'customer obsession',
        'passion', 'drive', 'attitude', 'mindset',
        'adaptability', 'flexibility'
    ])
    GROUP BY skill
    ORDER BY COUNT(*) DESC;
    ```
    EXPECTED: zero rows. If any rows return → either the prompt rejection didn't fire (LLM extracted a rejected term anyway) or our REJECTED_SOFT_SKILLS list missed a variant. Document each violation in SUMMARY with the offending skill string + count.

    **CORP-02 (skill_category distribution — expect 3 buckets, no NULL):**
    ```sql
    SELECT skill_category, COUNT(*)
    FROM job_requirements
    GROUP BY skill_category
    ORDER BY COUNT(*) DESC;
    ```
    EXPECTED: 3 rows: `hard | <large N>`, `domain | <medium N>`, `soft | <small N>`. NO null row. The exact numeric distribution depends on the corpus; reasonable shape: hard ~70-80% of total, domain ~10-15%, soft ~5-10% (per D-20 — only borderline differentiators end up `soft`).

    Also run a NULL guard:
    ```sql
    SELECT COUNT(*) FROM job_requirements WHERE skill_category IS NULL;
    ```
    EXPECTED: 0.

    **CORP-03 (location_country present OR location_country=NULL with location_region populated — expect both_null=0):**
    ```sql
    SELECT
        SUM(CASE WHEN location_country IS NOT NULL THEN 1 ELSE 0 END) AS country_present,
        SUM(CASE WHEN location_country IS NULL AND location_region IS NOT NULL THEN 1 ELSE 0 END) AS country_null_region_present,
        SUM(CASE WHEN location_country IS NULL AND location_region IS NULL THEN 1 ELSE 0 END) AS both_null
    FROM job_postings;
    ```
    EXPECTED: country_present + country_null_region_present == total postings (108 in the typical case, fewer if some postings failed reextract). both_null == 0.

    Also verify alpha-2 invariant:
    ```sql
    SELECT location_country, COUNT(*)
    FROM job_postings
    WHERE location_country IS NOT NULL AND LENGTH(location_country) != 2
    GROUP BY location_country;
    ```
    EXPECTED: zero rows. If non-2-letter codes appear, the LLM didn't comply with the ISO-3166 alpha-2 prompt instruction — record in SUMMARY as a known data-quality issue (Phase 5's dashboard tolerates surprises by filtering on known countries; Open Q5 mitigation).

    **CORP-04 (every row at PROMPT_VERSION='2.0'):**
    ```sql
    SELECT prompt_version, COUNT(*)
    FROM job_postings
    GROUP BY prompt_version
    ORDER BY COUNT(*) DESC;
    ```
    EXPECTED: one row, `2.0 | <total postings>`. If a `1.1 | <residual>` row remains, those are the postings that failed reextract (Task 3's `Failed` count). Document them in SUMMARY.

    Also confirm via the CLI surface:
    ```bash
    uv run job-rag list --stats
    ```
    EXPECTED output contains `prompt_version=2.0: <N>` with no STALE marker on any row. If any row carries the STALE marker, the residual is the same set of failures from Task 3.

    **Post-reextract lifespan drift check (proves the FastAPI startup warning flips clean):**
    ```bash
    cd C:/Users/adria/Documents/Repo/Career/job-rag
    timeout 10 uv run uvicorn job_rag.api.app:app --host 127.0.0.1 --port 8765 --log-level info 2>&1 | grep -E "prompt_version_(drift|check_clean|check_failed)" | head -3
    ```
    EXPECTED: at least one line containing `prompt_version_check_clean` (info-level structlog event from the lifespan). If `prompt_version_drift` appears instead, the residual stale rows are still detected — same failures from Task 3.

    Now CREATE `.planning/phases/02-corpus-cleanup/02-04-SUMMARY.md` with this structure (use the project's standard SUMMARY template + Phase 2-specific sections):

    ```markdown
    ---
    phase: 2
    plan: 04
    requirements_closed: [CORP-01, CORP-02, CORP-03, CORP-04]
    completed: <YYYY-MM-DD>
    ---

    # Plan 02-04 — Corpus Refresh Run Results

    ## Pre-flight

    - pg_dump backup at `.planning/phases/02-corpus-cleanup/pre-phase-2-backup.sql`: <size> bytes, taken <timestamp>.
    - Pre-reextract dry-run (Task 2): `Selected: <N>` (expected 108 unless prior partial run).

    ## Reextract Run (Task 3)

    | Metric | Value |
    |---|---|
    | Selected | <N> |
    | Succeeded | <S> |
    | Failed | <F> |
    | Skipped | 0 |
    | Total cost | $<C> |
    | Wall time | <minutes>m <seconds>s |

    Failures (if any):
    | posting_id | error |
    |---|---|
    | <uuid> | <error string> |

    Strategy for residual failures: <e.g., "re-ran twice, 2 postings stuck on Pydantic validation for missing salary_period — accepted residual; raw_text examined manually showed truncated source markdown"> OR "F=0, no residuals".

    ## SQL Sanity Check Results

    ### CORP-01 — REJECTED_SOFT_SKILLS noise
    Query: <verbatim from above>
    Result: <pasted output>
    Status: <PASS / FAIL with explanation>

    ### CORP-02 — skill_category distribution
    Query: <verbatim>
    Result:
    ```
    skill_category | count
    hard           | <N>
    domain         | <N>
    soft           | <N>
    ```
    NULL guard: <0>
    Status: <PASS / FAIL>

    ### CORP-03 — location_country / location_region presence
    Query: <verbatim>
    Result:
    ```
    country_present | country_null_region_present | both_null
    <X>             | <Y>                         | 0
    ```
    Alpha-2 invariant: <0 violations / list any non-2-letter codes>
    Status: <PASS / FAIL>

    ### CORP-04 — PROMPT_VERSION distribution
    Query: <verbatim>
    Result:
    ```
    prompt_version | count
    2.0            | <N>
    ```
    `job-rag list --stats` output: <pasted>
    Status: <PASS / FAIL>

    ## Lifespan Drift Surface

    Output from FastAPI cold-start post-reextract: <e.g., "prompt_version_check_clean current=2.0">.
    Status: <PASS / FAIL>

    ## Deviations from Plan

    <Anything that didn't go as predicted: cost above estimate, longer wall time, residual failures, alpha-2 anomalies, etc. Empty section is also valid if everything ran clean.>

    ## Phase Closure

    - [x] CORP-01 closed.
    - [x] CORP-02 closed.
    - [x] CORP-03 closed.
    - [x] CORP-04 closed.

    Phase 2 ready for `/gsd-verify-work 2`.
    ```

    Fill in every `<placeholder>` with actual run output. Do NOT leave any `<...>` in the final document.
  </action>
  <verify>
    <automated>cd C:/Users/adria/Documents/Repo/Career/job-rag &amp;&amp; uv run python -c "from sqlalchemy import create_engine, text; from job_rag.config import settings; eng = create_engine(settings.database_url, pool_pre_ping=True, future=True); con = eng.connect(); rows = con.execute(text(\"SELECT prompt_version, COUNT(*) FROM job_postings GROUP BY prompt_version\")).all(); con.close(); print(rows); cleaned = [(v, n) for v, n in rows if v == '2.0']; assert cleaned and cleaned[0][1] >= 100, f'CORP-04 incomplete: {rows}'"</automated>
  </verify>
  <done>
    - All 4 SQL sanity checks ran and their actual output captured.
    - `02-04-SUMMARY.md` created with all sections filled in (no remaining `<...>` placeholders).
    - At least 100 of the 108 postings carry `prompt_version='2.0'` post-reextract (>92% completion threshold; full 108 is the goal).
    - All 3 status fields (CORP-01, CORP-02, CORP-03, CORP-04) record either PASS or a documented FAIL+reason.
    - Lifespan drift surface verified to emit `prompt_version_check_clean`.
    - Verify command exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human review of 02-04-SUMMARY.md and the 4 sanity check results before phase closure</name>
  <files>.planning/phases/02-corpus-cleanup/02-04-SUMMARY.md</files>
  <action>Adrian reads 02-04-SUMMARY.md (produced by Task 4) and confirms each of the 7 review points listed in &lt;how-to-verify&gt;. If any check fails, Adrian invokes the appropriate resume-signal variant.</action>
  <verify>Adrian's response to the resume-signal — "approved" / "investigate &lt;CORP-XX&gt;" / "rerun" — drives the next action.</verify>
  <done>Adrian replied "approved". Phase 2 is ready for /gsd-verify-work 2.</done>
  <what-built>
    Tasks 1-4 ran the corpus refresh end-to-end and produced `02-04-SUMMARY.md` with the actual numbers. This checkpoint asks Adrian to confirm the run looks right before `/gsd-verify-work 2` runs.
  </what-built>
  <how-to-verify>
    Adrian opens `.planning/phases/02-corpus-cleanup/02-04-SUMMARY.md` and confirms:

    1. The reextract metrics block has `Succeeded` ≥ 100 (and ideally 108) — small residual failures are tolerable if explained.
    2. Cost is in the expected range ($0.10–$0.40 — 2x slack on the €0.20 estimate).
    3. CORP-01 status is PASS (zero rejected-term rows). If FAIL, the LLM extracted a rejected term — Adrian decides whether to:
       - Accept (rare term, small count, document and move on)
       - Re-run reextract on those specific posting IDs (Task 3's `--posting-id` form)
       - Add the missed term to `REJECTED_SOFT_SKILLS` and bump PROMPT_VERSION (would be a v2.1 follow-up — out of scope for Phase 2 closure).
    4. CORP-02 distribution looks reasonable (hard >> soft + domain). A heavily-soft distribution would suggest the rejection rules under-fired.
    5. CORP-03 has both_null=0. If non-zero, those postings have raw_text that doesn't mention any geography — manually review to decide.
    6. CORP-04 shows `prompt_version=2.0` is the only row (or near-only with documented residuals).
    7. Random spot check: `uv run job-rag list --stats` output looks sensible. `uv run job-rag stats` (the existing skill-frequency command) still runs without crash (Plan 03 swept it for `req.skill_type`).

    If any check FAILs and is not a documented acceptable residual, Adrian can ask Claude to investigate before signing off.
  </how-to-verify>
  <resume-signal>Type "approved" if 02-04-SUMMARY.md looks correct and the sanity checks pass (or have documented acceptable residuals). Type "investigate <CORP-XX>" to dig deeper on a specific failure. Type "rerun" to invoke `job-rag reextract` again before closure (handles transient LLM-rate-limit residuals).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reextract loop → 108 LLM calls | Real OpenAI spend (~€0.20). The `--all` guard rail does NOT apply here (this plan uses DEFAULT selection per Open Q2). |
| Reextract loop → DB writes | Per-posting commit means partial state IS the safety mode. Pre-flight pg_dump (Task 1 checkpoint) is the rollback surface for catastrophic failure. |
| SUMMARY write → planning tree | The SUMMARY contains the actual cost, posting failure strings, and any residual data anomalies. Already-sanitized: error strings come from `str(e)` (not tracebacks per Plan 03's logging convention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Bad LLM output corrupts row | mitigate | Per-posting commit + Pydantic validation in `extract_posting`. Failures are caught by `_reextract_one`'s try/except — log+continue, never abort. Pre-flight pg_dump enables full rollback if needed. |
| T-02-04-02 (T-COST-01) | Repudiation/DoS | Cost spike | mitigate | DEFAULT selection (not --all) caps work at exactly the stale row count. Per-posting commit means partial run is recoverable; if cost climbs unexpectedly mid-run, Adrian can Ctrl-C and the committed-so-far rows persist. |
| T-02-04-03 | Information Disclosure | SUMMARY leaks posting content | accept | Failure strings are `str(e)` (e.g., "Pydantic validation: location.country must be string, got None"). No raw posting content, no PII, no API keys. Adrian's own posting corpus is private — SUMMARY stays in `.planning/` (already inside the repo, not published). |
| T-02-04-04 | DoS (concurrent ingest) | Phase 2 reextract overlaps with `job-rag ingest` | accept | Documented in Plan 03 SUMMARY (Gotcha F): "Run reextract when no other write traffic is in flight." Adrian is single-user; this is a coordination convention, not a technical lock. |
</threat_model>

<verification>
After Tasks 1-5 complete:

1. `pre-phase-2-backup.sql` exists and is gitignored.
2. `job-rag reextract --dry-run` post-Plan-04 reports `Selected: 0` (every row now at PROMPT_VERSION='2.0', proving the run took effect).
3. `02-04-SUMMARY.md` exists, all sections filled in, all 4 CORP statuses are either PASS or documented acceptable residuals.
4. `uv run pytest -x -q` (full suite) STILL passes — no regressions.
5. Adrian responds "approved" to Task 5's checkpoint.

If any of these fail at verify time, Adrian can roll back via `psql "$DATABASE_URL" < pre-phase-2-backup.sql` and the entire plan-04 run can be retried (the migration in Plan 03 still applies; only the reextract data is rolled back). The full Phase 2 schema does NOT need to be reset.
</verification>

<success_criteria>
- [ ] Pre-flight `pg_dump` backup created and gitignored (Task 1).
- [ ] `job-rag reextract --dry-run` reported the expected 108-row baseline (Task 2).
- [ ] `job-rag reextract` ran to completion; ≥100 of 108 postings now at PROMPT_VERSION='2.0' (Task 3).
- [ ] All 4 SQL sanity checks (CORP-01, CORP-02, CORP-03, CORP-04) executed; results captured in 02-04-SUMMARY.md (Task 4).
- [ ] FastAPI lifespan drift surface flipped clean (`prompt_version_check_clean` event observed).
- [ ] Adrian approved the SUMMARY at the human-verify checkpoint (Task 5).
- [ ] Phase 2 ready for `/gsd-verify-work 2`.
</success_criteria>

<output>
The SUMMARY is `02-04-SUMMARY.md` itself (created by Task 4) — this plan's deliverable IS the summary document. After Task 5 approval, Phase 2 is closed pending `/gsd-verify-work 2`.
</output>
