---
phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-
plan: 04
subsystem: infra
tags: [terraform, azure, container-apps, key-vault, verification, ci-cd]

requires:
  - phase: 06.1-01
    provides: KV `lifecycle.ignore_changes = [value, value_wo, value_wo_version]` on all 4 out-of-band-seeded secrets
  - phase: 06.1-02
    provides: cpu/memory parameterization across compute module + prod env + prod.tfvars defaults (1.0/2Gi)
  - phase: 06.1-03
    provides: dual-var-file wrapper + README convention + CI pre-apply guard for auth env vars
provides:
  - "Live `terraform plan -detailed-exitcode` PASS proof against prod state (exit 0 = no changes)"
  - "Captured KV secret timestamps baseline for future regression detection"
  - "Live ACA container.resources + auth env-var lengths captured as documentation"
  - "In-flight Plan 03 bug discovered + fixed: CI pre-apply guard referenced wrong container app name"
affects: [phase-07-profile, phase-08-eval, future-terraform-applies]

tech-stack:
  added: []
  patterns:
    - "D-08 verification pattern: capture live BEFORE state + run -detailed-exitcode + commit evidence file before claiming a TF refactor is shippable"

key-files:
  created:
    - .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md
  modified:
    - .github/workflows/deploy-infra.yml  # Plan 03 follow-up: jobrag-prod-aca → jobrag-prod-api

key-decisions:
  - "Wrapper missing-file detection test SKIPPED — renaming prod.tfvars.local on operator workstation risks bricking the next apply; the wrapper guard code is reviewable via grep without runtime test"
  - "ephemeralStorage=4Gi in live container.resources is Azure's automatic Consumption-profile allocation for cpu=1.0/memory=2Gi — not managed by Plan 02; terraform plan correctly ignores it and exit code 0 confirms no-op"
  - "Investigation step revealed the actual ACA name is jobrag-prod-api (env-first) not jobrag-prod-aca; Plan 03's CI pre-apply guard was patched in commit cdf3ba3 before this evidence file was committed"

patterns-established:
  - "Live-state verification gate (D-08): before closing any TF refactor phase, capture live BEFORE state for each affected resource + run plan -detailed-exitcode; exit 0 alone is the no-op proof"
  - "Discovered-typo-in-flight pattern: when verification catches a typo introduced by an earlier plan in the same phase, fix as a sibling commit on the originating plan (not a new plan/phase) and document in the evidence file"

requirements-completed: []

duration: 25min
completed: 2026-05-26
---

# Phase 06.1 Plan 04: Pre-Flight Verification Summary

**Live `terraform plan -detailed-exitcode` PASS against prod — Plans 01+02+03 produce zero drift; in-flight typo in CI pre-apply guard caught and fixed before phase close.**

## Performance

- **Duration:** ~25 min (including interactive verification + investigation + follow-up fix)
- **Started:** 2026-05-26T09:00:00Z (approximately, after Plan 03 completion)
- **Completed:** 2026-05-26T09:25:00Z (approximately, with evidence file committed)
- **Tasks:** 1/1 (checkpoint:human-action)
- **Files modified:** 2 (1 new evidence file + 1 deploy-infra.yml fix-up)

## Accomplishments

- Live `bash scripts/tf-apply-prod.sh plan -detailed-exitcode` → exit 0 against prod state
- All 4 KV secret last-updated timestamps captured as baseline (openai/langfuse-public/langfuse-secret/seeded-user-entra-oid)
- Live ACA `container.resources` confirmed `{cpu: 1.0, memory: "2Gi"}` matching Plan 02 defaults
- All 3 auth env vars (BACKEND_AUDIENCE/ENTRA_TENANT_ID/ENTRA_TENANT_SUBDOMAIN) confirmed present and non-empty on live container
- Caught and fixed a typo Plan 03 inherited from the CONTEXT.md decision: the CI pre-apply guard referenced a non-existent container app name (`jobrag-prod-aca`); the actual name is `jobrag-prod-api`. Without this fix the entire D-10 Bug #3 closure would have been silently bypassed on every CI apply.

## Task Commits

1. **Plan 03 follow-up — correct container app name in CI pre-apply guard** — `cdf3ba3` (fix)
2. **Capture D-08 verification evidence** — `e3a2037` (docs)

## Files Created/Modified

- `.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md` — NEW; 85-line evidence file with prerequisites, wrapper sanity, BEFORE-state capture, plan exit code, PASS conclusion
- `.github/workflows/deploy-infra.yml` — 2-line fix: `jobrag-prod-aca` → `jobrag-prod-api` in the pre-apply guard's `az containerapp show` + first-apply-skip notice

## Decisions Made

- **Investigation before re-creation.** When `az containerapp show --name jobrag-prod-aca` returned `ResourceNotFound`, ran `az containerapp list` + `az resource list -g jobrag-prod-rg` before assuming the container app had been torn down. Discovered the actual name (`jobrag-prod-api`) and avoided an unnecessary apply cycle.
- **Skip the missing-prod.tfvars.local smoke test.** The plan offered an optional rename-and-restore test of the wrapper's exit-2 path. Skipped because renaming the operator's working `prod.tfvars.local` on a busy workstation risks the next apply running without it if the restore step is interrupted; the wrapper guard logic is short enough to review by reading the script.
- **Treat `ephemeralStorage: 4Gi` as Azure-managed, not a Plan 02 deviation.** Plan 02 declares only cpu + memory; the live container shows an extra ephemeralStorage field that Azure auto-allocates per Consumption profile. Confirmed no-op via `terraform plan -detailed-exitcode = 0` — TF is not managing this field, so it never appears in a diff. Documented in the evidence file.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] CI pre-apply guard referenced non-existent container app name**
- **Found during:** Task 1 (live verification — `az containerapp show --name jobrag-prod-aca` returned ResourceNotFound)
- **Issue:** Plan 03's CI pre-apply guard step (added in commit `49adea3`) used `jobrag-prod-aca` as the container app name. The actual TF resource name from `infra/modules/compute/main.tf:23` is `jobrag-${var.env}-api` = `jobrag-prod-api`. The wrong name would have made the guard's first-apply heuristic (`ENV_JSON == "[]"`) trigger on every CI run, silently skipping the guard entirely — defeating the whole D-10 Bug #3 closure.
- **Fix:** Plan 03 sibling commit `cdf3ba3` updates both occurrences (the `az containerapp show --name` line and the `::notice::Container app ... not found` message) to `jobrag-prod-api`. YAML still parses (`uv run python -c "import yaml; yaml.safe_load(...)"` exits 0).
- **Files modified:** `.github/workflows/deploy-infra.yml`
- **Verification:** `git diff` shows exactly 2 line changes both swapping `aca` → `api`; `infra/envs/prod/README.md:266` confirms the typo has bitten before ("Plan template typo: actual ACA app name is jobrag-prod-api (env-first)").
- **Committed in:** `cdf3ba3` (sibling fix-up commit on Plan 03)

---

**Total deviations:** 1 auto-fixed (1 missing critical caught in flight)
**Impact on plan:** The fix-up is small and surgical (2 lines). Without it Plan 03's D-10 Bug #3 closure would have been broken — the CI guard would have always reported "first-apply path" and skipped verification. Net effect: Phase 06.1's bug-closure promise actually holds in CI.

## Issues Encountered

- **Initial verification confusion: container app reported as not found.** Ran investigation commands (`az group show`, `az containerapp list`, `az resource list -g jobrag-prod-rg`) and confirmed the RG was healthy with 6 resources. The container app existed under a different name than the plan assumed. This led directly to the typo discovery + fix above.
- **No issues with the actual `terraform plan` outcome.** Exit code 0 cleanly; the only output was three pre-existing warnings (undeclared `api_audience` variable from `prod.tfvars.local`, AzureRM provider deprecations) — none in scope for Phase 06.1, none producing a diff.

## User Setup Required

None — verification was the user setup. Adrian ran `az login` to workforce tenant + had `prod.tfvars.local` populated + ran the live `terraform plan`. No external service configuration required to close out the phase.

## Next Phase Readiness

- **Phase 06.1 is shippable.** All four plans complete. The actual `bash scripts/tf-apply-prod.sh apply` against live state is now safe to run when Adrian chooses; it will be a near-no-op (state refresh only, no resource mutations expected).
- **The CI pre-apply guard is functionally correct.** Next CI run of `.github/workflows/deploy-infra.yml` will actually execute the BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN check against the real container, not silently skip it via the first-apply heuristic.
- **Phase 07 (Profile & Resume Upload) unblocked.** Phase 06.1 is decoupled from Phase 07; no cross-dependency.

---
*Phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-*
*Completed: 2026-05-26*
