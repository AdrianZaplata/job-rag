---
phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-
plan: 01
subsystem: infra

tags: [terraform, azurerm, key-vault, lifecycle, value_wo, ignore_changes, kv-secrets]

requires:
  - phase: 03-infrastructure-ci-cd
    provides: "4 azurerm_key_vault_secret resources in infra/envs/prod/main.tf with value_wo + value_wo_version=1 placeholders (introduced by commit 38f06eb, plan 03-08)"
  - phase: 06-chat
    provides: "UAT-DEBUG-HANDOFF Bugs #1 + #2 root-cause evidence — proved that lifecycle.ignore_changes = [value] does NOT cover value_wo, so subsequent terraform apply re-wrote the placeholder over out-of-band-seeded real OpenAI / Langfuse / seeded-user-oid values"

provides:
  - "All 4 KV secret resources (openai_api_key, langfuse_public_key, langfuse_secret_key, seeded_user_entra_oid) carry identical lifecycle.ignore_changes = [value, value_wo, value_wo_version]"
  - "seeded_user_entra_oid now has a lifecycle block (parity gap closed per Phase 06.1 D-02 — it had none before)"
  - "Foundation for Plan 04 (live terraform plan no-op verification) — these 4 resources MUST show zero diff against live state once Adrian re-applies"

affects: [phase-06.1-plan-02, phase-06.1-plan-03, phase-06.1-plan-04]

tech-stack:
  added: []
  patterns:
    - "AzureRM Key Vault Secret + out-of-band seeding survival contract: when value_wo is used as a placeholder write-trigger, ignore_changes MUST cover all three of {value, value_wo, value_wo_version} or each apply re-fires the placeholder write."

key-files:
  created: []
  modified:
    - infra/envs/prod/main.tf

key-decisions:
  - "Honored D-01 verbatim: ignore_changes covers all three of value, value_wo, value_wo_version on every KV secret resource."
  - "Honored D-02 verbatim: seeded_user_entra_oid got the same 3-attribute lifecycle block (it previously had none)."
  - "Used Edit replace_all on the 3 identical `ignore_changes = [value]` lines (openai_api_key, langfuse_public_key, langfuse_secret_key) rather than 3 separate targeted edits — atomic, deterministic, no risk of partial application."
  - "Did NOT touch the comment block at lines 130-136 (which contains the literal phrase `lifecycle.ignore_changes = [value]` as historical narration), nor the module \"compute\" block at lines 188+ (Plan 02 owns that region per merge_note)."

patterns-established:
  - "value_wo lifecycle protection — for any future azurerm_key_vault_secret using value_wo as an out-of-band seeding placeholder, the lifecycle block MUST be ignore_changes = [value, value_wo, value_wo_version]. [value] alone is a foot-gun."

requirements-completed: []

duration: 2min
completed: 2026-05-26
---

# Phase 06.1 Plan 01: value_wo lifecycle hardening Summary

**Added `value_wo` + `value_wo_version` to `lifecycle.ignore_changes` on all 4 out-of-band-seeded KV secrets in `infra/envs/prod/main.tf` so subsequent `terraform apply` cannot re-fire the placeholder write that wiped real OpenAI / Langfuse / seeded-user-oid values during Phase 06 UAT.**

## Performance

- **Duration:** 2 min (69s)
- **Started:** 2026-05-26T08:42:49Z
- **Completed:** 2026-05-26T08:43:58Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- 3 existing `lifecycle.ignore_changes = [value]` blocks (openai_api_key, langfuse_public_key, langfuse_secret_key) updated to `[value, value_wo, value_wo_version]`.
- 1 new `lifecycle` block added to `seeded_user_entra_oid` (parity gap closed — previously had no lifecycle block at all).
- `terraform fmt -check` exits 0; `terraform validate` exits 0 (pre-existing transitive AVM deprecation warning is out of scope).
- Diff is surgical: 7 insertions, 3 deletions, all inside lines 138-184. Zero collateral changes. Plan 02's region (lines 188+) untouched.

## Task Commits

Each task was committed atomically:

1. **Task 1: Update 3 existing lifecycle blocks + add 1 new lifecycle block** — `f66282a` (fix)

_Plan metadata commit follows separately at end of execution._

## Files Created/Modified

- `infra/envs/prod/main.tf` — 4 `azurerm_key_vault_secret` resources now carry `lifecycle.ignore_changes = [value, value_wo, value_wo_version]`. The 3 pre-existing lifecycle blocks were widened; `seeded_user_entra_oid` got its first lifecycle block.

## Decisions Made

- **D-01 + D-02 applied verbatim from `.planning/phases/06.1-.../06.1-CONTEXT.md`.** No new decisions emerged during execution — the plan was a deterministic mechanical edit and the surrounding context was already pinned by Phase 06 UAT forensics + the `terraform-value-wo-overwrites-secret.md` memory entry.

## Deviations from Plan

The plan's executor work was completed exactly as specified. Two minor noisy-acceptance-criteria observations are documented below for the verifier — neither required any code change.

### Acceptance criteria notes (no code deviation)

**1. [Note — not a deviation] Plan's `<verify><automated>` clause asserted `grep -c "value_wo_version" infra/envs/prod/main.tf` should return `4`**

- **Actual:** returns `8`.
- **Why:** each of the 4 KV secret resources references `value_wo_version` TWICE — once as the resource attribute (`value_wo_version = 1`) and once inside the new `ignore_changes = [value, value_wo, value_wo_version]` list. 4 × 2 = 8 raw grep matches. The plan author was reasoning about resources (4) but wrote the assertion as a raw line count.
- **Substantive truth:** all 4 KV secret resources still each declare `value_wo_version = 1`, and now each ALSO lists `value_wo_version` inside ignore_changes. Both are present, which is the actual requirement of D-01.
- **No fix needed.** Substantive correctness preserved.

**2. [Note — not a deviation] Plan's acceptance criteria asserted `grep -c "ignore_changes = \[value\]" infra/envs/prod/main.tf` should return `0`**

- **Actual:** returns `1`.
- **Why:** the single remaining match is on **line 135 — inside the explanatory comment block at lines 130-136**, which contains the narrating phrase `lifecycle.ignore_changes = [value]` as part of the historical description of the seeding model. The plan explicitly says "Do NOT modify the comment block at lines 130-136." All 4 *actual lifecycle blocks* were updated correctly to `[value, value_wo, value_wo_version]` (confirmed by the count-of-4 acceptance criterion, which DOES pass exactly).
- **No fix needed.** Touching the comment would violate the explicit "do not modify" instruction and the comment is accurate historical narration of the pre-PR state.

---

**Total deviations:** 0 code deviations. 2 acceptance-criteria notes for the verifier.
**Impact on plan:** None. The substantive requirements (4 lifecycle blocks each ignoring `value`, `value_wo`, `value_wo_version`; seeded_user_entra_oid gained its first lifecycle block; fmt + validate pass; no collateral edits) are all satisfied. The diff is precisely what the planner specified.

## Issues Encountered

None. The edit was a deterministic 3-replace + 1-insert pattern with no environmental friction. `terraform validate` surfaced a pre-existing deprecation warning on `azurerm_log_analytics_workspace.local_authentication_disabled` (transitive AVM module from `module.monitoring.log_analytics`) — out of scope per scope-boundary rules; logged here for awareness only.

## User Setup Required

None — no external service configuration required. The lifecycle change takes effect on the NEXT `terraform apply` Adrian runs against prod; until then, KV state is unchanged. Plan 04 is the verification gate that confirms the live `terraform plan` is a no-op for these 4 resources.

## Next Phase Readiness

- **Plan 02 (cpu/memory parameterization):** unblocked. It edits a different region of `infra/envs/prod/main.tf` (`module "compute"` at lines 188+) plus `infra/modules/compute/{main,variables}.tf` + `infra/envs/prod/variables.tf` + `infra/envs/prod/prod.tfvars`. No textual overlap with this plan — safe to run in parallel/sequence.
- **Plan 03 (README + scripts + CI audit):** unblocked.
- **Plan 04 (live `terraform plan` no-op verification, `autonomous: false`):** dependency satisfied. Adrian's local apply against the live prod tenant must show **zero diff** on the 4 KV secret resources after Plans 01 + 02 + 03 land. If Plan 04 surfaces a diff on any of `openai_api_key`, `langfuse_public_key`, `langfuse_secret_key`, or `seeded_user_entra_oid`, this plan is incomplete and re-opens.

## Self-Check: PASSED

- `infra/envs/prod/main.tf` modified — FOUND
- Commit `f66282a` exists in git log — FOUND
- `grep -c 'ignore_changes = \[value, value_wo, value_wo_version\]' infra/envs/prod/main.tf` = 4 — PASS
- `terraform fmt -check` exit 0 — PASS
- `terraform validate` exit 0 — PASS
- `awk` count of `lifecycle` blocks inside `seeded_user_entra_oid` = 1 — PASS
- Diff is `infra/envs/prod/main.tf` only — PASS

---
*Phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-*
*Completed: 2026-05-26*
