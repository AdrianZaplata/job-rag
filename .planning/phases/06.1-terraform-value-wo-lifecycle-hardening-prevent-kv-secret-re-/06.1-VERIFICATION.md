---
phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-
verified: 2026-05-26T11:30:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 06.1: Terraform value_wo lifecycle hardening — Verification Report

**Phase Goal:** Phase 06.1 closes the three Phase 06 UAT infra regressions (value_wo placeholder re-write wiping out-of-band-seeded KV secrets, hardcoded ACA cpu/memory reverting Adrian's manual OOM-fix bump, and the README's single-var-file `terraform apply` runbook silently breaking Entra auth env vars). Defensive infra-only phase: every future `terraform apply -var-file=prod.tfvars -var-file=prod.tfvars.local` against prod produces ZERO unintended drift.

**Verified:** 2026-05-26T11:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

The phase goal is operationally testable: it asserts that a future dual-var-file `terraform apply` against live prod produces zero drift on the three regression vectors. That assertion was tested end-to-end by Plan 04 (D-08 gate) and recorded in `06.1-VERIFICATION-EVIDENCE.md` — exit code `0` from `bash scripts/tf-apply-prod.sh plan -detailed-exitcode` against live prod state proves the goal holds.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | KV `lifecycle.ignore_changes` covers `value`, `value_wo`, `value_wo_version` on all 4 out-of-band-seeded secrets | VERIFIED | `grep -c 'ignore_changes = \[value, value_wo, value_wo_version\]' infra/envs/prod/main.tf` returns `4`. All 4 resources at lines 138, 151, 164, 177 carry the identical 3-attribute block. `seeded_user_entra_oid` (parity gap closed per D-02) has its first-ever lifecycle block at lines 185-187. Live evidence: `06.1-VERIFICATION-EVIDENCE.md` captures KV secret timestamps as baseline; `terraform plan -detailed-exitcode` exits 0 against live state. |
| 2 | ACA cpu/memory are parameterized (no hardcoded 0.5/1Gi); defaults match the live revision (1.0/2Gi) | VERIFIED | Module `infra/modules/compute/main.tf:104-105` uses `cpu = var.cpu` / `memory = var.memory`; `grep -c 'cpu    = 0.5\|memory = "1Gi"'` returns `0`. Module variables.tf declares `cpu` (number, default 1.0) + `memory` (string, default "2Gi") with Azure Consumption-profile validation. Env layer `infra/envs/prod/variables.tf` mirrors. `prod.tfvars` commits `cpu = 1.0` / `memory = "2Gi"`. Live ACA container resources: `{cpu: 1.0, memory: "2Gi"}` matches committed defaults — no diff. |
| 3 | README + scripts + CI workflow enforce dual-var-file invocations (or document intentional non-coverage with a guard per D-10 Path A) | VERIFIED | README has "Apply command convention" section + file pyramid table + CI-vs-local matrix; `grep -c -- '-var-file=prod.tfvars -var-file=prod.tfvars.local' infra/envs/prod/README.md` returns `6` (≥6 required). `scripts/tf-apply-prod.sh` is executable, hard-codes dual var-file, guards missing prod.tfvars.local with exit 2 + remediation. `scripts/refresh-swa-origin.sh` passes prod.tfvars.local + has inline guard. `.github/workflows/deploy-infra.yml` Path A guard intentionally keeps single-var-file but adds a pre-apply state-inspection guard that fails CI closed if BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN are empty on live ACA. Live verification: all 3 auth env vars present and non-empty on live container. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `infra/envs/prod/main.tf` (KV secrets section, lines 138-188) | 4 KV secret resources each with `lifecycle.ignore_changes = [value, value_wo, value_wo_version]` | VERIFIED | All 4 resources present with identical lifecycle blocks; `seeded_user_entra_oid` gained its first lifecycle block per D-02 |
| `infra/envs/prod/main.tf` (compute module block, lines 192-223) | `module "compute"` passes `cpu = var.cpu` + `memory = var.memory` | VERIFIED | Lines 207-209 wire cpu/memory; grouped with Phase 06.1 D-04 comment |
| `infra/modules/compute/main.tf` (container block) | `cpu = var.cpu` / `memory = var.memory` (no hardcoded literals) | VERIFIED | Lines 104-105 use variables; zero `cpu = 0.5` or `memory = "1Gi"` literals remain |
| `infra/modules/compute/variables.tf` | `cpu` (number, default 1.0) + `memory` (string, default "2Gi") with Azure Consumption-profile validation | VERIFIED | Variables present at lines 109-127 with `contains()` validation lists per D-03 |
| `infra/envs/prod/variables.tf` | Matching `cpu` + `memory` variables at env composition layer | VERIFIED | Variables present at lines 162-180 with same defaults + validation per D-04 |
| `infra/envs/prod/prod.tfvars` | Committed `cpu = 1.0` + `memory = "2Gi"` + D-09 file-pyramid header | VERIFIED | Lines 54-55 declare the size; lines 1-17 carry the 4-tier file pyramid header |
| `infra/envs/prod/README.md` | Apply command convention + file pyramid + CI matrix + dual-var-file runbook | VERIFIED | "Apply command convention (Phase 06.1 D-05/D-06)" section at lines 14-56; 6 dual-var-file invocations across runbook |
| `scripts/tf-apply-prod.sh` | NEW executable wrapper hard-coding dual var-file + missing-file guard | VERIFIED | 75-line script, mode 0755, exit 2 on missing prod.tfvars.local, exit 3 on no subcommand, `--no-local` escape hatch |
| `scripts/refresh-swa-origin.sh` | Pass-2 apply includes `-var-file=prod.tfvars.local` + inline guard | VERIFIED | Line 41 passes both files; lines 36-39 guard prod.tfvars.local existence |
| `.github/workflows/deploy-infra.yml` | D-10 policy comment block + Pre-apply guard step that fails closed on empty auth env vars | VERIFIED | Lines 52-100 carry the policy comment + guard step; Terraform Apply step intentionally byte-identical (Path A) |
| `.planning/phases/06.1-.../06.1-VERIFICATION-EVIDENCE.md` | D-08 evidence file with exit code 0 + KV timestamps + ACA resources + auth env lengths | VERIFIED | File present, captures exit code 0, all 4 KV timestamps, container resources `{cpu: 1.0, memory: "2Gi"}`, all 3 auth env vars non-zero length, Conclusion: PASS |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `infra/envs/prod/main.tf` 4 KV secret resources | Out-of-band `az keyvault secret set` seeding | AzureRM provider lifecycle.ignore_changes for `value_wo` + `value_wo_version` | WIRED | All 4 resources carry the 3-attribute ignore list; live KV timestamps preserved through plan with exit code 0 |
| `infra/envs/prod/prod.tfvars` (cpu/memory) | `infra/modules/compute/main.tf` container resource | env var → module input → resource attribute | WIRED | `prod.tfvars` → `var.cpu`/`var.memory` (env layer) → `module "compute"` input → `var.cpu`/`var.memory` (module layer) → `container.cpu`/`container.memory` |
| `infra/envs/prod/README.md` "Apply command convention" | `scripts/tf-apply-prod.sh` | README references the wrapper as recommended path | WIRED | Lines 22-30 reference `scripts/tf-apply-prod.sh` with 5 example invocations including Plan 04 verification path |
| `scripts/refresh-swa-origin.sh` terraform apply | `infra/envs/prod/prod.tfvars.local` | `-var-file=prod.tfvars.local` CLI flag | WIRED | Line 41 passes both var-files; line 36-39 guard prod.tfvars.local existence pre-apply |
| `.github/workflows/deploy-infra.yml` Pre-apply guard | Live ACA container template | `az containerapp show ... --query '...env'` + jq filter | WIRED | Lines 76-100 implement guard; Plan 04 caught + fixed the `jobrag-prod-aca` → `jobrag-prod-api` typo (commit `cdf3ba3`) before phase close |

### Data-Flow Trace (Level 4)

Not applicable in the traditional sense (infra-only phase, no rendered UI). However, the equivalent "data flowing" check for an infra phase is the live `terraform plan -detailed-exitcode` outcome:

| Concern | Source of Truth | Live State | Status |
|---------|-----------------|-----------|--------|
| KV secret values survive apply | Out-of-band `az keyvault secret set` | 4 KV secrets present with last-updated timestamps captured as baseline | FLOWING |
| Container size matches committed defaults | `prod.tfvars` | Live ACA: `{cpu: 1.0, memory: "2Gi"}` | FLOWING |
| Auth env vars present on container | `prod.tfvars.local` → module input → ACA env | BACKEND_AUDIENCE/ENTRA_TENANT_ID/ENTRA_TENANT_SUBDOMAIN all non-empty on live container | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Wrapper rejects no-subcommand invocation | `bash scripts/tf-apply-prod.sh` | exit `3`, prints usage to stderr | PASS |
| terraform fmt clean on prod env | `cd infra/envs/prod && terraform fmt -check` | exit `0` | PASS |
| Wrapper script bash syntax valid | `bash -n scripts/tf-apply-prod.sh` | exit `0` | PASS |
| Wrapper script executable | `test -x scripts/tf-apply-prod.sh` | exit `0` (mode 0755) | PASS |
| Live `terraform plan` is a no-op | `bash scripts/tf-apply-prod.sh plan -detailed-exitcode` (Adrian's machine, Plan 04) | exit `0` — D-08 gate satisfied | PASS |
| Missing-prod.tfvars.local guard fires | wrapper exit code 2 when file missing | SKIPPED — Adrian intentionally skipped the rename-and-restore test (workstation risk); guard logic reviewed via grep + inline source | SKIP |

### Requirements Coverage

Per ROADMAP.md Phase 06.1 entry: "TBD (this is an inserted hardening phase — no new REQ-IDs; the 4 KV secret resources, ACA container size, and Phase 4 D-04 auth env-var wiring all carry over from Phase 3 + Phase 4 unchanged)". No REQ-IDs to verify.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `infra/envs/prod/README.md` | 108-109 | Stale `jobrag-prod-aca` container name in out-of-band-seeding runbook prose (actual ACA name is `jobrag-prod-api`) | Warning | Operator following the runbook by hand will hit `ResourceNotFound` on `az containerapp revision restart` and may assume the seed itself failed. Tracked as REVIEW WR-01. Does NOT affect the phase goal (operator-facing prose, not part of the dual-var-file enforcement chain). |
| `.github/workflows/deploy-infra.yml` | 79-91 | CI pre-apply guard fails OPEN on any `az containerapp show` error class (auth, network, transient ARM 5xx) by swallowing stderr + falling into the "first-apply skip" branch | Warning | Tracked as REVIEW CR-01 (critical). Same defect class as the typo Plan 04 caught (`cdf3ba3`). A permissions blip or transient az error re-opens the Bug #3 hole. Does NOT contradict the must-have at verification time (guard logic IS present + functional; the defect is error-handling shape, not absence). Phase 06.1 must-have #3 reads "enforce dual-var-file invocations OR document intentional non-coverage with a guard (per D-10 Path A)" — the guard exists and is operational, satisfying the literal must-have wording. |
| `scripts/refresh-swa-origin.sh` | 27-39 | Sed rewrites prod.tfvars BEFORE checking prod.tfvars.local exists — leaves a half-applied state on guard failure | Warning | Tracked as REVIEW WR-02. The guard exists but ordering is wrong. Does NOT affect Phase 06.1's must-have (the guard IS present and the apply does pass both files). |
| `infra/modules/compute/variables.tf` + `infra/envs/prod/variables.tf` | cpu/memory blocks | cpu:memory 1:2 ratio documented in description text only — not enforced at plan time | Warning | Tracked as REVIEW WR-03. Independent `contains()` validation lists exist for each variable individually; the cross-variable rule is not enforced. Not a defect introduced by 06.1 in the safety direction (prior hardcoded 0.5/1Gi was always valid). |
| `.github/workflows/deploy-infra.yml` | 94 | CI guard's `.value // empty` jq filter silently treats `secretRef` env entries as empty | Warning | Tracked as REVIEW WR-04. Works correctly today (the 3 protected vars are plain literals); will silently misclassify if any var is ever moved to KV. Future-only concern. |
| (none found) | n/a | TODO/FIXME/PLACEHOLDER scan across 9 phase files | Info | No TODO/FIXME/HACK markers found in any of the 9 files touched by Phase 06.1 |

**Severity rollup:** 1 critical (CR-01) + 4 warnings (WR-01..04) per `06.1-REVIEW.md`. None invalidate the Phase 06.1 must-haves at verification time, but all are tracked for follow-up. The user's verification instructions explicitly stated "Treat them as informational input for verification, not blocking."

### Human Verification Required

None at this verification step.

The live `terraform plan -detailed-exitcode` against live prod state — which is the human-only D-08 gate for this phase — has already been executed by Adrian and captured as PASS in `06.1-VERIFICATION-EVIDENCE.md`:
- Exit code: `0`
- KV secret timestamps captured for all 4 secrets
- ACA container resources: `{cpu: 1.0, memory: "2Gi"}` matches committed defaults
- All 3 auth env vars (BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN) non-empty on live container
- Conclusion: PASS

The verifier confirmed the evidence file exists, contains the required PASS markers, and is internally consistent with the static code checks performed above. No additional human testing required to close out Phase 06.1.

### Gaps Summary

No gaps. All three must-haves verified against the actual codebase:
1. **KV lifecycle hardening (Plan 01):** All 4 `azurerm_key_vault_secret` resources carry `lifecycle.ignore_changes = [value, value_wo, value_wo_version]`; `seeded_user_entra_oid` parity gap closed.
2. **ACA cpu/memory parameterization (Plan 02):** Hardcoded literals replaced with `var.cpu`/`var.memory` at module + env layer; committed defaults (1.0/2Gi) match live revision; Azure Consumption-profile validation lists present per variable.
3. **Dual var-file enforcement (Plan 03):** README "Apply command convention" + file pyramid + CI-vs-local matrix + 6 dual-var-file runbook sites; new `scripts/tf-apply-prod.sh` wrapper with missing-file guard; `scripts/refresh-swa-origin.sh` passes both files; CI workflow has Path A pre-apply state-inspection guard documenting intentional non-coverage of auth vars.

The live `terraform plan -detailed-exitcode` no-op proof (Plan 04) confirms the combination of all three changes produces zero drift against live prod state — which is the operational restatement of the phase goal.

REVIEW findings (1 critical + 4 warnings in `06.1-REVIEW.md`) are tracked separately and are defensive hardening around the changes (e.g., guard fails open on az read errors, stale aca→api typo in runbook prose). Per the user's verification instructions, these are informational input and do not invalidate the must-haves.

---

_Verified: 2026-05-26T11:30:00Z_
_Verifier: Claude (gsd-verifier)_
