---
phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-
plan: 03
subsystem: infra

tags: [terraform, runbook, ci-cd, github-actions, shell-wrapper, prod-tfvars-local, bug-3-closure, defense-in-depth]

requires:
  - phase: 06-chat
    provides: "06-UAT-DEBUG-HANDOFF Bug #3 forensics — proved that the runbook's `terraform apply -var-file=prod.tfvars` single-var-file invocation silently wiped BACKEND_AUDIENCE / ENTRA_TENANT_* on the ACA container because the defaults in infra/envs/prod/variables.tf are \"\"."
  - phase: 06.1-plan-01
    provides: "value_wo lifecycle hardening (4 KV secret resources now carry ignore_changes = [value, value_wo, value_wo_version]) — orthogonal to this plan but part of the same defense-in-depth Bug #3 + Bug #1/#2 closure."
  - phase: 06.1-plan-02
    provides: "cpu/memory parameterization + D-09 file-pyramid header at the top of prod.tfvars — this plan's README D-09 table mirrors the same 4-tier convention to keep docs + tfvars header aligned."

provides:
  - "scripts/tf-apply-prod.sh — canonical wrapper that hard-codes dual-var-file invocation with missing-file guard (exit 2) + remediation message + --no-local escape hatch + no-subcommand guard (exit 3); functionally smoke-tested on both error branches."
  - "scripts/refresh-swa-origin.sh — pass 2 (CORS refresh) now passes prod.tfvars.local + has inline guard so pass 2 cannot silently re-wipe auth env vars even if pass 1 was correct."
  - "infra/envs/prod/README.md — new 'Apply command convention (Phase 06.1 D-05/D-06)' section + D-09 file-pyramid table + D-10 CI-vs-local variable-source matrix + 5 runbook sites updated to dual-var-file form + 1 Knowingly-accepted row added; zero stale single-var-file `terraform apply/plan` operational commands remain."
  - ".github/workflows/deploy-infra.yml — Phase 06.1 D-10 16-line policy comment block + new 'Pre-apply guard' step (before Terraform Apply) that fails CI closed with `::error::Refusing to apply ...` if any of BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN is empty on the live ACA template; first-apply path handled (skip when container app does not yet exist)."
  - "Foundation for Plan 04 (live `terraform plan -detailed-exitcode` no-op verification) — Adrian's verification run can now use either the raw dual-var-file form or `bash scripts/tf-apply-prod.sh plan -detailed-exitcode` per the new README convention."

affects: [phase-06.1-plan-04]

tech-stack:
  added: []
  patterns:
    - "Dual-var-file invocation discipline at THREE layers (defense-in-depth): wrapper script (scripts/tf-apply-prod.sh — local) + operator docs (infra/envs/prod/README.md — every runbook site) + CI guard (.github/workflows/deploy-infra.yml — pre-apply ACA template inspection). Removing any one layer would re-expose Bug #3; together they make the failure mode structurally impossible."
    - "Pre-apply state-inspection CI guard — read the live infra state (`az containerapp show ... --query 'properties.template.containers[0].env'`) BEFORE running `terraform apply` and refuse the apply if any expected-non-empty field is empty. Cheap, no TF-state coupling, reusable for any future 'CI must not silently wipe an out-of-band-seeded value' invariant."
    - "First-apply skip pattern — guard detects `ENV_JSON = \"[]\"` (container app does not exist yet) and emits `::notice::` instead of `::error::`, so the very first `terraform apply` against an empty subscription is not blocked. Preserves bootstrap UX while protecting steady-state."
    - "GitHub Actions structured annotation messaging — `::error::` / `::notice::` log prefixes surface guard outcomes directly in the Actions UI's Annotations panel + the job summary, not buried in raw logs."

key-files:
  created:
    - scripts/tf-apply-prod.sh
  modified:
    - scripts/refresh-swa-origin.sh
    - infra/envs/prod/README.md
    - .github/workflows/deploy-infra.yml

key-decisions:
  - "Honored D-05/D-06/D-07 verbatim: every operational `terraform plan/apply` invocation in the prod runbook + the refresh-swa-origin.sh helper now carries BOTH var files; the new wrapper hard-codes the invocation + guards missing prod.tfvars.local with exit 2 + remediation; conditional README lines 107 + 198 verified prose-only (no `terraform apply -var-file` flags) so left unchanged per the plan's Edit 2 table — documented in Task 2 commit body."
  - "Honored D-09 verbatim: README file-pyramid table mirrors the same 4-tier convention prepended at the top of prod.tfvars by Plan 02 — single source of truth for value-placement policy lives in both surfaces an operator might read first."
  - "Honored D-10 Path A verbatim (NOT Path B): CI uses a state-inspection pre-apply guard + 16-line policy comment block, NOT new TF_VAR_BACKEND_AUDIENCE / TF_VAR_ENTRA_TENANT_* env vars from GitHub Actions secrets. Path A preserves Phase 3 D-13 (out-of-band-seeded values stay out of GH Actions secrets) — the auth vars are public-by-design but the file-tier-discipline matters for uniformity with the other 4 KV secrets that ARE genuinely secret. The Terraform Apply step's `run:` is BYTE-IDENTICAL to before — CI continues to use single-var-file because prod.tfvars.local is gitignored and not on the runner."
  - "Used exact Edit blocks (not replace_all) on every README site + the YAML insertion — each edit had unique surrounding context (Prerequisites→Ordered runbook seam, exact code-block triplets in pass 1 / CORS / drift, the LAW knowingly-accepted row, the Terraform Init→Terraform Apply seam). Atomic, deterministic, no risk of partial application."
  - "Did NOT touch any infra/ Terraform files (variables.tf, main.tf, modules/) — Plans 01 and 02 already shipped those edits. Verified via `git status` post-commit: only the 4 files in the plan's `files_modified` list have changes."
  - "Functional smoke test for the wrapper executed locally during Task 1: temporarily renamed prod.tfvars.local → SMOKE-BAK, ran `bash scripts/tf-apply-prod.sh plan`, captured exit code 2 + full remediation message, restored the file. Also ran `bash scripts/tf-apply-prod.sh` (no subcommand) and confirmed exit code 3 + usage message. Both error branches behave exactly as the wrapper spec requires."

patterns-established:
  - "Wrapper-script-as-runbook-enforcement — for any future operational invariant that boils down to 'always pass flag X with value Y', prefer a thin wrapper script over a README-only convention: the wrapper makes the rule executable, fails loudly on violation, and documents itself via its own `--help` / error messages. README convention still exists but as the authoritative spec the wrapper implements, not the only line of defense."
  - "CI state-inspection pre-apply guard — for any CI-driven `terraform apply` against a tenant where some values are intentionally NOT managed by CI (out-of-band-seeded, local-state-only, etc.), insert a pre-apply read against the live state via the relevant `az <service> show` command + jq filter + fail-closed assertion. Avoids the silent-overwrite class of bugs without inverting the local-only-secrets policy."

requirements-completed: []

duration: 4m 14s
completed: 2026-05-26
---

# Phase 06.1 Plan 03: README + scripts + CI hardening Summary

**Closed the three runbook-level conditions that produced Phase 06 UAT Bug #3 (silent Entra auth breakage from `terraform apply -var-file=prod.tfvars` alone wiping `BACKEND_AUDIENCE` / `ENTRA_TENANT_*`) at THREE layers simultaneously: a new `scripts/tf-apply-prod.sh` wrapper that hard-codes dual-var-file + guards missing `prod.tfvars.local` (exit 2), every operational `terraform plan/apply` site in `infra/envs/prod/README.md` updated to dual-var-file + a new 'Apply command convention' section with D-09 file pyramid + D-10 CI-vs-local matrix, and a new D-10 pre-apply guard in `deploy-infra.yml` that fails CI closed if the live ACA's auth env vars are empty — without inverting Phase 3 D-13 by promoting the three auth values to GitHub Actions secrets.**

## Performance

- **Duration:** 4 min 14 s (254 s)
- **Started:** 2026-05-26T08:53:09Z
- **Completed:** 2026-05-26T08:57:23Z
- **Tasks:** 3
- **Files modified:** 3 (+ 1 created)

## Accomplishments

- **Wrapper layer (Task 1):** `scripts/tf-apply-prod.sh` (NEW, 75 lines, executable) hard-codes `terraform <sub> -var-file=prod.tfvars -var-file=prod.tfvars.local`, supports pass-through args, exits 2 on missing prod.tfvars.local with full remediation pointing at the README + the rebuild script, exits 3 on no subcommand with usage, supports `--no-local` escape hatch for testing the failure mode. Both error branches functionally smoke-tested locally (rename → exit 2 with remediation; no-args → exit 3 with usage). `scripts/refresh-swa-origin.sh` pass-2 `terraform apply` now also loads prod.tfvars.local + has inline guard so pass 2 cannot silently re-wipe Entra env vars.
- **Docs layer (Task 2):** New "Apply command convention (Phase 06.1 D-05/D-06)" section above the Ordered runbook containing the canonical command + wrapper reference + D-09 file-pyramid table (4 tiers: committed tfvars / gitignored .local / TF_VAR_* / az keyvault secret set) + D-10 CI-vs-local matrix. 5 operational runbook sites upgraded to dual-var-file form (Ordered runbook step 2, First apply pass 1 code block, Two-pass CORS bootstrap, ghcr_pat rotation, Drift detection). 2 conditional sites (old README lines 107 + 198) verified prose-only and explicitly left unchanged per the plan's Edit 2 conditional logic — documented in commit body. New "Knowingly-accepted security trade-offs" row added explaining the auth-vars-via-prod.tfvars.local policy with mitigation pointers at all three defense layers. `grep -c "-var-file=prod.tfvars -var-file=prod.tfvars.local" infra/envs/prod/README.md` = 6 (>=6 required).
- **CI layer (Task 3):** `.github/workflows/deploy-infra.yml` gained a 16-line Phase 06.1 D-10 policy comment block + a new "Pre-apply guard — verify auth env vars present on live ACA" step inserted BEFORE Terraform Apply (line 76 vs line 102 — order verified via awk). Guard runs `az containerapp show --query 'properties.template.containers[0].env' -o json` + iterates the three protected vars + jq-extracts each value + fails CI fast with `::error::Refusing to apply ...` if any is empty. First-apply path (container does not exist yet) handled via `ENV_JSON == "[]"` skip with `::notice::`. The existing Terraform Apply step's `run:` is byte-identical to before (single-var-file is intentional for CI per Path A — prod.tfvars.local is gitignored and not on the runner). YAML still parses cleanly via `yaml.safe_load`.
- **Scope discipline preserved:** zero changes to any `infra/` Terraform files (Plans 01 and 02 own those). `git diff --diff-filter=D` returns zero deletions on all three commits. All operational `terraform plan/apply` invocations in the prod runbook are now dual-var-file or explicitly out of scope (bootstrap dir, prose-only mentions).

## Task Commits

Each task was committed atomically:

1. **Task 1: Create scripts/tf-apply-prod.sh wrapper + update scripts/refresh-swa-origin.sh** — `ad36de2` (feat)
2. **Task 2: Update infra/envs/prod/README.md runbook to dual-var-file + add Apply command convention section** — `f378d37` (docs)
3. **Task 3: Audit + harden .github/workflows/deploy-infra.yml CI parity (D-10 Path A)** — `49adea3` (feat)

_Plan metadata commit follows separately at end of execution._

## Files Created/Modified

- **`scripts/tf-apply-prod.sh` (NEW, 75 lines, mode 0755)** — canonical wrapper: shebang `#!/bin/bash`, `set -euo pipefail`, REPO_ROOT/PROD_DIR/LOCAL_TFVARS path resolution, `--no-local` flag handling (shift), no-subcommand guard (exit 3 with usage to stderr), missing prod.tfvars.local guard (exit 2 with 8-line remediation message pointing at README + scripts/refresh-external-outputs.sh + `--no-local` bypass), `cd "$PROD_DIR"`, `exec terraform "$SUBCOMMAND" -var-file=prod.tfvars -var-file=prod.tfvars.local "$@"` (or single-var-file under `--no-local`). All paths reachable. Both error branches functionally tested.
- **`scripts/refresh-swa-origin.sh`** — pass-2 `terraform apply` at the end of the script now passes `-var-file=prod.tfvars.local` in addition to `-var-file=prod.tfvars`; a new Phase 06.1 D-07 inline guard immediately above the apply asserts `prod.tfvars.local` exists in the cwd (script's cwd is `infra/envs/prod/` per the existing `cd "$(dirname "$0")/../infra/envs/prod"`) and exits 2 with the FATAL message pointing at the README's 'Apply command convention' if missing. The `set -euo pipefail`, the cwd-change, the SWA host validation, and the in-place sed-based prod.tfvars rewrite (lines 27-32) are all unchanged.
- **`infra/envs/prod/README.md`** — 50 lines inserted, 5 lines updated:
  - **Inserted** above the Ordered runbook (between Prerequisites and W2 ordering): new "Apply command convention (Phase 06.1 D-05/D-06)" section containing canonical dual-var-file command + scripts/tf-apply-prod.sh wrapper reference + 5 example invocations including the Plan 04 `plan -detailed-exitcode` verification path + 5-line "Why both files are required" rationale paragraph linking back to 06-UAT-DEBUG-HANDOFF.md + D-09 file-pyramid 4-row table + D-10 CI-vs-local 4-row variable-source matrix.
  - **Updated 5 operational sites:** Ordered runbook step 2 (cd && init && apply), First apply pass 1 code block (terraform plan -out), Two-pass CORS bootstrap reference to the apply command, ghcr_pat rotation `terraform apply -var ghcr_pat=...`, Drift detection `terraform plan` periodically.
  - **Appended** a new row to the "Knowingly-accepted security trade-offs" table: auth env vars (backend_audience / entra_tenant_id / entra_tenant_subdomain) sourced from gitignored prod.tfvars.local + rationale + 3-layer mitigation.
  - **Left unchanged** (per plan's Edit 2 conditional logic, verified prose-only): old line 107 ("Image push and ACA revision update" intro paragraph — no `terraform apply -var-file` flag, prose only); old line 198 ("Home IP refresh" cross-reference to database module README — no terraform invocation, prose only). Both documented in the Task 2 commit body.
- **`.github/workflows/deploy-infra.yml`** — 50 lines inserted (no lines changed):
  - **Inserted** between the existing Terraform Init step (ending at the env block for OIDC vars) and the existing Terraform Apply step: a 16-line `# ─── Phase 06.1 D-10 — auth vars CI-vs-local policy ─────────────────────` comment block + a new `- name: Pre-apply guard — verify auth env vars present on live ACA` step with `set -euo pipefail` + `az containerapp show ... -o json` + first-apply skip path + for-loop over `BACKEND_AUDIENCE ENTRA_TENANT_ID ENTRA_TENANT_SUBDOMAIN` + jq value-extract + `::error::Refusing to apply ...` on empty + `::notice::ACA $VAR present` on non-empty.
  - **NOT changed:** the existing Terraform Apply step's `run:` command (`terraform apply -input=false -auto-approve -var-file=prod.tfvars`) — byte-identical per Path A intentional choice.
  - **NOT changed:** the existing TF_VAR_* env block on Terraform Apply or Terraform Init steps. No new env vars added.

## Decisions Made

- **D-05 + D-06 + D-07 + D-09 + D-10 applied verbatim from `.planning/phases/06.1-.../06.1-CONTEXT.md`.** No new decisions emerged during execution — the plan was a deterministic spec'd-out edit and the surrounding context was fully pinned by the Phase 06 UAT Bug #3 forensics in 06-UAT-DEBUG-HANDOFF.md.
- **D-10 Path A (chosen, per plan): annotate + pre-apply guard, NOT new TF_VAR_* env vars.** Confirmed by reading `.github/workflows/deploy-infra.yml` end-to-end — the established pattern is OIDC + TF_VAR_* only for true secrets (ghcr_pat) and Azure provider config (gha_client_id / tenant_id_workforce / use_oidc_auth). Promoting backend_audience / entra_tenant_id / entra_tenant_subdomain to GitHub Actions secrets would invert Phase 3 D-13 for no security gain (these values are public-by-design — visible in any issued JWT's iss/aud claims) and would create a divergent precedent. The guard step achieves the same fail-closed property without changing the secret-management model.
- **Conditional README sites 107 + 198 verified prose-only.** Read both passages end-to-end before applying any edits — line 107 introduces the Image push runbook with no `terraform apply -var-file` flag (the actual apply command is line 234 in the rotation table, which IS updated); line 198 cross-references `infra/modules/database/README.md`'s sed-based prod.tfvars one-liner with no terraform invocation in the prose itself. Per the plan's Edit 2 conditional logic ("if it's prose only, leave unchanged"), both are not-applicable. Documented in the Task 2 commit body.
- **Awk regex from the plan's acceptance criteria mis-flagged the now-corrected dual-var-file line 77.** The regex `^terraform plan -var-file=prod.tfvars[^.]` matches any non-`.` character after `-var-file=prod.tfvars`, including a literal space. The post-edit line 77 reads `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -out=plan-pass-1.tfplan` — the space after the first `-var-file=prod.tfvars` matches `[^.]`. Manually verified via a tighter grep (`grep -nE "terraform plan -var-file=prod\\.tfvars( |$|-)" infra/envs/prod/README.md | grep -v "prod.tfvars.local"` returns no matches) that ZERO actual single-var-file `terraform plan` operational commands remain. Substantive truth (no copy-paste hazards) preserved; awk regex was the loose-heuristic part of the plan, not the substantive requirement.

## Deviations from Plan

None — the plan's executor work was completed exactly as specified.

### Acceptance criteria notes (no code deviation)

**1. [Note — not a deviation] Plan's `<verify><automated>` clause asserted `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-infra.yml'))"` should exit 0**

- **Actual:** system `python3` (`/usr/bin/python3`) has no `yaml` module, so the bare invocation raises `ModuleNotFoundError: No module named 'yaml'`.
- **Resolution:** executed the same check via `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-infra.yml')); print('YAML_PARSE: PASS')"` — exited 0 with the expected `YAML_PARSE: PASS`. The project's uv-managed venv ships PyYAML (via transitive deps of langchain-openai / Instructor). The YAML itself is valid; only the `python3` invocation context differed from the plan's assumption.
- **No code fix needed.** Substantive correctness (YAML parses cleanly) preserved.

**2. [Note — not a deviation] Plan's acceptance criterion `awk '/^terraform plan -var-file=prod.tfvars[^.]/' infra/envs/prod/README.md` returns zero lines**

- **Actual:** returns `1` line (line 77, the now-CORRECT dual-var-file `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -out=plan-pass-1.tfplan` line).
- **Why:** `[^.]` matches any non-`.` character; the literal space between the first `-var-file=prod.tfvars` and ` -var-file=prod.tfvars.local` matches. The awk regex was intended to catch stale single-var-file lines but is loose enough to also match the corrected dual-var-file form.
- **Substantive truth:** verified with a stricter check (`grep -nE "terraform plan -var-file=prod\\.tfvars( |$|-)" infra/envs/prod/README.md | grep -v "prod.tfvars.local"`) → zero matches. No stale single-var-file `terraform plan` operational commands remain.
- **No fix needed.** Substantive correctness preserved.

---

**Total deviations:** 0 code deviations. 2 acceptance-criteria notes for the verifier (both loose-heuristic acceptance criteria from the plan author, not substantive requirement gaps).

**Impact on plan:** None. All substantive requirements (3-layer defense-in-depth: wrapper + README + CI guard; D-05/D-06/D-07/D-09/D-10 honored verbatim; functional smoke test of wrapper passed both error branches; YAML still parses; Terraform Apply step byte-identical per Path A; zero scope-bleed into infra/ Terraform files) are satisfied. The diff across 4 files is precisely what the planner specified.

## Issues Encountered

None. The 4 edits were all deterministic surrounding-context-anchored Write/Edit blocks with no environmental friction beyond the cosmetic `python3 -c "import yaml"` issue addressed above. The PreToolUse Read-before-Edit reminders that fired on the README.md (5×) and deploy-infra.yml (1×) edits are a known harness lifecycle quirk — both files WERE read at the start of execution (full reads logged); the edits succeeded; the reminders did not block.

## User Setup Required

None — no external service configuration required. The changes take effect on the NEXT runbook invocation Adrian runs (whether via the wrapper, the raw dual-var-file form, or the next CI-driven `deploy-infra.yml` run). Plan 04 is the verification gate that confirms the live `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -detailed-exitcode` is a no-op against the live prod tenant.

If Adrian wants to verify the CI pre-apply guard works before relying on it, the simplest dry run is: trigger `deploy-infra.yml` via `gh workflow run deploy-infra.yml --ref master` from a branch where this commit is present, and inspect the `Pre-apply guard` step's annotations (should see 3 × `::notice::ACA $VAR present (length N)` lines, indicating the live container template still has the seeded values from his last local apply).

## Next Phase Readiness

- **Plan 04 (live `terraform plan -detailed-exitcode` no-op verification, `autonomous: false`):** dependency satisfied — all three Wave 1+2 plans (01, 02, 03) have shipped. Adrian's local verification command per the new README convention is:
  ```bash
  bash scripts/tf-apply-prod.sh plan -detailed-exitcode
  ```
  Expected: exit code 0 + `Plan: 0 to add, 0 to change, 0 to destroy.` If the plan surfaces ANY diff (KV secret value/value_wo, container size cpu/memory, or auth env vars), the corresponding Wave 1+2 plan is incomplete and re-opens.
- **CI parity:** the next `deploy-infra.yml` run (whether `workflow_dispatch` or a push to `infra/**`) will exercise the new Pre-apply guard step against the live ACA. If Adrian's last local apply correctly seeded BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN, the guard emits 3 × `::notice::` and the apply proceeds. If for any reason any of those is empty (e.g., he hasn't run a local apply since the var was introduced), the guard fails CI fast with the documented remediation path — and the failure is correct, not a regression.

## Self-Check: PASSED

- `scripts/tf-apply-prod.sh` exists, executable (mode 0755), 75 lines — FOUND
- `scripts/refresh-swa-origin.sh` modified — FOUND (`-var-file=prod.tfvars.local` + inline guard)
- `infra/envs/prod/README.md` modified — FOUND (Apply command convention + file pyramid + CI matrix + 5 dual-var-file sites + 1 Knowingly-accepted row)
- `.github/workflows/deploy-infra.yml` modified — FOUND (D-10 comment block + Pre-apply guard step; Terraform Apply run line byte-identical)
- Commit `ad36de2` (Task 1) exists in git log — FOUND
- Commit `f378d37` (Task 2) exists in git log — FOUND
- Commit `49adea3` (Task 3) exists in git log — FOUND
- `bash -n scripts/tf-apply-prod.sh` exit 0 — PASS
- `bash -n scripts/refresh-swa-origin.sh` exit 0 — PASS
- `test -x scripts/tf-apply-prod.sh` — PASS
- Functional smoke: wrapper with prod.tfvars.local missing → exit code 2 + 8-line remediation message — PASS
- Functional smoke: wrapper with no subcommand → exit code 3 + usage to stderr — PASS
- `grep -c "-var-file=prod.tfvars -var-file=prod.tfvars.local" infra/envs/prod/README.md` = 6 (>=6 required) — PASS
- `grep -q "Apply command convention" infra/envs/prod/README.md` — PASS
- `grep -q "scripts/tf-apply-prod.sh" infra/envs/prod/README.md` — PASS
- `grep -q "file pyramid" infra/envs/prod/README.md` — PASS
- `grep -q "CI vs local: variable-source matrix" infra/envs/prod/README.md` — PASS
- `grep -q "06-UAT-DEBUG-HANDOFF" infra/envs/prod/README.md` — PASS
- `grep -q "Phase 06.1" infra/envs/prod/README.md` — PASS (Knowingly-accepted row added)
- `grep -q "Phase 06.1 D-10" .github/workflows/deploy-infra.yml` — PASS
- `grep -q "Pre-apply guard" .github/workflows/deploy-infra.yml` — PASS
- `grep -q "BACKEND_AUDIENCE ENTRA_TENANT_ID ENTRA_TENANT_SUBDOMAIN" .github/workflows/deploy-infra.yml` — PASS
- `grep -q "Refusing to apply" .github/workflows/deploy-infra.yml` — PASS
- `grep -q "scripts/tf-apply-prod.sh" .github/workflows/deploy-infra.yml` — PASS
- Terraform Apply step's `run:` byte-identical (`terraform apply -input=false -auto-approve -var-file=prod.tfvars$`) — PASS
- Pre-apply guard step ordering: line 76 < Terraform Apply line 102 — PASS
- `uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-infra.yml'))"` exit 0 — PASS
- Zero collateral file changes (only 4 plan-scoped files touched) — PASS
- Zero deletions across all 3 task commits — PASS

---
*Phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-*
*Completed: 2026-05-26*
