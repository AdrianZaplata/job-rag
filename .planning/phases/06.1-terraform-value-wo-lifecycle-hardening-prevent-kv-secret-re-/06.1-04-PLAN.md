---
phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-
plan: 04
type: execute
wave: 3
depends_on:
  - 06.1-03
files_modified:
  - .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md  # NEW — verification evidence file
autonomous: false
requirements: []
user_setup:
  - service: azure
    why: "Live `terraform plan` against the prod state requires Adrian's local az login + access to the prod subscription"
    env_vars: []
    dashboard_config:
      - task: "Confirm `az account show` returns the workforce tenant subscription that owns jobrag-prod-rg"
        location: "Adrian's local terminal"
must_haves:
  truths:
    - "Live `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -detailed-exitcode` against the live prod state exits 0 (NO changes) — proving Plans 01+02+03 together produce zero drift."
    - "The 4 KV secret resources show no diff (Plan 01 verification)."
    - "The ACA container template shows no cpu/memory diff (Plan 02 verification — defaults match the live revision Adrian manually bumped to)."
    - "The ACA container template shows no BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN diff (Plan 03 verification — prod.tfvars.local was passed correctly)."
    - "Evidence captured in `06.1-VERIFICATION-EVIDENCE.md` includes: exit code, full plan output's summary line, az CLI verifications of KV secret last-updated timestamps + ACA container resources block."
  artifacts:
    - path: .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md
      provides: "Evidence of zero-drift verification per D-08 — exit codes, command outputs, optional Azure CLI sanity checks"
      contains: "Plan: 0 to add, 0 to change, 0 to destroy"
  key_links:
    - from: Plans 01 + 02 + 03 committed edits
      to: live Azure prod state
      via: terraform plan -detailed-exitcode
      pattern: "exit code 0"
---

<objective>
Pre-flight verification of all Phase 06.1 edits against the live prod state, per D-08.

Adrian runs `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -detailed-exitcode`
from `infra/envs/prod/` (or `bash scripts/tf-apply-prod.sh plan -detailed-exitcode` from
repo root, which is equivalent post-Plan 03). The plan MUST be a no-op (exit code 0). If it
shows ANY change, one of the upstream plans has a defect:
- KV secret value/value_wo diff → Plan 01's ignore_changes is incomplete
- Container cpu/memory diff → Plan 02's defaults don't match the live revision
- BACKEND_AUDIENCE / ENTRA_TENANT_* diff → Plan 03 didn't propagate prod.tfvars.local correctly,
  OR prod.tfvars.local on Adrian's machine is stale / missing the keys

This plan is `autonomous: false` because it requires Adrian's local `az login` against the
prod subscription. CI cannot run it — the prod TF state is in the Azure Blob backend,
accessible via the workforce-tenant OIDC federation, but a CI runner has no `prod.tfvars.local`
and would fail the file-missing check inside `scripts/tf-apply-prod.sh`.

Output: `06.1-VERIFICATION-EVIDENCE.md` capturing the actual exit code + summary line + any
sanity checks. The phase is GREEN only when this plan's verification passes.

This plan is the final gate before Adrian executes the real `terraform apply -var-file=prod.tfvars
-var-file=prod.tfvars.local` against prod (which closes the loop on the operational fix —
the apply itself happens AFTER this verification proves the plan is no-op).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-CONTEXT.md
@.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-01-PLAN.md
@.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-02-PLAN.md
@.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-03-PLAN.md

<interfaces>
<!-- terraform plan -detailed-exitcode exit code semantics -->
- 0 = no changes (success — what we want)
- 1 = error (terraform itself failed)
- 2 = changes present (failure — at least one upstream plan has a defect)

<!-- The expected summary line on success: -->
<!--   "Plan: 0 to add, 0 to change, 0 to destroy." -->
<!-- If detailed-exitcode and the plan IS a no-op, terraform still prints that line. -->

<!-- Required Azure CLI sanity checks (from CONTEXT.md <specifics>): -->
- `az keyvault secret show --vault-name $KV_NAME --name openai-api-key --query attributes.updated -o tsv` BEFORE and AFTER a test apply — timestamps MUST be identical (run only if Adrian also runs the apply; for pure plan verification, capture the BEFORE timestamp only)
- `az containerapp show --name jobrag-prod-aca --resource-group jobrag-prod-rg --query 'properties.template.containers[0].resources' -o jsonc` — must show `{"cpu": 1.0, "memory": "2Gi"}` matching Plan 02's defaults
- `az containerapp show --name jobrag-prod-aca --resource-group jobrag-prod-rg --query 'properties.template.containers[0].env[?name==\`BACKEND_AUDIENCE\`].value | [0]' -o tsv` — must be non-empty (a real `api://<guid>` value)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Adrian runs live `terraform plan` verification + captures evidence</name>
  <files>.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-CONTEXT.md (decision D-08 — the verification gate semantics; the <specifics> block lists the exact az CLI commands to run)
    - .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-01-PLAN.md (Plan 01 acceptance criteria — KV secret resources must show zero diff)
    - .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-02-PLAN.md (Plan 02 acceptance criteria — container resources must match cpu=1.0/memory=2Gi)
    - .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-03-PLAN.md (Plan 03 acceptance criteria — auth env vars must remain non-empty)
    - scripts/tf-apply-prod.sh (Plan 03 wrapper — the canonical invocation path)
    - infra/envs/prod/README.md "Apply command convention" section (Plan 03 — operator-facing reference)
  </read_first>
  <action>
This is a `checkpoint:human-action` task. The full operator runbook (prerequisites, the
exact bash commands to run, the evidence template to fill in) is in `<how-to-verify>` below.
Adrian executes those steps in order and types "approved" when the conclusion is PASS.

Summary of what Adrian does:
1. Confirm prerequisites (az login workforce tenant, prod.tfvars.local present, terraform init).
2. Smoke-test the wrapper from Plan 03 (sanity that the missing-file guard fires correctly).
3. Capture live BEFORE state — KV secret timestamps + ACA container resources + auth env var
   lengths — into the evidence file.
4. Run `bash scripts/tf-apply-prod.sh plan -detailed-exitcode` against the live state.
5. Verify exit code is 0 AND summary line reads `Plan: 0 to add, 0 to change, 0 to destroy.`
   (modulo expected B5 image_tag drift, which is documented in the evidence file as
   not-counted-toward-fail).
6. Write `06.1-VERIFICATION-EVIDENCE.md` with the captured evidence + PASS conclusion.
7. Type "approved" to resume. On FAIL, type the responsible plan number(s).
  </action>
  <verify>
    <automated>test -f .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md && grep -q "Plan: 0 to add, 0 to change, 0 to destroy" .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md && grep -q "Conclusion.*PASS" .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md</automated>
  </verify>
  <done>
    `.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md` exists with: (a) prerequisite confirmations, (b) wrapper sanity results, (c) live BEFORE state capture (KV timestamps + ACA resources + auth env lengths), (d) `terraform plan -detailed-exitcode` exit code and summary line, (e) `Conclusion: PASS` (or `FAIL` with routing to the responsible plan). Adrian's "approved" reply confirms the gate is closed.
  </done>
  <what-built>
    Plans 01 (value_wo lifecycle hardening) + 02 (cpu/memory parameterization) + 03 (README
    + scripts + CI parity) have all landed in the repo. Frontmatter `depends_on` chain is
    01+02 → 03 → 04 (this plan). All `terraform fmt -check` and `terraform validate`
    checks have passed at the repo level. Now we need proof that against the LIVE prod
    state, this combination is a no-op.
  </what-built>
  <how-to-verify>
**Prerequisites:**
1. You are on a clone with `infra/envs/prod/prod.tfvars.local` populated (the Phase 4
   D-04 auth values, plus `home_ip`). If you are not sure, run:
   ```bash
   grep -c '^backend_audience\|^entra_tenant_id\|^entra_tenant_subdomain\|^home_ip' infra/envs/prod/prod.tfvars.local
   ```
   Expected: at least `4` (one line per variable). If `0` or missing, rebuild from
   `infra/external/` outputs via `bash scripts/refresh-external-outputs.sh` first.
2. You are logged in to the workforce tenant subscription that owns `jobrag-prod-rg`:
   ```bash
   az account show --query '{name:name, tenantId:tenantId, id:id}' -o jsonc
   ```
   Expected: `tenantId` = workforce tenant (`9a7d79f5-...` per repo memory `azure-tenant-split.md`).
   If not, run `az login --tenant 9a7d79f5-...` first.
3. The remote TF state backend is accessible:
   ```bash
   cd infra/envs/prod && terraform init -input=false
   ```
   Expected: "Terraform has been successfully initialized!"

**Verification step 1 — wrapper sanity (no Azure call):**
```bash
# From repo root
bash scripts/tf-apply-prod.sh   # no subcommand
# Expected: exits 3, prints usage to stderr
echo "Exit code: $?"
```

```bash
# Test missing-local detection (optional — only safe to run if you can restore the file)
mv infra/envs/prod/prod.tfvars.local /tmp/prod.tfvars.local.bak
bash scripts/tf-apply-prod.sh plan
# Expected: exits 2, prints "Missing infra/envs/prod/prod.tfvars.local" remediation
echo "Exit code: $?"
mv /tmp/prod.tfvars.local.bak infra/envs/prod/prod.tfvars.local
```

**Verification step 2 — capture pre-apply state of KV secret timestamps (D-08 evidence):**
```bash
cd infra/envs/prod
KV_NAME=$(terraform output -raw kv_name)
echo "KV: $KV_NAME"
echo ""
echo "## KV secret timestamps BEFORE Phase 06.1 apply"
for SECRET in openai-api-key langfuse-public-key langfuse-secret-key seeded-user-entra-oid; do
  TS=$(az keyvault secret show --vault-name "$KV_NAME" --name "$SECRET" --query 'attributes.updated' -o tsv)
  echo "$SECRET: $TS"
done
```
Capture this output for the evidence file.

**Verification step 3 — capture live ACA container resources + auth env vars:**
```bash
echo "## Live ACA container.resources BEFORE apply"
az containerapp show --name jobrag-prod-aca --resource-group jobrag-prod-rg \
  --query 'properties.template.containers[0].resources' -o jsonc

echo ""
echo "## Live ACA container.env auth values (lengths only, NOT values)"
for VAR in BACKEND_AUDIENCE ENTRA_TENANT_ID ENTRA_TENANT_SUBDOMAIN; do
  LEN=$(az containerapp show --name jobrag-prod-aca --resource-group jobrag-prod-rg \
    --query "properties.template.containers[0].env[?name=='$VAR'].value | [0]" -o tsv | wc -c)
  echo "$VAR length: $LEN"
done
```
Capture this output.

**Verification step 4 — the actual no-op proof (THE D-08 gate):**
```bash
# From repo root — uses the wrapper from Plan 03
bash scripts/tf-apply-prod.sh plan -detailed-exitcode
echo ""
echo "=== EXIT CODE: $? ==="
```

OR, equivalently:
```bash
cd infra/envs/prod
terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -detailed-exitcode
echo ""
echo "=== EXIT CODE: $? ==="
```

**Expected:**
- Exit code: **`0`** (no changes — verification PASSES)
- Summary line in the plan output: `Plan: 0 to add, 0 to change, 0 to destroy.`
- No resources listed under "Resource actions" / "Terraform will perform the following actions"

**If exit code is 2 (changes present):**
- Capture the FULL plan output verbatim (paste into the evidence file under a "## FAILED — diff observed" heading).
- Inspect the diff and route to the responsible plan:
  - Diff in `azurerm_key_vault_secret.*` → Plan 01 defect (re-check `ignore_changes = [value, value_wo, value_wo_version]` on all 4 resources)
  - Diff in `module.compute.azurerm_container_app.api.template[0].container[0].resources` → Plan 02 defect (cpu/memory defaults don't match live; bump prod.tfvars to whatever the live revision actually has)
  - Diff in `module.compute.azurerm_container_app.api.template[0].container[0].env` for BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN → prod.tfvars.local missing or stale; rebuild via `bash scripts/refresh-external-outputs.sh` and re-run.
  - Any other diff → unrelated drift (possibly portal edits or image_tag drift per Phase 3 B5 — these are expected and DOCUMENTED-OK in the evidence file; do NOT count toward this plan's pass/fail).
- DO NOT run `terraform apply` until the diff is reconciled.

**Verification step 5 — record evidence:**
Create `.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md`
with the following structure. Fill in real values from steps 1-4 above. Treat each
question as "answer in the file".

```markdown
# Phase 06.1 — Verification Evidence (D-08)

**Captured:** YYYY-MM-DD by Adrian
**Source:** Live `terraform plan` against prod state from Adrian's local az login

## Prerequisites

- `az account show` tenant: `<tenantId>` (workforce, per azure-tenant-split memory)
- `infra/envs/prod/prod.tfvars.local` present with backend_audience / entra_tenant_id / entra_tenant_subdomain / home_ip lines (count: `<N>`)
- `terraform init -input=false` from `infra/envs/prod/` succeeded

## Wrapper sanity (no Azure)

- `bash scripts/tf-apply-prod.sh` (no subcommand) → exit `<EC>` (expected 3)
- `bash scripts/tf-apply-prod.sh plan` with prod.tfvars.local moved aside → exit `<EC>` (expected 2) — OPTIONAL test, mark SKIPPED if not run

## Live state BEFORE the plan

### KV secret timestamps (must NOT change after a future apply per D-01/D-02)

| Secret | Last updated |
|--------|--------------|
| openai-api-key | `<TS>` |
| langfuse-public-key | `<TS>` |
| langfuse-secret-key | `<TS>` |
| seeded-user-entra-oid | `<TS>` |

### ACA container resources (must match D-03 defaults: cpu=1.0, memory=2Gi)

```json
<paste az containerapp show output here>
```

### ACA auth env var lengths (must all be non-zero)

| Var | Length |
|-----|--------|
| BACKEND_AUDIENCE | `<N>` |
| ENTRA_TENANT_ID | `<N>` |
| ENTRA_TENANT_SUBDOMAIN | `<N>` |

## `terraform plan -detailed-exitcode` outcome (the D-08 gate)

- Command: `bash scripts/tf-apply-prod.sh plan -detailed-exitcode`
- Exit code: `<EC>`
- Summary line from plan output:
  ```
  Plan: <N> to add, <N> to change, <N> to destroy.
  ```

### PASS criterion
Exit code = 0 AND summary line = `Plan: 0 to add, 0 to change, 0 to destroy.`

### If FAIL (exit code = 2)
Full plan output (paste verbatim):
```
<paste output>
```
Routing:
- [ ] Diff in `azurerm_key_vault_secret.*` → Plan 01 defect
- [ ] Diff in container resources → Plan 02 defect
- [ ] Diff in container auth env vars → prod.tfvars.local stale or missing
- [ ] Other diff (unrelated drift) → documented-OK per Phase 3 B5

## Conclusion

[PASS | FAIL] — Phase 06.1 plans 01+02+03 produce a zero-drift `terraform plan` against
live prod state. Phase is ready for `bash scripts/tf-apply-prod.sh apply` if/when Adrian
chooses to materialize the changes against the live state. (Note: the changes are TF-level
only — value_wo + cpu/memory module wiring — so the apply itself is also expected to be a
near-no-op except for refreshing state metadata.)
```

**Once the evidence file is written and `Conclusion: PASS`:** type "approved" to resume.

If `Conclusion: FAIL`: type the responsible plan number(s) (e.g., "FAIL plan 02 — cpu
mismatch") so the orchestrator can route the fix back to the right plan.
  </how-to-verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md` succeeds
    - `grep -q "Plan: 0 to add, 0 to change, 0 to destroy" 06.1-VERIFICATION-EVIDENCE.md` succeeds (no-op proof captured)
    - `grep -q "Conclusion" 06.1-VERIFICATION-EVIDENCE.md` succeeds
    - `grep -E "Conclusion.*PASS|Conclusion.*FAIL" 06.1-VERIFICATION-EVIDENCE.md` returns at least 1 line
    - File contains KV secret timestamps table (4 rows: openai-api-key, langfuse-public-key, langfuse-secret-key, seeded-user-entra-oid)
    - File contains ACA container resources block matching `{"cpu": 1.0, "memory": "2Gi"}` (or notes the mismatch + routes to Plan 02)
    - File contains BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN length table — all 3 lengths non-zero (or routes to Plan 03 fix path)
    - Adrian replies "approved" to the resume-signal
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark Phase 06.1 verification PASSED, or "FAIL plan NN — &lt;reason&gt;" if the plan output shows unexpected drift.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo edits ↔ live Azure state | The plan verification is the GATE that proves the repo edits actually behave as intended against live state |
| Adrian's local az login ↔ workforce tenant subscription | Plan execution requires the workforce-tenant context per azure-tenant-split memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-11 | Tampering | KV secret integrity | mitigate | Step 2 captures KV secret last-updated timestamps as a baseline. Adrian compares them BEFORE and AFTER any future apply to confirm Plan 01's lifecycle blocks held. |
| T-06.1-12 | Information Disclosure | Evidence file contents | accept | The evidence file captures secret last-updated TIMESTAMPS only (never the secret values), env var LENGTHS only (never the values themselves). No new disclosure. The verification-evidence.md file lives under `.planning/phases/06.1-…/` which IS committed; the timestamp + length data is non-sensitive operational metadata. |
| T-06.1-13 | Elevation of Privilege | n/a | n/a | Verification is read-only (plan, az show); no apply. No privilege change. |
</threat_model>

<verification>
- `.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-VERIFICATION-EVIDENCE.md` exists
- The file contains the exit code from `terraform plan -detailed-exitcode`
- The file contains the summary line from the plan output
- The file's "Conclusion" line reads `PASS` (or routes to the failing plan)
- KV secret timestamps captured (4 rows)
- ACA container resources block captured + matches `{"cpu": 1.0, "memory": "2Gi"}`
- ACA auth env var lengths captured (3 rows, all non-zero)
</verification>

<success_criteria>
- Adrian's local `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -detailed-exitcode` against live prod state exits 0
- Plan summary line: `Plan: 0 to add, 0 to change, 0 to destroy.` (modulo expected B5 image_tag drift, which is documented in evidence file)
- 06.1-VERIFICATION-EVIDENCE.md committed with all captured evidence + PASS conclusion
- Phase 06.1 is shippable: Plans 01+02+03 produce zero unintended drift against live state; running the actual `terraform apply` is now safe (and itself a near-no-op)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-/06.1-04-SUMMARY.md` summarizing the verification outcome.
</output>
