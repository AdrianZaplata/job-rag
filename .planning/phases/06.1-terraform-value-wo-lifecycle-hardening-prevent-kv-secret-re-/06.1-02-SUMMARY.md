---
phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-
plan: 02
subsystem: infra

tags: [terraform, azure-container-apps, cpu, memory, parameterization, tfvars, file-pyramid]

requires:
  - phase: 06-chat
    provides: "06-UAT-DEBUG-HANDOFF Bug #4 forensics — proved 0.5 vCPU / 1Gi was OOM-killing the agent during cross-encoder preload + astream_events; Adrian manually bumped the live revision to 1.0 / 2Gi via az containerapp update"
  - phase: 03-infrastructure-ci-cd
    provides: "infra/modules/compute scaffold (the hardcoded cpu=0.5, memory=\"1Gi\" literals at main.tf:104-105 introduced by Plan 03-05a's compute module bring-up)"
  - phase: 06.1-plan-01
    provides: "non-overlapping edit region in infra/envs/prod/main.tf (Plan 01 owns lines 138-184 KV-secret resources; Plan 02 owns lines 188-219 module \"compute\" block) — merge-safe within Wave 1"

provides:
  - "infra/modules/compute exposes cpu (number, default 1.0) + memory (string, default \"2Gi\") variables with Azure Consumption-profile (1:2 cpu:memory) validation"
  - "infra/envs/prod surfaces matching cpu/memory variables and threads them through the module \"compute\" block"
  - "infra/envs/prod/prod.tfvars commits cpu = 1.0 + memory = \"2Gi\" as the version-controlled source of truth — container size now PR-visible instead of buried in az containerapp update history"
  - "infra/envs/prod/prod.tfvars carries the D-09 file-pyramid header comment documenting which values belong in committed tfvars vs gitignored .local vs TF_VAR_* env vars vs out-of-band az keyvault secret set"
  - "Foundation for Plan 04 (live terraform plan no-op verification) — the container resource MUST show zero diff against the live revision once Adrian re-applies"

affects: [phase-06.1-plan-03, phase-06.1-plan-04]

tech-stack:
  added: []
  patterns:
    - "Module-variable parameterization with Azure Consumption-profile validation — contains() lists enforce the cpu:memory 1:2 ratio at plan time, blocking invalid combinations (e.g., cpu=1.0, memory=1Gi) before any apply touches the live container."
    - "tfvars file pyramid (D-09) — 4 tiers of value placement documented inline at the top of prod.tfvars: committed tfvars (deployment-stable config) → gitignored .local (per-machine/rotating) → TF_VAR_* env vars (true secrets) → out-of-band az keyvault secret set (runtime secrets that survive TF apply via Plan 01's value_wo lifecycle hardening)."

key-files:
  created: []
  modified:
    - infra/modules/compute/variables.tf
    - infra/modules/compute/main.tf
    - infra/envs/prod/variables.tf
    - infra/envs/prod/main.tf
    - infra/envs/prod/prod.tfvars

key-decisions:
  - "Honored D-03 verbatim: cpu/memory variables added to compute module with the spelled-out defaults (1.0 / \"2Gi\") and Azure Consumption-profile contains() validation lists. Hardcoded literals at main.tf:104-105 replaced with var.cpu / var.memory."
  - "Honored D-04 verbatim: matching cpu/memory variables surfaced at the env composition layer with identical descriptions (so help text travels with the variable wherever an operator reads it — no need to chase the module definition). Module inputs threaded through grouped just below tags = local.tags and above the existing Phase 4 D-04 auth-env-vars block per the plan's exact insertion point."
  - "Honored D-09 verbatim: file-pyramid header comment prepended to prod.tfvars listing all 4 tiers (committed tfvars / gitignored .local / TF_VAR_* / az keyvault secret set) with the canonical apply command + a forward-reference to the Plan 03 wrapper script (scripts/tf-apply-prod.sh)."
  - "Used exact Edit blocks (not replace_all) on every edit — each edit had unique surrounding context (last variable block in each file; exact tags = local.tags + blank line + Phase 4 D-04 comment region in main.tf; exact image_tag = \"latest\" line in prod.tfvars). Atomic, deterministic, no risk of partial application."
  - "Did NOT touch the 4 azurerm_key_vault_secret resources at infra/envs/prod/main.tf lines 138-184 (Plan 01's territory). Verified via `git diff infra/envs/prod/main.tf | grep -c \"azurerm_key_vault_secret\"` = 0."

patterns-established:
  - "ACA size parameterization — any future Container App module variable for cpu/memory MUST carry the same contains() validation lists (cpu in [0.25, 0.5, 0.75, 1.0, 1.25, 1.5, 1.75, 2.0]; memory in [0.5Gi…4Gi at 0.5Gi steps]) so plan-time errors catch invalid Azure Consumption-profile combinations before apply."
  - "tfvars file pyramid header — any new env-level tfvars file under infra/envs/{env}/ SHOULD carry an analogous 4-tier inline header so operators immediately know where each kind of value belongs."

requirements-completed: []

duration: 2m 50s
completed: 2026-05-26
---

# Phase 06.1 Plan 02: ACA cpu/memory parameterization + tfvars discipline Summary

**Replaced the hardcoded `cpu = 0.5` / `memory = "1Gi"` literals in `infra/modules/compute/main.tf` with `var.cpu` / `var.memory`, surfaced matching variables at the prod env composition layer, committed `cpu = 1.0` + `memory = "2Gi"` to `prod.tfvars`, and prepended the D-09 file-pyramid header — so a bare `terraform apply` can no longer revert Adrian's manual Phase 06 UAT bump that fixed the cross-encoder OOM-kill (Bug #4).**

## Performance

- **Duration:** 2 min 50 s (170 s)
- **Started:** 2026-05-26T08:47:03Z
- **Completed:** 2026-05-26T08:49:53Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Module layer parameterized: `infra/modules/compute/variables.tf` now declares `cpu` (number, default 1.0) + `memory` (string, default "2Gi") with Azure Consumption-profile validation; `infra/modules/compute/main.tf` lines 104-105 use `var.cpu` / `var.memory` — zero hardcoded literals remain.
- Env layer surfaced: `infra/envs/prod/variables.tf` declares matching variables with identical descriptions + validation; `infra/envs/prod/main.tf` `module "compute"` block (lines 204-208) threads `cpu = var.cpu` + `memory = var.memory` through, grouped just below `tags = local.tags` and above the Phase 4 D-04 auth-env-vars block.
- Source of truth committed: `infra/envs/prod/prod.tfvars` carries `cpu = 1.0` + `memory = "2Gi"` (matches the live revision Adrian bumped to during Phase 06 UAT M1) plus the D-09 file-pyramid header documenting the 4-tier value-placement convention.
- Scope-boundary preserved: zero `azurerm_key_vault_secret` references in the `infra/envs/prod/main.tf` diff — Plan 01's region (lines 138-184) is byte-identical to its post-Plan-01 state.
- `terraform fmt -check` exits 0 in both `infra/modules/compute/` and `infra/envs/prod/`.
- `terraform validate` exits 0 in `infra/envs/prod/` (the pre-existing transitive AVM deprecation warning on `azurerm_log_analytics_workspace.local_authentication_disabled` from `module.monitoring.log_analytics` is out of scope — same warning Plan 01 documented).

## Task Commits

Each task was committed atomically:

1. **Task 1: Parameterize ACA cpu/memory in compute module** — `c5e7431` (feat)
2. **Task 2: Wire ACA cpu/memory through prod env layer + commit defaults** — `fcbfc24` (feat)

_Plan metadata commit follows separately at end of execution._

## Files Created/Modified

- `infra/modules/compute/variables.tf` — appended `cpu` + `memory` variables with Azure Consumption-profile `contains()` validation (cpu in [0.25, 0.5, 0.75, 1.0, 1.25, 1.5, 1.75, 2.0]; memory in [0.5Gi, 1Gi, 1.5Gi, 2Gi, 2.5Gi, 3Gi, 3.5Gi, 4Gi]) and the D-03 explanatory comment block.
- `infra/modules/compute/main.tf` — lines 104-105 changed from `cpu = 0.5` / `memory = "1Gi"` to `cpu = var.cpu` / `memory = var.memory`. All other attributes in the `container { … }` block and the surrounding `template { … }` block (min_replicas / max_replicas / termination_grace_period_seconds) untouched.
- `infra/envs/prod/variables.tf` — appended matching `cpu` + `memory` variables with verbatim-copied descriptions and identical validation (so help text travels with the variable wherever an operator reads it per D-04 reasoning) plus the D-04 explanatory comment block.
- `infra/envs/prod/main.tf` — `module "compute"` block (lines 204-208 in the post-edit file) gained `cpu = var.cpu` + `memory = var.memory` inputs grouped below `tags = local.tags` and above the Phase 4 D-04 auth-env-vars block. **The 4 `azurerm_key_vault_secret` resources at lines 138-184 (Plan 01's territory) are byte-identical.**
- `infra/envs/prod/prod.tfvars` — prepended the D-09 4-tier file-pyramid header at top (above the existing `# Adrian's prod environment values.` line); appended `cpu = 1.0` + `memory = "2Gi"` section after `image_tag = "latest"` with the D-04 explanatory comment linking back to the Phase 06 UAT Bug #4 forensics.

## Decisions Made

- **D-03 + D-04 + D-09 applied verbatim from `.planning/phases/06.1-.../06.1-CONTEXT.md`.** No new decisions emerged during execution — the plan was a deterministic mechanical edit and the surrounding context was already pinned by the Phase 06 UAT Bug #4 forensics in 06-UAT-DEBUG-HANDOFF.md.
- **Description verbatim-copy at module + env layer is intentional** (D-04 reasoning): an operator reading `infra/envs/prod/variables.tf` to figure out what `cpu` does does not need to bounce to `infra/modules/compute/variables.tf` — the help text + validation + defaults are identical at both layers. The trade-off (slight DRY violation) buys local-readability at the operator's eye-level point of entry.

## Deviations from Plan

None. The plan's executor work was completed exactly as specified.

The acceptance criteria as written required exact-string `grep` matches on long literal strings (e.g., `grep -q "contains(\[0.25, 0.5, 0.75, 1.0, 1.25, 1.5, 1.75, 2.0\], var.cpu)"`). All such matches passed on the first run — the verbatim-copied D-03 / D-04 blocks landed in both files exactly as the planner specified them.

---

**Total deviations:** 0 code deviations. 0 acceptance-criteria notes.
**Impact on plan:** None. The substantive requirements (module-layer parameterization with validation; env-layer surface + module input wiring; committed source-of-truth values in tfvars; D-09 header; `terraform fmt -check` + `terraform validate` both green; Plan 01's KV-secret region untouched) are all satisfied. The diff across 5 files is precisely what the planner specified.

## Issues Encountered

None. The 5 edits were all deterministic surrounding-context-anchored Edit blocks with no environmental friction. `terraform validate` surfaced the same pre-existing deprecation warning Plan 01 documented (`azurerm_log_analytics_workspace.local_authentication_disabled` transitive AVM from `module.monitoring.log_analytics`) — out of scope per scope-boundary rules; logged for awareness only.

## User Setup Required

None — no external service configuration required. The variable changes take effect on the NEXT `terraform apply` Adrian runs against prod; until then, the live container template is unchanged. Plan 04 is the verification gate that confirms the live `terraform plan` is a no-op for the container resource (cpu = 1.0 / memory = "2Gi" in the live revision matches the new tfvars-committed defaults).

## Next Phase Readiness

- **Plan 03 (README + scripts + CI audit):** unblocked. It edits `infra/envs/prod/README.md`, `scripts/refresh-swa-origin.sh`, and audits `.github/workflows/deploy-infra.yml` + creates `scripts/tf-apply-prod.sh`. No overlap with this plan's files. The D-09 header comment in `prod.tfvars` already forward-references `scripts/tf-apply-prod.sh` (Plan 03 will create that wrapper).
- **Plan 04 (live `terraform plan` no-op verification, `autonomous: false`):** dependency satisfied. Adrian's local apply against the live prod tenant must show **zero diff** on the `azurerm_container_app.api` resource's `template[0].container[0].resources` block after Plans 01 + 02 + 03 land. If Plan 04 surfaces ANY container-size diff (cpu or memory), this plan's defaults are wrong (would re-tune until live matches). The defaults committed here (1.0 / 2Gi) match the live revision Adrian bumped to during Phase 06 UAT M1 per 06-UAT-DEBUG-HANDOFF Bug #4, so no diff is expected.

## Self-Check: PASSED

- `infra/modules/compute/variables.tf` modified — FOUND (variable cpu + memory present, defaults 1.0 / "2Gi", validation `contains()` present for both)
- `infra/modules/compute/main.tf` modified — FOUND (`cpu = var.cpu` + `memory = var.memory` present; no `cpu = 0.5` or `memory = "1Gi"` remaining)
- `infra/envs/prod/variables.tf` modified — FOUND (variable cpu + memory present, defaults 1.0 / "2Gi")
- `infra/envs/prod/main.tf` modified — FOUND (`cpu = var.cpu` + `memory = var.memory` inside `module "compute"` block; zero `azurerm_key_vault_secret` references in diff)
- `infra/envs/prod/prod.tfvars` modified — FOUND (D-09 file-pyramid header at top; `cpu = 1.0` + `memory = "2Gi"` present; D-09 tier-4 `Out-of-band via az keyvault secret set` line present)
- Commit `c5e7431` (Task 1) exists in git log — FOUND
- Commit `fcbfc24` (Task 2) exists in git log — FOUND
- `terraform fmt -check` in `infra/modules/compute/` exit 0 — PASS
- `terraform fmt -check` in `infra/envs/prod/` exit 0 — PASS
- `terraform validate` in `infra/envs/prod/` exit 0 — PASS
- `git diff infra/envs/prod/main.tf | grep -c "azurerm_key_vault_secret"` = 0 — PASS (Plan 01's region untouched)

---
*Phase: 06.1-terraform-value-wo-lifecycle-hardening-prevent-kv-secret-re-*
*Completed: 2026-05-26*
