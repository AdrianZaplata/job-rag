# Phase 06.1 — Verification Evidence (D-08)

**Captured:** 2026-05-26 by Adrian
**Source:** Live `terraform plan` against prod state from Adrian's local az login (workforce tenant)

## Prerequisites

- `az account show` tenant: workforce tenant (per `azure-tenant-split.md`); subscription owns `jobrag-prod-rg` (state: `Succeeded`, location: `westeurope`)
- `infra/envs/prod/prod.tfvars.local` present with `backend_audience` / `entra_tenant_id` / `entra_tenant_subdomain` / `home_ip` (verified by non-empty BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN on live ACA — see "ACA auth env var lengths" below)
- `terraform init -input=false` from `infra/envs/prod/` succeeded
- Live RG `jobrag-prod-rg` contains: `jobrag-prod-spa`, `jobrag-prod-kv`, `jobrag-prod-law`, `jobrag-prod-aca-env`, `jobrag-prod-pg-ie`, `jobrag-prod-api` (the actual ACA name — not `jobrag-prod-aca`)

## Wrapper sanity (no Azure)

- `bash scripts/tf-apply-prod.sh` (no subcommand) → exit `3` (expected 3 — usage printed to stderr) **PASS**
- Missing-file detection test → SKIPPED (prod.tfvars.local is the canonical local file; renaming risks bricking workstation state)

## Live state BEFORE the plan

### KV secret timestamps (must NOT change after a future apply per D-01/D-02)

| Secret | Last updated |
|--------|--------------|
| openai-api-key | 2026-05-24T12:37:27+00:00 |
| langfuse-public-key | 2026-05-24T20:04:20+00:00 |
| langfuse-secret-key | 2026-05-24T20:04:46+00:00 |
| seeded-user-entra-oid | 2026-05-23T20:24:26+00:00 |

These timestamps establish the baseline. After any future `terraform apply`, re-running `az keyvault secret show --vault-name jobrag-prod-kv --name <SECRET> --query attributes.updated -o tsv` must return the same value for all 4 secrets — proving Plan 01's `lifecycle.ignore_changes = [value, value_wo, value_wo_version]` actually held under live `terraform apply`.

### ACA container resources (Plan 02 verification: must match cpu=1.0, memory=2Gi)

Queried via `az containerapp show --name jobrag-prod-api --resource-group jobrag-prod-rg --query 'properties.template.containers[0].resources' -o jsonc`:

```json
{
  "cpu": 1.0,
  "ephemeralStorage": "4Gi",
  "memory": "2Gi"
}
```

`cpu` and `memory` match Plan 02's defaults verbatim. `ephemeralStorage` (4Gi) is Azure's automatic allocation for the cpu=1.0/memory=2Gi Consumption profile and is not managed by Plan 02 — neither `infra/modules/compute/main.tf` nor `infra/envs/prod/prod.tfvars` declares it. Confirmed no-op in the `terraform plan` (exit code 0 below).

### ACA auth env var lengths (Plan 03 verification: must all be non-zero)

Queried via `az containerapp show --name jobrag-prod-api ... --query "properties.template.containers[0].env[?name=='<VAR>'].value | [0]" -o tsv | wc -c`:

| Var | Length (chars + trailing newline) |
|-----|-----------------------------------|
| BACKEND_AUDIENCE | 43 |
| ENTRA_TENANT_ID | 37 |
| ENTRA_TENANT_SUBDOMAIN | 7 |

All three non-zero — confirms `prod.tfvars.local` was threaded correctly into the live container template. Bug #3 closure validated against actual live state: a hypothetical `terraform apply -var-file=prod.tfvars` (single var-file) would have produced a diff zeroing these three values; the dual-var-file invocation via the new `scripts/tf-apply-prod.sh` wrapper preserves them.

## `terraform plan -detailed-exitcode` outcome (the D-08 gate)

- Command: `bash scripts/tf-apply-prod.sh plan -detailed-exitcode`
- Wrapper resolves to: `terraform plan -var-file=prod.tfvars -var-file=prod.tfvars.local -detailed-exitcode` (executed inside `infra/envs/prod/`)
- **Exit code: `0`**
- `-detailed-exitcode` semantics: `0` = no changes; `1` = error; `2` = changes present
- No `Resource actions` block emitted; no `Terraform will perform the following actions` header. Exit code 0 alone is the no-op proof.

### Non-blocking warnings observed (all pre-existing, none in scope for Phase 06.1)

1. **`api_audience` undeclared variable.** `prod.tfvars.local` carries a value for `api_audience` that no root-module variable declares. Pre-existing — predates 06.1. Phase 4 / Phase 6 carryover; tracked separately.
2. **AzureRM provider deprecation: `enable_rbac_authorization` → `rbac_authorization_enabled`** (in `module.kv.key_vault`). Pre-existing AzureRM AVM module; not in scope for the value_wo hardening.
3. **`local_authentication_disabled` deprecated value** (Log Analytics workspace in `module.monitoring`). Pre-existing AVM module; not in scope.

None of these warnings produce a diff in the plan; the exit code is 0 unambiguously.

### PASS criterion

Exit code = 0 AND no resource changes listed. **MET.**

## Conclusion

**PASS** — Phase 06.1 plans 01+02+03 produce a zero-drift `terraform plan` against live prod state.

- Plan 01 (KV `lifecycle.ignore_changes` hardening) — verified: the live KV secret timestamps (above) serve as the baseline; the plan is a no-op against the 4 KV secret resources because the new `[value, value_wo, value_wo_version]` ignore list already protects every attribute that would have triggered a write.
- Plan 02 (cpu/memory parameterization) — verified: live container resources `{cpu: 1.0, memory: "2Gi"}` match the committed defaults in `prod.tfvars` byte-for-byte; bare `terraform apply` produces no container-size diff.
- Plan 03 (tfvars discipline + CI parity) — verified: live container template carries the BACKEND_AUDIENCE / ENTRA_TENANT_ID / ENTRA_TENANT_SUBDOMAIN values from `prod.tfvars.local`; the dual-var-file wrapper (`scripts/tf-apply-prod.sh`) produces a zero-diff plan. (Additionally: `cdf3ba3` corrected an in-flight typo in the CI pre-apply guard's container app name — `jobrag-prod-aca` → `jobrag-prod-api` — caught during this verification step before the guard could fire in a real CI run.)

Phase 06.1 is shippable. Running the actual `bash scripts/tf-apply-prod.sh apply` against the live state is now safe and itself a near-no-op (state refresh only; no resource mutations expected).
