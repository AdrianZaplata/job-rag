---
quick_id: 260505-j0p
slug: drive-static-tf-to-fully-green-bump-tf-v
description: Drive static-tf to fully green — bump TF version + sweep fmt/tflint/tfsec/validate
date: 2026-05-05
status: planned
---

# Quick Task 260505-j0p: Drive static-tf CI to fully green on PR #4

## Context

After 260505-h1p closed the tfsec TLS finding, `static-tf` failed on
`Terraform validate (envs/prod)` because the AVM module
`Azure/avm-res-dbforpostgresql-flexibleserver/azurerm 0.2.2` declares
`variable "administrator_password_wo" { ephemeral = true }`, which requires
Terraform ≥ 1.10. CI is pinned at 1.9.5.

This task drives `static-tf` end-to-end green: bump CI TF pin, sweep all four
gates locally, fix every finding atomically, push, repeat until CI is green,
then promote UAT Test 2 to `pass`.

PR: https://github.com/AdrianZaplata/job-rag/pull/4
Branch: test/static-tf-smoke
Phase: 03-infrastructure-ci-cd (UAT Test 2)

## Local tooling

- terraform v1.15.0 ✓
- tflint v0.62.0 ✓
- tfsec ✗ NOT INSTALLED locally — rely on CI for tfsec gate.
  (acceptable: at most one extra CI round-trip if a finding surfaces;
  same outcome as local-first sweep.)

## Tasks

### Task 1: Bump CI Terraform version

- **Files:** `.github/workflows/static-tf.yml`
- **Action:** `terraform_version: 1.9.5` → `terraform_version: 1.15.0`
- **Commit:** `chore(ci): bump terraform_version to 1.15.0`

### Task 2: Local sweep — fmt

- **Action:** `terraform fmt -check -recursive infra/` from repo root.
- **If drift:** Run `terraform fmt -recursive infra/` and commit per area.
- **Commit pattern:** `style(infra/<area>): terraform fmt`

### Task 3: Local sweep — validate (per env)

- **Action:** For each of `infra/bootstrap`, `infra/envs/prod`, `infra/envs/dev`:
  `terraform init -backend=false -input=false && terraform validate`.
- **Fix policy:** Missing required attribute / wrong type → fix; PAUSE if shape is wrong.
- **Commit pattern:** `fix(infra/<area>): <one-line>`

### Task 4: Local sweep — tflint

- **Action:** `cd infra && tflint --init && tflint --recursive --config="$(pwd)/.tflint.hcl"`.
- **Fix policy:** Default to fixing; PAUSE if relaxing a rule.
- **Commit pattern:** `fix(infra/<area>): <one-line>`

### Task 5: Push and watch CI

- **Action:** `git push origin test/static-tf-smoke && gh pr checks 4 --watch`
  (also carries local-only docs commit a2cf2f5 from 260505-h1p).
- **If CI surfaces tfsec or any new finding:** fix it (same atomic-commit rules)
  and re-push. PAUSE only on the four conditions in PAUSE section below.

### Task 6: When fully green — promote UAT

- **Files:** `.planning/phases/03-infrastructure-ci-cd/03-UAT.md`
- **Action:**
  - Test 2 result: change `issue` → `pass`; remove `reported` and `severity` fields.
  - Summary counts: passed `1 → 2`, issues `1 → 0`, pending `16` unchanged.
  - Gaps section: remove the Test 2 entry; if no other entries remain, replace
    Gaps body with `[none yet]`.
- **Commit:** included in the final docs commit alongside the quick-task artifacts.

## must_haves

- **Truths:**
  - CI Terraform version supports AVM `ephemeral` (≥ 1.10).
  - `static-tf` workflow on PR #4 is fully green end-to-end (fmt + tflint + tfsec
    + per-env validate) on the run that lands after this push.
  - UAT Test 2 reflects the live CI status.
- **Artifacts:**
  - `.github/workflows/static-tf.yml` (TF version bump)
  - Any infra files touched by fmt/validate/tflint fixes
  - `.planning/phases/03-infrastructure-ci-cd/03-UAT.md` (Test 2 → pass)
  - `${QUICK_DIR}/260505-j0p-SUMMARY.md`
- **Key links:**
  - PR #4
  - Prior quick task 260505-h1p (tfsec fix that surfaced this validate failure)

## PAUSE conditions (ask Adrian before acting)

- A tfsec finding whose fix would change a documented design decision (D-10 / A1 / A4).
- A validate failure suggesting the WRONG resource shape (not a missing arg).
- Any finding requiring relaxing a tflint/tfsec rule.
- Any finding outside `infra/` or `.github/workflows/static-tf.yml`.

## Out of scope

- `src/job_rag/config.py`, `src/job_rag/services/ingestion.py` ruff errors
  (pre-existing `lint-and-test` failures, not Phase 3).
