---
phase: quick-260527-q87
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/external/variables.tf
  - infra/external/terraform.tfvars.example
  - infra/external/README.md
  - infra/modules/identity/variables.tf
  - infra/envs/prod/main.tf
  - infra/envs/dev/main.tf
autonomous: true
requirements:
  - CI-STATIC-TF-GREEN
must_haves:
  truths:
    - "tflint reports 0 issues across infra/ (no terraform_unused_declarations warnings remain)"
    - "Both deleted variables (logout_redirect_uri, swa_origin in identity module) leave zero references in any *.tf file"
    - "Env-level var.swa_origin in infra/envs/{prod,dev}/variables.tf is PRESERVED (still consumed by locals.tf CORS path + infra/external/ invocation)"
    - "terraform fmt -check passes on all touched files"
    - "static-tf GitHub Actions check on PR #11 (docs/05-verify-work-close-out) goes green on re-run"
  artifacts:
    - path: "infra/external/variables.tf"
      provides: "External-tenant input variables, MINUS the dead logout_redirect_uri block (lines 18-22 removed)"
      contains: "tenant_id_external, environment, spa_redirect_uris (and no `logout_redirect_uri`)"
    - path: "infra/external/terraform.tfvars.example"
      provides: "Example tfvars for the external module, MINUS line 10's logout_redirect_uri row"
      contains: "tenant_id_external, environment, spa_redirect_uris (no logout_redirect_uri row)"
    - path: "infra/external/README.md"
      provides: "External-tenant runbook with the dead logout_redirect_uri bullet stripped from the Step 1 tfvars comment block"
      contains: "Step 1 — Prepare tfvars (no `logout_redirect_uri from same SWA origin` line)"
    - path: "infra/modules/identity/variables.tf"
      provides: "Identity module inputs MINUS the unused swa_origin declaration (lines 1-5 removed)"
      contains: "github_owner, github_repo, resource_group_id, kv_id, subscription_id (and no `swa_origin`)"
    - path: "infra/envs/prod/main.tf"
      provides: "Prod composition with the identity module call MINUS the `swa_origin = var.swa_origin` pass-through line"
      contains: 'module "identity" block with github_owner/github_repo/resource_group_id/kv_id/subscription_id (no swa_origin = var.swa_origin)'
    - path: "infra/envs/dev/main.tf"
      provides: "Dev composition with the identity module call MINUS the `swa_origin = var.swa_origin` pass-through line"
      contains: 'module "identity" block with github_owner/github_repo/resource_group_id/kv_id/subscription_id (no swa_origin = var.swa_origin)'
  key_links:
    - from: "infra/external/main.tf"
      to: "var.logout_redirect_uri"
      via: "NONE (this is the deletion criterion)"
      pattern: "logout_redirect_uri"
      expectation: "grep returns ZERO hits in infra/ *.tf after the deletion"
    - from: "infra/modules/identity/main.tf"
      to: "var.swa_origin"
      via: "NONE (this is the deletion criterion)"
      pattern: "swa_origin"
      expectation: "grep returns ZERO hits in infra/modules/identity/ *.tf after the deletion"
    - from: "infra/envs/{prod,dev}/main.tf module \"identity\" { ... } block"
      to: "removed pass-through"
      via: "argument list no longer contains `swa_origin = var.swa_origin`"
      pattern: "swa_origin = var.swa_origin"
      expectation: "grep returns ZERO hits in infra/envs/{prod,dev}/main.tf after the deletion"
    - from: "infra/envs/{prod,dev}/locals.tf (allowed_origins_csv CORS path)"
      to: "var.swa_origin (env-level)"
      via: "PRESERVED — locals.tf still consumes the env-level variable; do NOT delete `variable \"swa_origin\"` from infra/envs/{prod,dev}/variables.tf"
      pattern: "var.swa_origin"
      expectation: "infra/envs/prod/variables.tf and infra/envs/dev/variables.tf are UNTOUCHED by this plan"
---

<objective>
Unblock the `static-tf` GitHub Actions check on PR #11 (`docs/05-verify-work-close-out`) by deleting two Terraform variable declarations that tflint correctly flagged as unused. Both have been pre-confirmed dead by the orchestrator; this plan is delete-only, no rewiring.

Purpose: PR #11 is the Phase 6 close-out documentation PR. The `static-tf` job has been failing because tflint's `terraform_unused_declarations` rule (enabled in `infra/.tflint.hcl:27-29`) flags `logout_redirect_uri` (external/variables.tf:18) and `swa_origin` (modules/identity/variables.tf:1) as declared-but-unused — both are dead-code remnants of pre-existing scaffolding that was superseded by other Terraform plumbing.

Output: Six surgical edits across four file groups; the static-tf check transitions from RED to GREEN on the next CI run.

Why these are safe deletes (preserved here for SUMMARY.md traceability — DO NOT re-debate, decision is locked):

1. **`logout_redirect_uri`** (`infra/external/variables.tf:18-22`): Phase 4 D-12 (logout flow) is functionally complete via the SPA's `redirect_uris` list — UAT passed in `.planning/phases/04-frontend-shell-auth/04-06-SUMMARY.md:176` ("Sign-out: ✅ PASS"). This variable was scaffolded in Plan 04-01 against Pitfall 11 (Entra returning 400 on unregistered post-logout URI) but was never threaded into any `azuread_application_*` resource in `infra/external/main.tf`. Entra accepts post-logout redirects to URIs already in the SPA `redirect_uris` list — verified by working production logout. No future phase plans to wire it up.

2. **`swa_origin`** (`infra/modules/identity/variables.tf:1-5`): The identity module's own `main.tf` never reads `var.swa_origin` — confirmed by direct read of `infra/modules/identity/main.tf` (only references `var.github_owner`, `var.github_repo`, `var.resource_group_id`, `var.kv_id`, `var.subscription_id`). The real DEPL-12 two-pass for SWA-origin → SPA redirect_uris flows through `infra/external/` via the `spa_redirect_uris` list variable, NOT through the identity module. The identity module's pass-through was misrouted plumbing — a parameter that goes nowhere. **CRITICAL:** the env-level `var.swa_origin` in `infra/envs/{prod,dev}/variables.tf` is still consumed by `locals.tf` (CORS `allowed_origins_csv`) and by the `infra/external/` invocation — leave it alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@infra/external/variables.tf
@infra/external/main.tf
@infra/external/terraform.tfvars.example
@infra/external/README.md
@infra/modules/identity/variables.tf
@infra/modules/identity/main.tf
@infra/envs/prod/main.tf
@infra/envs/dev/main.tf
@infra/.tflint.hcl
@.github/workflows/static-tf.yml

<interfaces>
<!-- Exact lines/anchors the executor will be editing. Snippets are verbatim from the loaded files. -->

### Delete 1 — `infra/external/variables.tf` lines 18-22 (plus the blank line at 17)

Current text (verbatim, lines 17-22):
```hcl

variable "logout_redirect_uri" {
  type        = string
  description = "Post-logout redirect URI (D-12). Typically the SWA origin. Empty = no special logout target registered."
  default     = ""
}
```
After deletion: file ends at line 16 (the closing `}` of `spa_redirect_uris`). Drop the trailing blank line at 17 so the file ends cleanly with one terminating newline after the `}`.

### Delete 2 — `infra/external/terraform.tfvars.example` line 10

Current text (verbatim, line 10):
```hcl
logout_redirect_uri = "https://YOUR-SWA-DEFAULT-HOSTNAME.azurestaticapps.net"
```
After deletion: file ends at line 9 (the closing `]` of `spa_redirect_uris`). Drop the line entirely; do not leave a blank trailing line beyond the file's single terminating newline.

### Delete 3 — `infra/external/README.md` line 25 (single comment line inside a fenced code block)

Current text (verbatim, lines 22-26):
```markdown
cp terraform.tfvars.example terraform.tfvars.local
# Edit terraform.tfvars.local:
#   - tenant_id_external from `terraform -chdir=../bootstrap output -raw tenant_id_external`
#   - spa_redirect_uris[1] from `terraform -chdir=../envs/prod output -raw swa_default_origin`
#   - logout_redirect_uri from same SWA origin (no trailing slash)
```
After deletion: remove the entire `#   - logout_redirect_uri from same SWA origin (no trailing slash)` line. Surrounding lines (the two tfvars-key bullets above) stay verbatim. Do NOT restructure the README.

### Delete 4 — `infra/modules/identity/variables.tf` lines 1-5 (plus the blank line at 6)

Current text (verbatim, lines 1-6):
```hcl
variable "swa_origin" {
  type        = string
  description = "Static Web App default origin (e.g. https://jobrag-prod-spa-xxxxxxxx.azurestaticapps.net). Empty string on first apply (DEPL-12 two-pass) — SPA redirect_uris filters empties via compact()."
  default     = ""
}

variable "github_owner" {
```
After deletion: file starts at the (formerly line-7) `variable "github_owner" {` block, with a single leading line that's the `variable "github_owner" {` declaration. No leading blank line.

### Delete 5 — `infra/envs/prod/main.tf` line 26 (inside `module "identity"`)

Current text (verbatim, lines 19-32 — anchor block):
```hcl
module "identity" {
  source = "../../modules/identity"

  providers = {
    azuread.workforce = azuread.workforce
  }

  swa_origin        = var.swa_origin
  github_owner      = var.github_owner
  github_repo       = var.github_repo
  resource_group_id = azurerm_resource_group.prod.id
  kv_id             = module.kv.kv_id                      # Gap 8.B: KV-scoped Secrets Officer for GHA SP
  subscription_id   = data.azurerm_subscription.current.id # Gap 8.D: sub-scoped Cost Mgmt Contributor (D-08 named exception)
}
```
After deletion: remove ONLY the line `  swa_origin        = var.swa_origin`. The remaining 5 argument lines must realign so `terraform fmt -check` passes (terraform fmt aligns `=` columns within argument blocks; after removing the longest left-key `swa_origin`, fmt will recompute the alignment to the next-longest key `resource_group_id` — that is correct and expected).

### Delete 6 — `infra/envs/dev/main.tf` line 36 (inside `module "identity"`)

Current text (verbatim, lines 29-42 — anchor block):
```hcl
module "identity" {
  source = "../../modules/identity"

  providers = {
    azuread.workforce = azuread.workforce
  }

  swa_origin        = var.swa_origin
  github_owner      = var.github_owner
  github_repo       = var.github_repo
  resource_group_id = azurerm_resource_group.dev.id
  kv_id             = module.kv.kv_id                      # parity with prod (Gap 8.B)
  subscription_id   = data.azurerm_subscription.current.id # parity with prod (Gap 8.D)
}
```
After deletion: remove ONLY the line `  swa_origin        = var.swa_origin`. Same fmt-realignment expectation as prod.

### What stays (negative-space contract — DO NOT touch)

- `infra/envs/prod/variables.tf` and `infra/envs/dev/variables.tf` — `variable "swa_origin"` at env scope is consumed by `locals.tf` (CORS `allowed_origins_csv`) AND by the `infra/external/` invocation. **NOT IN SCOPE.**
- `infra/envs/{prod,dev}/locals.tf` — references to `var.swa_origin` here are correct, leave them.
- Any other `.tf` file in the repo — strictly not in scope.
- `infra/external/README.md` structure, headings, prose — only the one bullet line goes.
- `infra/external/terraform.tfvars.example` lines 1-9 — verbatim, untouched.
- `infra/modules/identity/main.tf` — already does not reference `var.swa_origin`, no edit needed here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete two dead variables flagged by tflint (logout_redirect_uri, swa_origin) and their pass-through references</name>
  <files>
    infra/external/variables.tf,
    infra/external/terraform.tfvars.example,
    infra/external/README.md,
    infra/modules/identity/variables.tf,
    infra/envs/prod/main.tf,
    infra/envs/dev/main.tf
  </files>
  <action>
Make six surgical edits — delete-only, no additions, no rewording, no documentation explaining the deletion (the commit message + SUMMARY.md capture the why). The exact anchor text for each edit is in the `<interfaces>` block above.

**Edit 1 — `infra/external/variables.tf`:** Delete the `variable "logout_redirect_uri" { ... }` block (currently lines 18-22), plus the blank separator line at line 17. File should now end cleanly after the `spa_redirect_uris` block's closing `}` at line 16, with a single trailing newline.

**Edit 2 — `infra/external/terraform.tfvars.example`:** Delete line 10 (`logout_redirect_uri = "https://YOUR-SWA-DEFAULT-HOSTNAME.azurestaticapps.net"`). File should end at the `]` closing `spa_redirect_uris`.

**Edit 3 — `infra/external/README.md`:** Delete the single line `#   - logout_redirect_uri from same SWA origin (no trailing slash)` (currently line 25). Surrounding bullet comments stay verbatim. Do NOT restructure the README, do NOT collapse adjacent whitespace, do NOT add a replacement comment.

**Edit 4 — `infra/modules/identity/variables.tf`:** Delete the `variable "swa_origin" { ... }` block (currently lines 1-5) plus the blank line at 6. File should now start with `variable "github_owner" {` as its first line.

**Edit 5 — `infra/envs/prod/main.tf`:** Inside the `module "identity"` block (currently lines 19-32), delete ONLY the line `  swa_origin        = var.swa_origin` (currently line 26). The 5 remaining argument lines stay; do not add comments explaining what was removed. After deletion, run `terraform fmt infra/envs/prod/main.tf` locally if `terraform` is available — fmt will realign the `=` columns within the argument block (the longest left-key is now `resource_group_id`, which is correct and expected). If `terraform` is not available locally, the realignment will happen on the executor's next manual touch; rely on the CI fmt-check (step "Terraform fmt" in `.github/workflows/static-tf.yml:25-26`) to catch any drift — if CI fmt-check fails after this plan ships, that is the executor's followup, not a planning gap.

**Edit 6 — `infra/envs/dev/main.tf`:** Same as Edit 5 but on the dev composition — delete ONLY the line `  swa_origin        = var.swa_origin` (currently line 36) inside the `module "identity"` block (currently lines 29-42). Same fmt-realignment expectation.

**Hard constraints:**
- DO NOT touch `infra/envs/prod/variables.tf` or `infra/envs/dev/variables.tf` — the env-scoped `var.swa_origin` is still live for CORS (`locals.tf` → `allowed_origins_csv`) and for the `infra/external/` invocation.
- DO NOT touch `infra/modules/identity/main.tf` — it already does not reference `var.swa_origin`.
- DO NOT touch any other `.tf` file.
- DO NOT add new comments, TODOs, or explanatory prose to any file. The commit message and SUMMARY.md carry the rationale.
- DO NOT rename, refactor, reorder, or reformat anything outside the six surgical deletes (terraform fmt realignment of column-aligned `=` signs inside the touched `module "identity"` blocks IS allowed — that's automated by `terraform fmt`, not a manual edit).

**Order of operations:**
1. Edit files in any order — they are independent.
2. After all six edits, run the verification commands in `<verify>` below.
3. Only proceed to commit if all grep guards return EMPTY and (if available) `terraform fmt -check` + `tflint` + `terraform validate` all pass.
  </action>
  <verify>
    <automated>
# Run from repo root. Each command below is a hard gate — exit code 0 + expected output required before commit.

# Guard 1 — zero remaining references to logout_redirect_uri in any .tf file across infra/
grep -rn "logout_redirect_uri" infra/ --include="*.tf" ; test $? -eq 1 || { echo "FAIL: logout_redirect_uri still present in infra/ .tf files"; exit 1; }

# Guard 2 — zero remaining references to swa_origin inside the identity module's .tf files
grep -rn 'swa_origin' infra/modules/identity/ --include="*.tf" ; test $? -eq 1 || { echo "FAIL: swa_origin still present in infra/modules/identity/ .tf files"; exit 1; }

# Guard 3 — zero remaining pass-through invocations of swa_origin in the env compositions
grep -n 'swa_origin = var.swa_origin' infra/envs/prod/main.tf infra/envs/dev/main.tf ; test $? -eq 1 || { echo "FAIL: identity-module pass-through 'swa_origin = var.swa_origin' still present in env main.tf"; exit 1; }

# Guard 4 — negative-space: env-level variable "swa_origin" MUST still exist in both env variables.tf files
grep -q 'variable "swa_origin"' infra/envs/prod/variables.tf || { echo "FAIL: env-level variable \"swa_origin\" was incorrectly removed from infra/envs/prod/variables.tf"; exit 1; }
grep -q 'variable "swa_origin"' infra/envs/dev/variables.tf  || { echo "FAIL: env-level variable \"swa_origin\" was incorrectly removed from infra/envs/dev/variables.tf"; exit 1; }

# Guard 5 — negative-space: also confirm the README's logout_redirect_uri bullet is gone but the file still exists
grep -q 'logout_redirect_uri' infra/external/README.md && { echo "FAIL: logout_redirect_uri reference still present in infra/external/README.md"; exit 1; }
test -f infra/external/README.md || { echo "FAIL: infra/external/README.md was somehow removed"; exit 1; }

# Guard 6 — tfvars example line is gone but file is otherwise intact (still mentions tenant_id_external + spa_redirect_uris)
grep -q 'logout_redirect_uri' infra/external/terraform.tfvars.example && { echo "FAIL: logout_redirect_uri row still present in tfvars example"; exit 1; }
grep -q 'tenant_id_external' infra/external/terraform.tfvars.example || { echo "FAIL: tfvars example was over-edited (tenant_id_external missing)"; exit 1; }
grep -q 'spa_redirect_uris' infra/external/terraform.tfvars.example  || { echo "FAIL: tfvars example was over-edited (spa_redirect_uris missing)"; exit 1; }

# Optional gates — run only if the tool is installed locally; otherwise skip and rely on CI.

# Optional 1 — terraform fmt -check
command -v terraform >/dev/null && terraform fmt -check infra/external/variables.tf infra/modules/identity/variables.tf infra/envs/prod/main.tf infra/envs/dev/main.tf || echo "SKIP: terraform not installed locally; CI will gate fmt"

# Optional 2 — tflint recursive
command -v tflint >/dev/null && ( cd infra && tflint --recursive --config="$(pwd)/.tflint.hcl" ) || echo "SKIP: tflint not installed locally; CI will gate tflint"

# Optional 3 — terraform validate per env (matches static-tf.yml lines 50-59)
command -v terraform >/dev/null && ( cd infra/envs/prod && terraform init -backend=false && terraform validate ) || echo "SKIP: terraform not installed locally; CI will gate validate (prod)"
command -v terraform >/dev/null && ( cd infra/envs/dev  && terraform init -backend=false && terraform validate ) || echo "SKIP: terraform not installed locally; CI will gate validate (dev)"

echo "OK: all hard grep guards passed"
    </automated>
  </verify>
  <done>
- All 6 grep guards exit 0 with the expected "empty" / "present" semantics described above.
- If `terraform` / `tflint` are installed locally, the three optional gates also pass: tflint reports `0 issue(s) found.`, `terraform fmt -check` exits 0, and `terraform validate` exits 0 on both envs.
- The changes are committed in a single commit (delete-only diff across six file edits).
- On PR #11, the `static-tf` GitHub Actions check transitions from RED to GREEN on the next CI run after the commit lands on `docs/05-verify-work-close-out`. (Verify via `gh pr checks 11` or the PR UI; if static-tf is still red, the executor must NOT amend with code changes unrelated to this plan's scope — escalate as a Phase 7 planning concern.)
  </done>
</task>

</tasks>

<verification>
The plan is verified complete when:
1. `git diff --stat` shows exactly 6 files modified (the four file groups, with `infra/envs/prod/main.tf` + `infra/envs/dev/main.tf` counted separately).
2. `git diff` shows ONLY deletions (no `+` lines except those that result from `terraform fmt`'s column-realignment of `=` signs in the two `module "identity"` argument blocks — and those are allowed because they are not new content, just whitespace normalization on lines already touched).
3. All six grep guards from `<verify>` exit 0 with the expected output semantics.
4. On the next CI run after merge to the PR branch, the `static-tf` job's "Run tflint" step exits 0 (`0 issue(s) found.`).
</verification>

<success_criteria>
- `static-tf` GitHub Actions check on PR #11 (`docs/05-verify-work-close-out`) goes GREEN on the next CI run.
- Zero residual references to `logout_redirect_uri` in any `infra/**/*.tf` file.
- Zero residual references to `swa_origin` in `infra/modules/identity/**/*.tf`.
- Zero `swa_origin = var.swa_origin` lines in `infra/envs/{prod,dev}/main.tf` (identity-module pass-through gone).
- Env-level `variable "swa_origin"` in `infra/envs/{prod,dev}/variables.tf` is STILL PRESENT (negative-space contract preserved — CORS path via `locals.allowed_origins_csv` is unchanged).
- One delete-only commit; no out-of-scope edits.
</success_criteria>

<output>
After completion, create `.planning/quick/260527-q87-delete-dead-tflint-flagged-variables-log/260527-q87-SUMMARY.md` capturing:
- The six files edited (with line-anchors of the deletions).
- Confirmation that the negative-space contract held (env-level `var.swa_origin` untouched).
- The PR #11 CI run URL where `static-tf` transitioned RED → GREEN.
- A one-line forward note: this closes the tflint-flagged dead-code gap; no further action needed for Phase 6 close-out, and Phase 7 (Profile & Resume Upload) remains the next real work surface (per STATE.md current position).
</output>
