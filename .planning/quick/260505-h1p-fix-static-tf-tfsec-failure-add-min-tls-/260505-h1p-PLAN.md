---
quick_id: 260505-h1p
slug: fix-static-tf-tfsec-failure-add-min-tls-
description: Fix static-tf tfsec failure — add min_tls_version=TLS1_2 to tfstate storage account
date: 2026-05-05
status: planned
---

# Quick Task 260505-h1p: Fix static-tf tfsec failure on PR #4

## Context

CI run https://github.com/AdrianZaplata/job-rag/actions/runs/25366641088 fails the
`static-tf` workflow on the tfsec gate with rule `azure-storage-use-secure-tls-policy`
(CRITICAL). The bootstrap state-backend storage account (`infra/bootstrap/main.tf`,
`azurerm_storage_account "tfstate"`) ships without `min_tls_version`, so it accepts
TLS 1.0 / 1.1 — a real security regression on the resource holding remote tfstate.

PR: https://github.com/AdrianZaplata/job-rag/pull/4
Branch: test/static-tf-smoke
Phase: 03-infrastructure-ci-cd (UAT Test 2)

## Tasks

### Task 1: Add min_tls_version to tfstate storage account

- **Files:** `infra/bootstrap/main.tf`
- **Action:** Inside `resource "azurerm_storage_account" "tfstate"` (currently lines
  52-81), add `min_tls_version = "TLS1_2"` near the other top-level account
  properties (account_tier, account_replication_type). Do not reorganize the block.
- **Verify:** `cd infra/bootstrap && terraform fmt && terraform validate -backend=false`
- **Done:** File contains `min_tls_version = "TLS1_2"` inside the tfstate storage
  account resource; `terraform validate` reports success.

### Task 2: Commit and push

- **Action:**
  - `git add infra/bootstrap/main.tf`
  - Commit with message: `fix(infra/bootstrap): set min_tls_version=TLS1_2 on tfstate storage account`
  - `git push origin test/static-tf-smoke` (also pushes the local-only commit
    a387143 from the previous quick task — single push covers both)
- **Done:** Both commits visible on origin/test/static-tf-smoke.

### Task 3: Watch CI and react

- **Action:** `gh pr checks 4 --watch` — wait for static-tf to complete.
- **Done (success path):** static-tf goes fully green (fmt + tflint + tfsec + per-env
  validate). Proceed to Task 4.
- **Done (failure path):** If a NEW failure appears on a different gate, capture it
  in SUMMARY.md and STOP — do not iterate. Out of scope:
  - `lint-and-test` ruff failures in `src/job_rag/config.py` and
    `src/job_rag/services/ingestion.py` (pre-existing).
  - Any modification to `infra/.tflint.hcl` or `infra/envs/prod/prod.tfvars`
    (already fixed in ff0697c and e2a061e).

### Task 4: Update UAT (only on static-tf success)

- **Files:** `.planning/phases/03-infrastructure-ci-cd/03-UAT.md`
- **Action:**
  - Test 2 result: change `issue` → `pass`; remove `reported` and `severity` fields.
  - Summary counts: passed `1 → 2`, issues `1 → 0`, pending `16` (unchanged).
  - Gaps section: remove the Test 2 entry; if no other entries remain, replace the
    Gaps section body with `[none yet]`.
- **Done:** UAT reflects Test 2 passing.

## must_haves

- **Truths:**
  - Bootstrap state-backend storage account enforces TLS 1.2 minimum.
  - tfsec `azure-storage-use-secure-tls-policy` no longer fires on `static-tf`.
- **Artifacts:**
  - `infra/bootstrap/main.tf` (one-line addition inside tfstate storage account)
  - `.planning/phases/03-infrastructure-ci-cd/03-UAT.md` (Test 2 → pass, on success)
  - `${QUICK_DIR}/260505-h1p-SUMMARY.md`
- **Key links:**
  - PR #4: https://github.com/AdrianZaplata/job-rag/pull/4
  - Failing run: https://github.com/AdrianZaplata/job-rag/actions/runs/25366641088
  - tfsec rule: azure-storage-use-secure-tls-policy

## Out of scope

- Pre-existing ruff failures (`src/job_rag/config.py`, `src/job_rag/services/ingestion.py`).
- Edits to `infra/.tflint.hcl` or `infra/envs/prod/prod.tfvars`.
- Any other tfsec/tflint/checkov findings not part of this CI run's blocking failure.
